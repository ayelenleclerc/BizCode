# Componentes de ejecución (no tablas)

**Alcance:** ejecutables, scripts, accesos directos en la copia del legado. **Solo documentar comportamiento** si hay evidencia (manual, archivo de texto, salida legítima). Si no: indicar **no evidenciado**.

## Carpetas típicas

| Ubicación | Contenido observado en copias |
|-----------|-------------------------------|
| `DOSPrint/` | `DOSPrint.exe`, `DOSPrintUI.exe`, documentación PDF |
| `DOSPRINTER/` | Subcarpetas de drivers / HP u otros |
| `esc/` | `.lnk`, `.pif` (p. ej. `STOCK.pif`, `IMPRESORA.lnk`) |
| `sistema/` | `.EXE` del producto (si existen) — listar por nombre cuando se inventarie |

## Estado

- Completar con listado de archivos por extensión (`.exe`, `.bat`, `.pif`) desde la copia real.
