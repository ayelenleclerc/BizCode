openapi: 3.1.0
info:
  title: BizCode API
  version: 0.1.0
  description: |
    REST API for BizCode commercial management system.
    Runs as an Express 5 sidecar on localhost:3001.
    Responses use a JSON envelope `{ success: true, data: ... }` except `/api/health`.
    Session authentication is required for protected resources.
    Protected endpoints can optionally receive `x-bizcode-channel` to enforce channel scope authorization.
    All routes accept optional `X-Request-Id` (max 128 characters); invalid or empty values are ignored and a UUID is assigned. The effective id is echoed on every response (`X-Request-Id`; see runtime middleware `correlationId`).
    Interactive documentation (Swagger UI): http://localhost:3001/api-docs/ (same contract as this file).
  contact:
    name: BizCode Team

servers:
  - url: http://localhost:3001
    description: Local development / Tauri sidecar

tags:
  - name: health
    description: Health check
  - name: auth
    description: Authentication and session bootstrap
  - name: clientes
    description: Customer management
  - name: articulos
    description: Product management
  - name: rubros
    description: Product categories
  - name: proveedores
    description: Suppliers (master data and CSV import)
  - name: compras
    description: Purchase orders to suppliers (#135)
  - name: recuentos
    description: Physical inventory count (#136)
  - name: repartos
    description: Delivery routes grouping orders (#140)
  - name: logistica
    description: Logistics KPIs and operational reports (#145)
  - name: facturas
    description: Invoice management
  - name: notasCredito
    description: Credit notes (invoice void flow, module billing.credit_notes, #146)
  - name: afip
    description: Argentina AFIP electronic invoicing (CAE; homologación mock in dev)
  - name: pedidos
    description: Commercial orders and quotes (ADR-0009)
  - name: contratos
    description: Recurring service contracts and scheduled invoicing (#245, module service.contracts)
  - name: ordenes-trabajo
    description: Service work orders / repair tickets (#246, module service.orders)
  - name: garantias
    description: Product warranties by serial / IMEI (#251, module service.warranties)
  - name: cajas
    description: Cash drawers and cashier shifts (#247, module pos.cashier)
  - name: listas-precios
    description: Customer price lists with quantity-tiered pricing (#234, module catalog.pricelists)
  - name: categorias-articulo
    description: Hierarchical article categories and attributes for variants (#235, module catalog.variants)
  - name: depositos
    description: Multi-warehouse deposits and transfers (#236, module inventory.warehouses)
  - name: comisiones
    description: Seller commissions configuration and monthly settlements (#237, module finance.commissions)
  - name: importaciones
    description: Bulk Excel/CSV import jobs with dry-run and SSE progress (#238, module platform.data_import)
  - name: cobros
    description: Customer payment collections
  - name: portal
    description: B2B customer self-service portal (#240)
  - name: mercadopago
    description: Mercado Pago online payments integration (#174+)
  - name: cobranzas
    description: Overdue invoice reminders (#134)
  - name: cheques
    description: Check portfolio — physical and ECHEQ (#231, module fiscal.cheques)
  - name: formasPago
    description: Payment methods
  - name: users
    description: User management (requires users.manage permission)
  - name: chat
    description: Internal chat conversations and messages
  - name: reportes
    description: Financial and operational reports
  - name: contabilidad
    description: Accounting exports — Libro IVA Ventas Fase 1 (#147); Compras out of scope
  - name: dashboard
    description: Operational dashboard summary and historical sales analytics (#138)
  - name: notifications
    description: In-app notifications
  - name: audit
    description: Audit event history (read-only)
  - name: modules
    description: Platform module catalog and feature-flag metadata
  - name: platform
    description: Super-admin platform operations (tenant configuration)

paths:
  /api/auth/setup-owner:
    post:
      tags: [auth]
      summary: Bootstrap first owner account
      description: One-time endpoint to create initial tenant and owner user.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetupOwnerInput'
      responses:
        '201':
          description: Owner created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetupOwnerEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/ConflictError'

  /api/auth/login:
    post:
      tags: [auth]
      summary: Create authenticated session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginInput'
      responses:
        '200':
          description: Session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid credentials
          headers:
            X-RateLimit-Limit:
              schema:
                type: integer
              example: 5
            X-RateLimit-Remaining:
              schema:
                type: integer
            X-RateLimit-Reset:
              schema:
                type: string
                format: date-time
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '429':
          description: Account locked after too many failed login attempts
          headers:
            X-RateLimit-Limit:
              schema:
                type: integer
              example: 5
            X-RateLimit-Remaining:
              schema:
                type: integer
              example: 0
            X-RateLimit-Reset:
              schema:
                type: string
                format: date-time
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/auth/logout:
    post:
      tags: [auth]
      summary: Revoke current session
      responses:
        '200':
          description: Session revoked (or already missing)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogoutEnvelope'

  /api/auth/me:
    get:
      tags: [auth]
      summary: Get authenticated claims
      responses:
        '200':
          description: Current authenticated claims
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthClaimsEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/health:
    get:
      tags: [health]
      summary: Health check
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: API is running
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  /api/metrics:
    get:
      tags: [health]
      summary: In-memory observability metrics
      description: |
        Returns aggregated technical counters kept in process memory.
        This endpoint excludes tenant/user identifiers and client hints.
      security:
        - cookieSession: []
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: Aggregated in-memory metrics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetricsEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Metrics endpoint disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/modules/catalog:
    get:
      tags: [modules]
      summary: Module catalog (feature flags metadata)
      description: |
        Returns the full module catalog with dependency graph metadata, deployment environment,
        and business presets. Does not include tenant-specific enabled modules (see `/api/me/features`).
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: Module catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModuleCatalogEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/me/features:
    get:
      tags: [modules]
      summary: Enabled modules for current tenant
      description: Returns module and integration keys enabled for the authenticated user's tenant.
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: Tenant feature flags
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantFeaturesEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/planes:
    get:
      tags: [platform]
      summary: Public SaaS plan catalog
      description: Lists active subscription plans with limits and feature keys (no authentication required).
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: Plan catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanCatalogEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/me/plan:
    get:
      tags: [platform]
      summary: Current tenant SaaS plan and usage
      description: Returns plan limits, enabled plan features, and current usage for the authenticated tenant.
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: Tenant plan snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantPlanSnapshotEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/superadmin/stats:
    get:
      tags: [platform]
      summary: Platform-wide tenant statistics (super admin)
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      responses:
        '200':
          description: Global platform stats (metadata only)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuperadminGlobalStatsEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/superadmin/tenants:
    get:
      tags: [platform]
      summary: List tenants (super admin)
      parameters:
        - $ref: '#/components/parameters/XRequestId'
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Filter by tenant name or slug (case-insensitive)
      responses:
        '200':
          description: Tenant list with aggregate counts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuperadminTenantListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [platform]
      summary: Create tenant with default module config (super admin)
      parameters:
        - $ref: '#/components/parameters/XRequestId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuperadminTenantCreateInput'
      responses:
        '201':
          description: Tenant created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuperadminTenantCreateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '409':
          description: Slug already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/superadmin/tenants/{tenantId}:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
    get:
      tags: [platform]
      summary: Tenant detail with platform stats (super admin)
      responses:
        '200':
          description: Tenant metadata and aggregate stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuperadminTenantDetailEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
    patch:
      tags: [platform]
      summary: Suspend or reactivate tenant (super admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuperadminTenantPatchInput'
      responses:
        '200':
          description: Updated tenant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuperadminTenantDetailEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/superadmin/tenants/{tenantId}/config:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
    get:
      tags: [platform]
      summary: Get tenant module configuration (super admin)
      responses:
        '200':
          description: Tenant configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantConfigEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
    put:
      tags: [platform]
      summary: Update tenant module configuration (super admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantConfigUpsertBody'
      responses:
        '200':
          description: Updated configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantConfigEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/superadmin/tenants/{tenantId}/config/history:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
      - $ref: '#/components/parameters/ListLimit'
      - $ref: '#/components/parameters/ListOffset'
    get:
      tags: [platform]
      summary: Tenant configuration change history (super admin)
      responses:
        '200':
          description: History entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantConfigHistoryEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/superadmin/tenants/{tenantId}/config/apply-template:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
    post:
      tags: [platform]
      summary: Apply module preset to tenant (super admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantConfigApplyTemplateBody'
      responses:
        '200':
          description: Configuration after preset
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantConfigEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/superadmin/tenants/{tenantId}/plan:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
    post:
      tags: [platform]
      summary: Change tenant SaaS plan (super admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantPlanChangeBody'
      responses:
        '200':
          description: Updated tenant plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantPlanSnapshotEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/superadmin/tenants/{tenantId}/pricing:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
      - name: modules
        in: query
        required: false
        schema:
          type: string
        description: Comma-separated module keys for price preview without persisting
    get:
      tags: [platform]
      summary: Estimated monthly pricing for tenant (super admin)
      responses:
        '200':
          description: Pricing breakdown
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantPricingEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/superadmin/tenants/{tenantId}/trials:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
    get:
      tags: [platform]
      summary: List active module trials for tenant (super admin)
      responses:
        '200':
          description: Active trials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantModuleTrialListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
    post:
      tags: [platform]
      summary: Activate or extend module trial (super admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantModuleTrialActivateBody'
      responses:
        '201':
          description: Trial activated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantModuleTrialEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/superadmin/tenants/{tenantId}/trials/{moduleKey}:
    parameters:
      - $ref: '#/components/parameters/TenantIdPath'
      - name: moduleKey
        in: path
        required: true
        schema:
          type: string
    delete:
      tags: [platform]
      summary: Deactivate module trial (super admin)
      responses:
        '200':
          description: Trial deactivated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantModuleTrialEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Trial not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/clientes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [clientes]
      summary: List customers
      parameters:
        - name: q
          in: query
          schema:
            type: string
          description: Filter by código, razón social or CUIT (Express query param)
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: List of customers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteListEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [clientes]
      summary: Create a customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteInput'
      responses:
        '200':
          description: Customer created (Express returns 200 with body)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/import/template:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [clientes]
      summary: Download UTF-8 CSV template for bulk customer import
      responses:
        '200':
          description: CSV with fixed header row and one example data row (UTF-8 BOM)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/import:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [clientes]
      summary: Import customers from a CSV file
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: UTF-8 CSV; first row must match the template headers exactly
      responses:
        '200':
          description: Import summary (rows created vs skipped with per-row errors)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [clientes]
      summary: Get customer by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Customer or null if not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteNullableEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    put:
      tags: [clientes]
      summary: Update a customer
      parameters:
        - $ref: '#/components/parameters/BizcodeChannelHeader'
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteInput'
      responses:
        '200':
          description: Customer updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/cuenta-corriente:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Customer accounts-receivable statement
      description: Ledger movements, current balance, credit-limit alert, and 6-month debt series (#232). Requires finance.ledger module.
      parameters:
        - name: tipo
          in: query
          schema:
            type: string
            enum: [saldo_inicial, factura, nota_credito, cobro, retencion, percepcion, cheque_rechazado, ajuste]
        - name: desde
          in: query
          description: ISO-8601 filter start (inclusive)
          schema:
            type: string
            format: date-time
        - name: hasta
          in: query
          description: ISO-8601 filter end (inclusive)
          schema:
            type: string
            format: date-time
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Statement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteCuentaCorrienteEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/cuenta-corriente/saldo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Customer accounts-receivable balance only
      responses:
        '200':
          description: Balance snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteCuentaCorrienteSaldoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/cuenta-corriente/antiguedad:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Customer AR aging buckets
      parameters:
        - name: asOf
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Aging buckets 0-30, 31-60, 61-90, +90 days
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteCuentaCorrienteAntiguedadEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/cuenta-corriente/estado-de-cuenta/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Download customer account statement PDF
      parameters:
        - name: desde
          in: query
          schema:
            type: string
            format: date-time
        - name: hasta
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: PDF document
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/cuenta-corriente/estado-de-cuenta/enviar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [clientes]
      summary: Email customer account statement PDF
      description: Uses tenant SMTP when configured; returns success without error when SMTP is absent (#232).
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteCuentaCorrienteEnviarInput'
      responses:
        '200':
          description: Send accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteCuentaCorrienteEnviarEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/cuenta-corriente/ajuste:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [clientes]
      summary: Manual customer ledger adjustment
      description: Requires sales.create; posts audit event cliente_cc_ajuste.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteCuentaCorrienteAjusteInput'
      responses:
        '201':
          description: Adjustment posted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovimientoClienteCCEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/facturas-pendientes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Pending customer invoices for receipt allocation
      description: Active invoices with remaining balance for imputation (#233). Requires module `finance.receipts`.
      responses:
        '200':
          description: Pending invoices
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacturaPendienteClienteListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/recibos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Customer payment receipt history
      description: Requires module `finance.receipts` and permission `reports.financial.read`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated payment receipts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReciboCobroListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [clientes]
      summary: Register customer payment receipt
      description: |
        Creates `ReciboCobro` with tenant-correlative `numero`, posts `cobro` ledger movement (negative monto = bruto),
        optional `RetencionAplicada` rows, FIFO or manual imputaciones, and audit event `recibo_cobro_create`.
        Requires module `finance.receipts` and `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReciboCobroInput'
      responses:
        '201':
          description: Payment receipt created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReciboCobroEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/recibos/{reciboId}/anular:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: reciboId
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [clientes]
      summary: Void customer payment receipt
      description: Sets estado `anulado` and posts compensating ledger movement. Audit `recibo_cobro_void`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReciboCobroVoidInput'
      responses:
        '200':
          description: Receipt voided
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReciboCobroEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '422':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/clientes/{id}/recibos/{reciboId}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: reciboId
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [clientes]
      summary: Customer payment receipt PDF
      description: Returns PDF with amount in words (#233). Requires module `finance.receipts`.
      responses:
        '200':
          description: PDF document
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [articulos]
      summary: List products
      parameters:
        - name: q
          in: query
          schema:
            type: string
          description: Filter by code or description
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: List of products
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloListEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [articulos]
      summary: Create a product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticuloInput'
      responses:
        '200':
          description: Product created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [articulos]
      summary: Get product by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Product or null
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloNullableEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    put:
      tags: [articulos]
      summary: Update a product
      parameters:
        - $ref: '#/components/parameters/BizcodeChannelHeader'
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticuloInput'
      responses:
        '200':
          description: Product updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/stock-historial:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [articulos]
      summary: List manual stock adjustments for a product
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated adjustment history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockAjusteListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Product not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/articulos/{id}/proveedores:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [articulos]
      summary: Compare supplier catalog prices for a product (#274)
      description: |
        Lists active suppliers with an active catalog entry for the article, including list price,
        price age, and last received purchase order date. Requires module `logistics.purchases` and
        permission `products.read` or `suppliers.read`.
      parameters:
        - name: sortBy
          in: query
          schema:
            type: string
            enum: [precio, precioListaFecha, ultimaCompra]
        - name: sortDir
          in: query
          schema:
            type: string
            enum: [asc, desc]
      responses:
        '200':
          description: Supplier price comparison for the article
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloProveedoresComparadorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/stock-ajuste:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [articulos]
      summary: Apply a manual stock adjustment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockAjusteInput'
      responses:
        '200':
          description: Adjustment applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockAdjustEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Product not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Insufficient stock (INSUFFICIENT_STOCK)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/articulos/import/template:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [articulos]
      summary: Download UTF-8 CSV template for bulk product import
      responses:
        '200':
          description: CSV with fixed header row and one example row (UTF-8 BOM)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/import:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [articulos]
      summary: Import products from CSV (rubroCodigo must match an existing Rubro.codigo)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: UTF-8 CSV; first row must match the template headers exactly
      responses:
        '200':
          description: Import summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/migrate-dbf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [articulos]
      summary: Import products from legacy ARTICULOS.DBF (rubros must exist first)
      description: Requires `settings.business.manage`. Import `RUBROS.DBF` before `ARTICULOS.DBF`.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: FoxPro DBF file (cp437)
      responses:
        '200':
          description: Import summary (created, updated, per-row errors)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/rubros:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [rubros]
      summary: List product categories
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: List of categories
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RubroListEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [rubros]
      summary: Create a category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RubroInput'
      responses:
        '200':
          description: Category created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RubroEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/rubros/import/template:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [rubros]
      summary: Download UTF-8 CSV template for bulk category import
      responses:
        '200':
          description: CSV with fixed header row and one example row (UTF-8 BOM)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/rubros/import:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [rubros]
      summary: Import product categories from CSV
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Import summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/rubros/migrate-dbf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [rubros]
      summary: Import product categories from legacy RUBROS.DBF
      description: Requires `settings.business.manage`. Upserts by tenant and codigo.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: FoxPro DBF file (cp437)
      responses:
        '200':
          description: Import summary (created, updated, per-row errors)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [proveedores]
      summary: List suppliers
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
        - name: activo
          in: query
          required: false
          schema:
            type: string
            enum: ['true', 'false']
          description: Filter by active flag (logical status).
        - name: categoria
          in: query
          required: false
          schema:
            type: string
            enum: [materia_prima, insumos, servicios, logistica]
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Supplier list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorListEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [proveedores]
      summary: Create supplier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProveedorInput'
      responses:
        '200':
          description: Supplier created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/import/template:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [proveedores]
      summary: Download UTF-8 CSV template for bulk supplier import
      responses:
        '200':
          description: CSV with fixed header row and one example row (UTF-8 BOM)
          content:
            text/csv:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/import:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [proveedores]
      summary: Import suppliers from CSV
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Import summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/comparar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [proveedores]
      summary: Compare supplier prices for an article (#274)
      description: |
        Same payload as `GET /api/articulos/{id}/proveedores` with `articuloId` as a query parameter.
        Requires module `logistics.purchases` and permission `products.read` or `suppliers.read`.
      parameters:
        - name: articuloId
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
        - name: sortBy
          in: query
          schema:
            type: string
            enum: [precio, precioListaFecha, ultimaCompra]
        - name: sortDir
          in: query
          schema:
            type: string
            enum: [asc, desc]
      responses:
        '200':
          description: Supplier price comparison
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloProveedoresComparadorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [proveedores]
      summary: Get supplier by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Supplier or null
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorNullableEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    put:
      tags: [proveedores]
      summary: Update supplier
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProveedorInput'
      responses:
        '200':
          description: Supplier updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Duplicate codigo for tenant
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    delete:
      tags: [proveedores]
      summary: Deactivate supplier (logical delete)
      description: Sets `activo` to false. Does not remove rows referenced by purchase orders or vouchers.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Supplier deactivated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/historial:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Supplier purchase history summary (#272)
      description: Aggregated purchase metrics, top articles, and OC/comprobante list for a rolling period.
      parameters:
        - name: dias
          in: query
          description: Rolling period length in days (default 90)
          schema:
            type: integer
            enum: [30, 90, 180, 365]
            default: 90
      responses:
        '200':
          description: History summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorHistorialEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/articulos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Supplier purchased articles with weighted average price (#272)
      description: PPP and price evolution from received purchase-order lines in the selected period.
      parameters:
        - name: dias
          in: query
          description: Rolling period length in days (default 90)
          schema:
            type: integer
            enum: [30, 90, 180, 365]
            default: 90
      responses:
        '200':
          description: Article metrics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorArticulosHistorialEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/catalogo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Supplier catalog — article codes and list prices (#273)
      description: Requires module `logistics.purchases` and permission `suppliers.read`.
      responses:
        '200':
          description: Catalog items for the supplier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorCatalogoListEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [proveedores]
      summary: Add article to supplier catalog (#273)
      description: Requires module `logistics.purchases` and permission `suppliers.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProveedorCatalogoInput'
      responses:
        '201':
          description: Catalog entry created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorCatalogoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          $ref: '#/components/responses/ConflictError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/catalogo/import:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [proveedores]
      summary: Import supplier catalog from CSV (#273)
      description: |
        CSV columns: `codigo_proveedor`, `codigo_interno` (article `codigo`), optional `precio`, `unidad`.
        Requires module `logistics.purchases` and permission `suppliers.manage`.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Import summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorCatalogoImportEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/catalogo/{articuloId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: articuloId
        in: path
        required: true
        schema:
          type: integer
    put:
      tags: [proveedores]
      summary: Update supplier catalog entry (#273)
      description: Requires module `logistics.purchases` and permission `suppliers.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProveedorCatalogoUpdateInput'
      responses:
        '200':
          description: Updated catalog entry
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorCatalogoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          $ref: '#/components/responses/ConflictError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/cuenta-corriente:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Supplier accounts-payable statement
      description: Ledger movements, current balance, credit-limit alert, and 6-month debt series (#270).
      parameters:
        - name: tipo
          in: query
          schema:
            type: string
            enum: [factura_compra, pago, nc_proveedor, ajuste]
        - name: from
          in: query
          description: ISO-8601 filter start (inclusive)
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: ISO-8601 filter end (inclusive)
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Statement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorCuentaCorrienteEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/cuenta-corriente/saldo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Supplier accounts-payable balance only
      responses:
        '200':
          description: Balance snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProveedorCuentaCorrienteSaldoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/cuenta-corriente/ajuste:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [proveedores]
      summary: Manual supplier ledger adjustment
      description: Requires suppliers.manage; posts audit event proveedor_cc_ajuste.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProveedorCuentaCorrienteAjusteInput'
      responses:
        '201':
          description: Movement created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovimientoProveedorCCEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/pagos/comprobantes-pendientes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Pending purchase vouchers for payment allocation
      description: Active comprobantes with remaining balance for imputation (#271). Requires module `finance.receipts`.
      responses:
        '200':
          description: Pending vouchers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComprobantePendienteListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/pagos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Supplier payment receipt history
      description: Requires module `finance.receipts` and permission `suppliers.read`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated payment receipts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReciboPagoListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [proveedores]
      summary: Register supplier payment
      description: |
        Creates `ReciboPago` with tenant-correlative `numero`, posts `pago` ledger movement (negative monto = sum of facturas/bruto),
        optional `RetencionAplicada` rows (#276), and audit event `recibo_pago_create`.
        `total` is net cash (sum facturas − sum retenciones). Requires module `finance.receipts` and `suppliers.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReciboPagoInput'
      responses:
        '201':
          description: Payment receipt created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReciboPagoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/{id}/pagos/{reciboId}/anular:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: reciboId
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [proveedores]
      summary: Void supplier payment receipt
      description: Sets estado `anulado` and posts compensating ledger movement. Audit `recibo_pago_void`.
      responses:
        '200':
          description: Receipt voided
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReciboPagoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '422':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/proveedores/facturas-pendientes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [proveedores]
      summary: List unpaid supplier vouchers with due-date alert state
      description: Requires module finance.ledger and permission suppliers.read (#275).
      parameters:
        - name: estado
          in: query
          schema:
            $ref: '#/components/schemas/FacturaPendienteEstado'
        - name: proveedorId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Pending payable vouchers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacturasPendientesEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/configuracion/alertas-proveedores:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [proveedores]
      summary: Get supplier payable alert settings
      description: Requires module finance.ledger and permission suppliers.read (#275).
      responses:
        '200':
          description: Alert configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertaProveedorConfigEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: [proveedores]
      summary: Update supplier payable alert settings
      description: Requires module finance.ledger and permission suppliers.manage (#275).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AlertaProveedorConfigInput'
      responses:
        '200':
          description: Updated configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertaProveedorConfigEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/proveedores/{id}/pagos/{reciboId}/retenciones:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: reciboId
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: List withholdings applied to a payment receipt (#276)
      description: Requires module `finance.receipts` and permission `suppliers.read`.
      responses:
        '200':
          description: Applied withholdings
          content:
            application/json:
              schema:
                type: object
                required: [success, data]
                properties:
                  success:
                    type: boolean
                    const: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ReciboPagoRetencion'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/proveedores/{id}/pagos/{reciboId}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
      - name: reciboId
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [proveedores]
      summary: Download supplier payment receipt PDF
      description: PDF with company header, supplier data, imputed vouchers, payment method (#271).
      responses:
        '200':
          description: PDF document
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/compras:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [compras]
      summary: List purchase orders
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: estado
          in: query
          schema:
            type: string
            enum: [draft, sent, received, cancelled]
        - name: proveedorId
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Paginated purchase orders
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [compras]
      summary: Create purchase order (draft)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenCompraCreateInput'
      responses:
        '201':
          description: Purchase order created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Supplier not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid article line
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/compras/{id}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [compras]
      summary: Download purchase order PDF (#323)
      description: |
        Returns a PDF with supplier catalog code and description per line when snapshotted.
        Requires module `logistics.purchases` and permission `suppliers.read`.
      responses:
        '200':
          description: Purchase order PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/compras/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [compras]
      summary: Get purchase order by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Purchase order detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [compras]
      summary: Update draft purchase order
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenCompraUpdateInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Not editable or invalid lines
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/compras/{id}/send:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [compras]
      summary: Mark purchase order as sent
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/compras/{id}/cancel:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [compras]
      summary: Cancel purchase order
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/compras/{id}/receive:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [compras]
      summary: Receive goods (partial allowed)
      description: |
        Requires `suppliers.manage` and `inventory.adjust`. Creates `StockAjuste` rows
        with motivo `compra` per line in a single transaction.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenCompraReceiveInput'
      responses:
        '200':
          description: Received (full or partial)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Not receivable or quantity exceeds pending
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/recuentos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [recuentos]
      summary: List physical inventory counts
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated inventory counts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecuentoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [recuentos]
      summary: Start inventory count (snapshot active articles)
      description: Requires `inventory.count`. Only one `in_progress` count per tenant.
      responses:
        '201':
          description: Count started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecuentoEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Count already in progress
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/recuentos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [recuentos]
      summary: Get inventory count by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Count detail with items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecuentoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/recuentos/{id}/items:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    put:
      tags: [recuentos]
      summary: Update physical quantities (partial allowed)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecuentoItemsInput'
      responses:
        '200':
          description: Updated count
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecuentoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Not editable or invalid line
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/recuentos/{id}/close:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [recuentos]
      summary: Close inventory count and apply stock adjustments
      description: |
        Requires all items to have `cantFisica`. For each line with non-zero variance,
        updates article stock and creates `StockAjuste` with motivo `recuento`.
        Lines with zero variance do not create adjustments.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Count closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecuentoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Incomplete items, not closable, or insufficient stock
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/recuentos/{id}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [recuentos]
      summary: Download variance PDF (closed counts only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: PDF document
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Count not closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [facturas]
      summary: List invoices
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: List of invoices with items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacturaListEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [facturas]
      summary: Create an invoice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FacturaInput'
      responses:
        '200':
          description: Invoice created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacturaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Client suspended, insufficient stock, or other business rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/arca/config:
    get:
      tags: [afip]
      summary: AFIP fiscal config status (metadata only)
      description: Requires `settings.fiscal.manage`. Does not return certificate or private key.
      responses:
        '200':
          description: Config status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArcaConfigStatusEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [afip]
      summary: Upsert tenant AFIP credentials
      description: Requires `settings.fiscal.manage`. Certificate and key encrypted at rest.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArcaConfigInput'
      responses:
        '200':
          description: Config saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArcaConfigEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/arca/auth:
    post:
      tags: [afip]
      summary: Request AFIP TA (ticket de acceso)
      description: Requires `settings.fiscal.manage`. Homologación mock when AFIP network is not configured.
      responses:
        '200':
          description: TA issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArcaTaEnvelope'
        '404':
          description: Fiscal config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/arca/cae:
    post:
      tags: [afip]
      summary: Request CAE for a factura
      description: Requires `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArcaCaeInput'
      responses:
        '200':
          description: CAE issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArcaCaeEnvelope'
        '404':
          description: Factura or config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '502':
          description: AFIP request failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/regimenes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: List withholding/perception regimes (#228)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      parameters:
        - name: activo
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Regime list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegimenRetencionListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [contabilidad]
      summary: Create withholding/perception regime (#228)
      description: Requires `finance.retenciones`, `settings.fiscal.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegimenRetencionInput'
      responses:
        '201':
          description: Regime created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegimenRetencionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/regimenes/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    put:
      tags: [contabilidad]
      summary: Update withholding/perception regime (#228)
      description: Requires `finance.retenciones`, `settings.fiscal.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegimenRetencionUpdateInput'
      responses:
        '200':
          description: Regime updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegimenRetencionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Regime not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/config-retenciones:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: Tenant withholding agent flags (#228)
      description: Requires `finance.retenciones`, `settings.fiscal.manage`.
      responses:
        '200':
          description: Agent config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalRetencionesConfigEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [contabilidad]
      summary: Upsert tenant withholding agent flags (#228)
      description: Requires `finance.retenciones`, `settings.fiscal.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FiscalRetencionesConfigInput'
      responses:
        '200':
          description: Config saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalRetencionesConfigEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/retenciones:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: Applied withholdings/perceptions history (#228)
      description: Requires `finance.retenciones`, `reports.financial.read`. Paginated list.
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
        - name: tipo
          in: query
          schema:
            type: string
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated applied withholdings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RetencionAplicadaListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/retenciones/preview:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: entidadTipo
        in: query
        required: true
        schema:
          type: string
          enum: [cliente, proveedor]
      - name: entidadId
        in: query
        required: true
        schema:
          type: integer
          minimum: 1
      - name: monto
        in: query
        required: true
        schema:
          type: number
          exclusiveMinimum: 0
      - name: contexto
        in: query
        schema:
          type: string
          enum: [factura, cobro]
        description: For `entidadTipo=cliente`, `factura` previews perceptions; `cobro` previews withholdings (#229).
      - name: neto1
        in: query
        schema:
          type: number
          minimum: 0
      - name: neto2
        in: query
        schema:
          type: number
          minimum: 0
      - name: neto3
        in: query
        schema:
          type: number
          minimum: 0
    get:
      tags: [contabilidad]
      summary: Preview withholdings/perceptions (#228, #276, #229)
      description: |
        Requires `finance.retenciones`, `reports.financial.read`.
        `entidadTipo=proveedor` — suggested withholdings on supplier payment (#276).
        `entidadTipo=cliente` with `contexto=factura` — perceptions on invoice (neto1+neto2+neto3 base).
        `entidadTipo=cliente` with `contexto=cobro` — customer withholdings on collection (`monto` = bruto).
      responses:
        '200':
          description: Preview result (stub)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RetencionesPreviewEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/retenciones/export:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: format
        in: query
        required: true
        schema:
          type: string
          enum: [sicore, sifere]
      - name: from
        in: query
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        schema:
          type: string
          format: date-time
    get:
      tags: [contabilidad]
      summary: Export applied withholdings TXT (#276)
      description: |
        Requires `finance.retenciones`, `reports.financial.read`.
        `format=sicore` for Ganancias/IVA; `format=sifere` for IIBB. Excludes voided payment receipts.
      responses:
        '200':
          description: TXT export
          content:
            text/plain:
              schema:
                type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/presentaciones/preview:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: formato
        in: query
        required: true
        schema:
          type: string
          enum: [sicore, sifere]
      - name: periodo
        in: query
        required: true
        schema:
          type: string
          pattern: '^\\d{4}-\\d{2}$'
    get:
      tags: [contabilidad]
      summary: Preview monthly SICORE/SIFERE presentation (#242)
      description: |
        Requires `finance.retenciones`, `reports.financial.read`.
        Returns operations for the period with regime totals and CUIT validation warnings.
      responses:
        '200':
          description: Preview envelope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresentacionPreviewEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/presentaciones:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: List generated presentation files (#242)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      responses:
        '200':
          description: History list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresentacionRetencionListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [contabilidad]
      summary: Generate and store presentation TXT (#242)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PresentacionRetencionInput'
      responses:
        '201':
          description: Created presentation record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresentacionRetencionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/presentaciones/{id}/archivo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [contabilidad]
      summary: Download stored presentation TXT (#242)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      responses:
        '200':
          description: TXT file
          content:
            text/plain:
              schema:
                type: string
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/presentaciones/{id}/presentado:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    patch:
      tags: [contabilidad]
      summary: Mark presentation as filed in AFIP (#242)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      responses:
        '200':
          description: Updated presentation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresentacionRetencionEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/fiscal/retenciones/{id}/comprobante/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [contabilidad]
      summary: Download withholding certificate PDF (#276)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      responses:
        '200':
          description: PDF certificate
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/pedidos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [pedidos]
      summary: List commercial orders / quotes
      description: Requires `orders.create` or `reports.operational.read`.
      parameters:
        - name: estado
          in: query
          schema:
            type: string
            enum: [draft, confirmed, invoiced, cancelled]
        - name: clienteId
          in: query
          schema:
            type: integer
            minimum: 1
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated pedidos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [pedidos]
      summary: Create pedido (draft)
      description: Requires `orders.create`. Initial estado is `draft`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoInput'
      responses:
        '201':
          description: Pedido created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Client suspended or business rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/pedidos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [pedidos]
      summary: Get pedido by id
      description: Requires `orders.create` or `reports.operational.read`.
      responses:
        '200':
          description: Pedido detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoEnvelope'
        '404':
          description: Pedido not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [pedidos]
      summary: Update pedido (draft only)
      description: Requires `orders.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoInput'
      responses:
        '200':
          description: Pedido updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Pedido not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Not in draft state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags: [pedidos]
      summary: Cancel pedido (soft)
      description: Requires `sales.cancel`. Sets estado to `cancelled` (not allowed when invoiced).
      responses:
        '200':
          description: Pedido cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoEnvelope'
        '404':
          description: Pedido not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/pedidos/{id}/confirm:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [pedidos]
      summary: Confirm pedido (draft → confirmed)
      description: Requires `orders.create`.
      responses:
        '200':
          description: Pedido confirmed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoEnvelope'
        '404':
          description: Pedido not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/pedidos/{id}/invoice:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [pedidos]
      summary: Invoice pedido (creates Factura)
      description: Requires `sales.create`. Pedido must be `confirmed`. Creates invoice via FacturaService (no CAE in this release).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoInvoiceInput'
      responses:
        '200':
          description: Pedido invoiced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PedidoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Pedido not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Client suspended, insufficient stock
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/pedidos/{id}/remito:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [pedidos, remitos]
      summary: Create delivery note from confirmed pedido
      responses:
        '201':
          description: Draft remito created from pedido
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'

  /api/contratos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contratos]
      summary: List recurring service contracts
      description: Requires module `service.contracts` and `sales.create` or `reports.operational.read`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated contratos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [contratos]
      summary: Create recurring contract
      description: Requires module `service.contracts` and `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContratoInput'
      responses:
        '201':
          description: Contrato created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Client suspended or business rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contratos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [contratos]
      summary: Get contrato by id
      description: Requires module `service.contracts` and `sales.create` or `reports.operational.read`.
      responses:
        '200':
          description: Contrato detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoEnvelope'
        '404':
          description: Contrato not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [contratos]
      summary: Update contrato
      description: Requires module `service.contracts` and `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContratoUpdateInput'
      responses:
        '200':
          description: Contrato updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Contrato not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contratos/{id}/facturas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [contratos]
      summary: List invoices generated for a contract
      description: Requires module `service.contracts` and `sales.create` or `reports.operational.read`.
      responses:
        '200':
          description: Facturas linked to the contrato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoFacturasEnvelope'
        '404':
          description: Contrato not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contratos/{id}/pause:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [contratos]
      summary: Pause an active contract
      description: Requires module `service.contracts` and `sales.create`. Transitions `activo` → `pausado`.
      responses:
        '200':
          description: Contrato paused
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoEnvelope'
        '404':
          description: Contrato not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contratos/{id}/resume:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [contratos]
      summary: Resume a paused contract
      description: Requires module `service.contracts` and `sales.create`. Transitions `pausado` → `activo`.
      responses:
        '200':
          description: Contrato resumed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoEnvelope'
        '404':
          description: Contrato not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contratos/{id}/ajuste-manual:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [contratos]
      summary: Apply a manual percentage price adjustment
      description: Requires module `service.contracts` and `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContratoAjusteManualInput'
      responses:
        '200':
          description: Adjustment applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContratoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Contrato not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-trabajo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [ordenes-trabajo]
      summary: List service work orders
      description: Requires module `service.orders` and `sales.create` or `reports.operational.read`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: estado
          in: query
          schema:
            $ref: '#/components/schemas/OrdenTrabajoEstado'
      responses:
        '200':
          description: Paginated work orders with dashboard counts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenTrabajoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [ordenes-trabajo]
      summary: Create work order
      description: Requires module `service.orders` and `sales.create`. Auto-detects warranty by serial when applicable.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenTrabajoInput'
      responses:
        '201':
          description: Work order created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenTrabajoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Client suspended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-trabajo/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [ordenes-trabajo]
      summary: Get work order by id
      description: Requires module `service.orders` and `sales.create` or `reports.operational.read`.
      responses:
        '200':
          description: Work order detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenTrabajoEnvelope'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [ordenes-trabajo]
      summary: Update work order
      description: Requires module `service.orders` and `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenTrabajoUpdateInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenTrabajoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state for update
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-trabajo/{id}/transicion:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [ordenes-trabajo]
      summary: Transition work-order state
      description: Requires module `service.orders` and `sales.create`. Audits previousEstado → estado.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenTrabajoTransitionInput'
      responses:
        '200':
          description: Transition applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenTrabajoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-trabajo/{id}/facturar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [ordenes-trabajo]
      summary: Convert work order to invoice
      description: Requires module `service.orders` and `sales.create`. Parts decrement stock; labor/service do not.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenTrabajoFacturarInput'
      responses:
        '201':
          description: Invoice created and OT marked facturado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenTrabajoFacturarEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invalid state or already invoiced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Warranty OT cannot be invoiced to customer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/garantias:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [garantias]
      summary: List warranties
      description: Requires module `service.warranties` and `sales.create` or `reports.operational.read`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: estado
          in: query
          schema:
            $ref: '#/components/schemas/GarantiaEstado'
        - name: q
          in: query
          description: Search by serial, IMEI, or customer name
          schema:
            type: string
        - name: proximas
          in: query
          description: When true, only vigentes expiring in the next 3 months
          schema:
            type: boolean
      responses:
        '200':
          description: Paginated warranties with counts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GarantiaListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [garantias]
      summary: Register warranty manually
      description: Requires module `service.warranties` and `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GarantiaRegisterInput'
      responses:
        '201':
          description: Warranty created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GarantiaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/garantias/lookup:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: serial
        in: query
        required: true
        schema:
          type: string
          minLength: 1
    get:
      tags: [garantias]
      summary: Lookup warranty by serial or IMEI
      description: Requires module `service.warranties`.
      responses:
        '200':
          description: Lookup result (vigente, vencida, or sin_registro)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GarantiaLookupEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/garantias/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [garantias]
      summary: Get warranty detail with usage history
      responses:
        '200':
          description: Warranty detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GarantiaEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/garantias/{id}/anular:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [garantias]
      summary: Anular warranty
      responses:
        '200':
          description: Warranty anulada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GarantiaEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Already anulada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/garantias/{id}/usos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [garantias]
      summary: Register warranty usage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GarantiaUsoInput'
      responses:
        '201':
          description: Usage registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GarantiaEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Cannot use vencida or anulada warranty
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/cajas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cajas]
      summary: List cash drawers
      description: Requires module `pos.cashier`.
      responses:
        '200':
          description: Cash drawers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CajaListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [cajas]
      summary: Create cash drawer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nombre]
              properties:
                nombre:
                  type: string
                  minLength: 1
                  maxLength: 80
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CajaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Duplicate nombre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/turnos-caja:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cajas]
      summary: List cashier shifts
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: estado
          in: query
          schema:
            type: string
            enum: [abierto, cerrado]
        - name: cajaId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Paginated shifts with counts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnoCajaListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [cajas]
      summary: Open cashier shift
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TurnoCajaOpenInput'
      responses:
        '201':
          description: Shift opened
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnoCajaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Caja already open
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/turnos-caja/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [cajas]
      summary: Get shift detail
      responses:
        '200':
          description: Shift detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnoCajaEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/turnos-caja/{id}/movimientos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [cajas]
      summary: Register manual cash movement
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MovimientoCajaManualInput'
      responses:
        '201':
          description: Movement registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnoCajaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Turno not open
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/turnos-caja/{id}/cerrar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [cajas]
      summary: Close shift with physical cash count
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TurnoCajaCloseInput'
      responses:
        '200':
          description: Shift closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnoCajaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Turno not open
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/turnos-caja/{id}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [cajas]
      summary: PDF report for closed shift
      responses:
        '200':
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Shift not closed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/listas-precios:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [listas-precios]
      summary: List price lists
      description: Requires module `catalog.pricelists`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: activa
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Paginated price lists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListaPrecioListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [listas-precios]
      summary: Create price list
      description: Requires module `catalog.pricelists`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListaPrecioCreateInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListaPrecioEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Duplicate nombre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/listas-precios/precio-efectivo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [listas-precios]
      summary: Resolve effective price for an article
      description: >-
        Requires module `catalog.pricelists`. Resolution cascade:
        tiered price -> list item (fixed/percent) -> base article price.
      parameters:
        - name: articuloId
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
        - name: listaPrecioId
          in: query
          schema:
            type: integer
            minimum: 1
        - name: cantidad
          in: query
          schema:
            type: number
            exclusiveMinimum: 0
      responses:
        '200':
          description: Effective price
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrecioEfectivoResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/listas-precios/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [listas-precios]
      summary: Get price list with items and tiers
      description: Requires module `catalog.pricelists`.
      responses:
        '200':
          description: Price list detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListaPrecioEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: [listas-precios]
      summary: Update price list
      description: Requires module `catalog.pricelists`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListaPrecioPatchInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListaPrecioEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Duplicate nombre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags: [listas-precios]
      summary: Delete price list
      description: Requires module `catalog.pricelists`.
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: List has customers assigned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/listas-precios/{id}/items:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [listas-precios]
      summary: Upsert a price-list item (with optional tiers)
      description: Requires module `catalog.pricelists`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListaPrecioItemInput'
      responses:
        '200':
          description: Item upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListaPrecioItemEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/listas-precios/{id}/items/{itemId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: itemId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    delete:
      tags: [listas-precios]
      summary: Remove a price-list item
      description: Requires module `catalog.pricelists`.
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/listas-precios/{id}/actualizar-masivo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [listas-precios]
      summary: Bulk percentage update over a price list (preview or apply)
      description: Requires module `catalog.pricelists`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListaPrecioBulkUpdateInput'
      responses:
        '200':
          description: Preview or applied bulk update
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListaPrecioBulkUpdateResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/categorias-articulo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [categorias-articulo]
      summary: List hierarchical article categories
      description: Requires module `catalog.variants`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: padreId
          in: query
          schema:
            oneOf:
              - type: integer
                minimum: 1
              - type: string
                enum: [null]
        - name: activo
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Paginated categories
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoriaArticuloListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [categorias-articulo]
      summary: Create article category
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoriaArticuloCreateInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoriaArticuloEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/categorias-articulo/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [categorias-articulo]
      summary: Get category by id
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Category with attributes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoriaArticuloEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    patch:
      tags: [categorias-articulo]
      summary: Update category
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoriaArticuloPatchInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoriaArticuloEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    delete:
      tags: [categorias-articulo]
      summary: Delete category
      description: Requires module `catalog.variants`. Fails if articles or children exist.
      responses:
        '200':
          description: Deleted
        '409':
          description: Category in use
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/categorias-articulo/{id}/atributos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [categorias-articulo]
      summary: Add attribute to category
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nombre]
              properties:
                nombre:
                  type: string
                  maxLength: 40
                orden:
                  type: integer
                  minimum: 0
                valores:
                  type: array
                  items:
                    type: object
                    required: [valor]
                    properties:
                      valor:
                        type: string
                        maxLength: 40
                      orden:
                        type: integer
                        minimum: 0
      responses:
        '201':
          description: Created attribute
        '409':
          description: Duplicate name
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/categorias-articulo/{id}/atributos/{atributoId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: atributoId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    patch:
      tags: [categorias-articulo]
      summary: Update category attribute
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                nombre:
                  type: string
                  maxLength: 40
                orden:
                  type: integer
                  minimum: 0
      responses:
        '200':
          description: Updated attribute
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    delete:
      tags: [categorias-articulo]
      summary: Delete category attribute
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/categorias-articulo/{id}/atributos/{atributoId}/valores:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: atributoId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [categorias-articulo]
      summary: Add value to category attribute
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [valor]
              properties:
                valor:
                  type: string
                  maxLength: 40
                orden:
                  type: integer
                  minimum: 0
      responses:
        '201':
          description: Created value
        '409':
          description: Duplicate value
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/categorias-articulo/{id}/atributos/{atributoId}/valores/{valorId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: atributoId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: valorId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    delete:
      tags: [categorias-articulo]
      summary: Delete attribute value
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/precio-catalogo-efectivo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [articulos]
      summary: Resolve catalog effective price (offer/override/category cascade)
      description: Requires module `catalog.variants`.
      parameters:
        - name: articuloId
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Catalog effective price
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrecioCatalogoEfectivoResponse'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/variantes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [articulos]
      summary: List variants of a parent article
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Variant list
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/variantes/generar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [articulos]
      summary: Generate combinatorial variants for a parent article
      description: Requires module `catalog.variants`. Marks the article as parent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [atributoValorIdsPorAtributo]
              properties:
                atributoValorIdsPorAtributo:
                  type: array
                  minItems: 1
                  items:
                    type: array
                    minItems: 1
                    items:
                      type: integer
                      minimum: 1
                codigoInicio:
                  type: integer
                  minimum: 1
      responses:
        '201':
          description: Variants created
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/stock-familia:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [articulos]
      summary: Aggregated stock of active variants under a parent
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Family stock aggregate
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/ofertas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [articulos]
      summary: List temporary offers for a variant
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Offers
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [articulos]
      summary: Create temporary offer for a sellable variant
      description: Requires module `catalog.variants`. Rejects parent articles.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [precioOferta, vigenciaDesde, vigenciaHasta]
              properties:
                precioOferta:
                  type: number
                  minimum: 0
                vigenciaDesde:
                  type: string
                  format: date-time
                vigenciaHasta:
                  type: string
                  format: date-time
                activa:
                  type: boolean
      responses:
        '201':
          description: Created
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/ofertas/{ofertaId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: ofertaId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    patch:
      tags: [articulos]
      summary: Update temporary offer
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                precioOferta:
                  type: number
                  minimum: 0
                vigenciaDesde:
                  type: string
                  format: date-time
                vigenciaHasta:
                  type: string
                  format: date-time
                activa:
                  type: boolean
      responses:
        '200':
          description: Updated
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    delete:
      tags: [articulos]
      summary: Delete temporary offer
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/imagenes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [articulos]
      summary: List article images
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Images
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [articulos]
      summary: Upload article image (JPEG/PNG/WebP, max 5MB, max 8 images)
      description: Requires module `catalog.variants`. Multipart field `file`.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Uploaded
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/imagenes/orden:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    put:
      tags: [articulos]
      summary: Reorder article images (first becomes principal)
      description: Requires module `catalog.variants`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ordenIds]
              properties:
                ordenIds:
                  type: array
                  minItems: 1
                  items:
                    type: integer
                    minimum: 1
      responses:
        '200':
          description: Reordered images
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/articulos/{id}/imagenes/{imagenId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
      - name: imagenId
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    delete:
      tags: [articulos]
      summary: Delete article image
      description: Requires module `catalog.variants`.
      responses:
        '200':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/cobros:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cobros]
      summary: List customer payments
      parameters:
        - name: clienteId
          in: query
          schema:
            type: integer
            minimum: 1
        - name: desde
          in: query
          schema:
            type: string
            format: date
          description: Filter fecha >= (YYYY-MM-DD or ISO-8601)
        - name: hasta
          in: query
          schema:
            type: string
            format: date
          description: Filter fecha <= end of day
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated list of payments
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CobroListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [cobros]
      summary: Register a customer payment
      description: |
        Decrements `Cliente.balance` by bruto (`monto` neto + sum of `retenciones[]` when present) in the same transaction (#229).
        When the customer has at least one active invoice (`estado: A`), adjusts `Cliente.score` from the oldest active invoice due date
        (`factura.fecha + creditDays`) vs payment date: on-time +5; 1–10 days late −3; 11–30 −7; >30 −15.
        No score change when there is no active invoice (on-account payment). Audit metadata includes
        `scoreBefore`, `scoreAfter`, `delta`. Requires `sales.create`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CobroInput'
      responses:
        '200':
          description: Payment registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CobroCreateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Client suspended or inactive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/cobros/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cobros]
      summary: Get payment by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Payment detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CobroEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Payment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/cobros/{id}/retenciones:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [cobros]
      summary: List withholdings applied on a customer payment (#229)
      description: Requires `finance.retenciones`, `reports.financial.read`.
      responses:
        '200':
          description: Withholding lines linked to the cobro
          content:
            application/json:
              schema:
                type: object
                required: [success, data]
                properties:
                  success:
                    type: boolean
                    const: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CobroRetencion'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Cobro not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/cobranzas/vencidas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cobranzas]
      summary: List overdue active invoices
      description: |
        Returns active invoices (`estado` A) past due per customer `creditDays`, minus
        `ParamEmpresa.recordatorioDiasGracia`. Requires `reports.financial.read`.
      responses:
        '200':
          description: Overdue invoices
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CobranzasVencidasEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/cobranzas/recordatorios:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [cobranzas]
      summary: Send overdue payment reminder
      description: |
        Persists `CobroRecordatorio`, dispatches `invoice_overdue` notification, and audits
        `cobranza_recordatorio_send`. At most one successful send per invoice per calendar day.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CobranzaRecordatorioInput'
      responses:
        '201':
          description: Reminder sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CobranzaRecordatorioEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Reminder already sent today
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invoice not overdue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/ticket:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [facturas]
      summary: Download 80mm ticket PDF (operational; non-fiscal without issued CAE)
      description: |
        Requires `reports.operational.read`. Thermal-style ticket for counter use.
        When `estadoCae` is not `issued` or CAE is missing, the PDF is explicitly **non-fiscal** (quotation/preview).
        Does not replace the legal fiscal PDF at `/api/facturas/{id}/pdf`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Ticket PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/print:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [facturas]
      summary: Print invoice using selected device (phase 1 mock)
      description: |
        Requires `reports.operational.read`. Phase 1 endpoint for device selection with mocked channels.
        - `pdf`: returns download path for legal PDF.
        - `fiscal`: if `FISCAL_PRINTER_ENABLED=false`, automatically falls back to PDF.
        - `thermal`: executes mock thermal channel (`mock-serial`) without real hardware dependency.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FacturaPrintInput'
      responses:
        '200':
          description: Printing workflow executed (or PDF fallback resolved)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacturaPrintEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/printing/status:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [printing]
      summary: Printing device status (phase 1 mock)
      description: |
        Requires `settings.business.manage`. Returns opt-in flags from server env (`FISCAL_PRINTER_ENABLED`, `THERMAL_PRINTER_ENABLED`; default false) and mock channel modes. PDF/legal billing does not require hardware.
      responses:
        '200':
          description: Printing status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrintingStatusEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/printing/test:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [printing]
      summary: Test mock printing device
      description: |
        Requires `settings.business.manage`. Runs phase-1 mock adapters with a fixed test payload (no invoice id).
        When `device` is `fiscal` and `FISCAL_PRINTER_ENABLED` is not `true`, or `device` is `thermal` and `THERMAL_PRINTER_ENABLED` is not `true`, returns HTTP 200 with `fallbackToPdf: true`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrintingTestInput'
      responses:
        '200':
          description: Mock test executed (or fiscal PDF fallback flag)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrintingTestEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [facturas]
      summary: Download legal fiscal invoice PDF (requires issued CAE)
      description: |
        Requires `reports.operational.read`. **Legal fiscal voucher** (RG 4291-aligned layout; manual AFIP validation pending per ADR-0014).
        Returns 422 when `estadoCae` is not `issued` or CAE is missing. Use `/pdf/preview` for non-fiscal preview.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: PDF file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: CAE not issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/pdf/preview:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [facturas]
      summary: Preview invoice PDF (watermarked, non-fiscal)
      description: Requires `reports.operational.read`. Non-fiscal preview without CAE; no valid AFIP QR/barcode.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: PDF preview
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/void:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    put:
      tags: [facturas]
      summary: Void an invoice
      description: |
        Sets `estado` to `N` (anulada), reverses the customer balance by the invoice total,
        creates a tenant-scoped `NotaCredito`, and records `AuditEvent` `factura_void` with motivo
        and `notaCreditoId` in metadata — all in one database transaction. Requires `sales.cancel`
        and module `billing.credit_notes`. When the source invoice has `estadoCae === issued`, the
        credit note CAE is requested asynchronously via AFIP adapter.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoidInput'
      responses:
        '200':
          description: Invoice voided with credit note and updated customer balance snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FacturaVoidEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Invoice already voided
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/facturas/{id}/mp:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [facturas, mercadopago]
      summary: Mercado Pago payment status for invoice (#175, #177)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`. Returns derived estado including `expired` when preference or QR TTL elapsed; includes active link or QR channel.
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Payment link status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoFacturaPaymentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/mp/preference:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [facturas, mercadopago]
      summary: Create Mercado Pago checkout preference for invoice (#175)
      description: |
        Creates a Mercado Pago Checkout preference for the invoice outstanding balance (ARS), stores `mpPreferenceId` and `mpPaymentLink` on `Factura`.
        Requires `reports.financial.read` and tenant integration `mercadopago`. Returns 409 when an active non-expired preference or QR already exists.
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '201':
          description: Preference created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoFacturaPaymentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice or Mercado Pago config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Active preference or QR already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invoice not eligible (voided, paid, or MP API error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/mp/qr:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [facturas, mercadopago]
      summary: Create Mercado Pago instore dynamic QR for invoice (#177)
      description: |
        Creates a Mercado Pago instore dynamic QR for the invoice outstanding balance (ARS), stores `mpQrData`, `mpQrOrderId` and `mpQrExpiresAt` on `Factura` (10 minute TTL).
        Requires `reports.financial.read` and tenant integration `mercadopago`. Returns 409 when an active non-expired preference or QR already exists.
        Payment confirmation uses the same webhook as #176 (`external_reference` = `{tenantId}:{facturaId}`).
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '201':
          description: Dynamic QR created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoFacturaPaymentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Invoice or Mercado Pago config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Active preference or QR already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invoice not eligible (voided, paid, or MP API error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/webhooks/mercadopago:
    post:
      tags: [mercadopago]
      summary: Mercado Pago payment webhook (#176)
      description: |
        Public IPN endpoint (no JWT). Validates `x-signature` with the tenant `webhookSecret` from Mercado Pago configuration (#174).
        Responds `200` immediately and processes the payment asynchronously: on `approved`, creates a customer receipt (`ReciboCobro`) with `mercadopago` payment method and invoice allocation; updates `Factura.mpEstado`.
        Chargeback notifications (`type: chargebacks`) enqueue `MercadoPagoChargeback` and notify managers; no automatic credit note (#179).
        Idempotent per `mpPaymentId` (`MercadoPagoProcessedPayment`) for payment events. Rate-limited per IP.
      security: []
      parameters:
        - name: data.id
          in: query
          required: false
          schema:
            type: string
          description: Payment id from Mercado Pago (also present in JSON body `data.id`).
        - name: type
          in: query
          required: false
          schema:
            type: string
        - name: x-signature
          in: header
          required: true
          schema:
            type: string
          description: Mercado Pago HMAC signature (`ts=...;v1=...`).
        - name: x-request-id
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MercadoPagoWebhookBody'
      responses:
        '200':
          description: Notification accepted (processing may continue asynchronously)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoWebhookAckEnvelope'
        '400':
          description: Invalid signature or payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/facturas/{id}/remito:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [facturas, remitos]
      summary: Create delivery note from invoice
      description: |
        Creates a draft `remito_x` copying active invoice lines. Requires module `fiscal.remito` and `sales.create`.
        Stock is not decremented on the remito (stock remains on invoice creation).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '201':
          description: Draft remito created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Invoice already linked to a non-voided remito
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/remitos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [remitos]
      summary: List delivery notes
      description: Paginated list. Requires module `fiscal.remito`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: estado
          in: query
          schema:
            type: string
            enum: [borrador, emitido, entregado, anulado]
        - name: clienteId
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Paginated remitos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [remitos]
      summary: Create delivery note (draft)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemitoInput'
      responses:
        '201':
          description: Draft created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/remitos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [remitos]
      summary: Get delivery note
      responses:
        '200':
          description: Delivery note detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
    put:
      tags: [remitos]
      summary: Update draft delivery note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemitoUpdateInput'
      responses:
        '200':
          description: Updated draft delivery note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'
        '409':
          description: Only borrador can be updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/remitos/{id}/emitir:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [remitos]
      summary: Issue delivery note (assign correlativo)
      responses:
        '200':
          description: Issued delivery note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'

  /api/remitos/{id}/entregar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [remitos]
      summary: Mark delivery note as delivered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemitoEntregarInput'
      responses:
        '200':
          description: Delivered delivery note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'

  /api/remitos/{id}/anular:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [remitos]
      summary: Void issued delivery note
      responses:
        '200':
          description: Voided delivery note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemitoEnvelope'

  /api/remitos/{id}/pdf:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [remitos]
      summary: Download delivery note PDF
      responses:
        '200':
          description: application/pdf
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /api/cheques/resumen:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cheques]
      summary: Check portfolio summary
      description: Aggregates for in-portfolio, due within 7 days and rejected checks. Requires module `fiscal.cheques`.
      responses:
        '200':
          description: Portfolio summary totals
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeResumenEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/cheques:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [cheques]
      summary: List checks
      description: Paginated portfolio list. Requires module `fiscal.cheques`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: tipo
          in: query
          schema:
            type: string
            enum: [recibido, emitido]
        - name: estado
          in: query
          schema:
            type: string
            enum: [en_cartera, emitido, depositado, endosado, descontado, cobrado, rechazado, anulado]
        - name: banco
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Paginated checks
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags: [cheques]
      summary: Register check in portfolio
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeInput'
      responses:
        '201':
          description: Check created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '409':
          description: Duplicate bank/number
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/cheques/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [cheques]
      summary: Get check
      responses:
        '200':
          description: Check detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
    put:
      tags: [cheques]
      summary: Update check (en_cartera only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeUpdateInput'
      responses:
        '200':
          description: Updated check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'
        '409':
          description: Only en_cartera checks can be updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'

  /api/cheques/{id}/depositar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Deposit check
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Deposited check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/{id}/endosar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Endorse check to supplier
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Endorsed check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/{id}/descontar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Discount check
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Discounted check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/{id}/cobrar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Mark check as collected
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Collected check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/{id}/rechazar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Reject check
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Rejected check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/{id}/devolver-cartera:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Return rejected check to portfolio
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Check returned to portfolio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/{id}/anular:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
    post:
      tags: [cheques]
      summary: Void check in portfolio
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChequeTransicionInput'
      responses:
        '200':
          description: Voided check
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChequeEnvelope'

  /api/cheques/alertas/run:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [cheques]
      summary: Run due-soon check alerts job
      responses:
        '200':
          description: Alert job result
          content:
            application/json:
              schema:
                type: object
                required: [success, data]
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      sent:
                        type: integer
                      skipped:
                        type: integer

  /api/notas-credito:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [notasCredito]
      summary: List credit notes
      description: |
        Paginated list filtered by `createdAt` in `[from, to]` (inclusive calendar days, server local timezone).
        Optional `clienteId` filters by the originating invoice's customer. Requires module `billing.credit_notes`
        and one of `reports.financial.read`, `reports.operational.read`.
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: clienteId
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated credit notes with originating invoice header
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotaCreditoListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/notas-credito/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [notasCredito]
      summary: Get credit note by id
      description: |
        Returns a credit note with originating invoice header for the current tenant. Requires module
        `billing.credit_notes` and one of `reports.financial.read`, `reports.operational.read`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Credit note row
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotaCreditoEnvelope'
        '400':
          description: Invalid path id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contabilidad/libro-iva-ventas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: Libro IVA Ventas export (ARCA RG 3685 — Fase 1)
      description: |
        Generates **Libro IVA Ventas** from persisted `Factura` fiscal fields (types A/B/C, netos, IVA, total, estado).
        Includes credit notes and voided vouchers (`tipo 999`) per ADR-0013 when `NotaCredito` falls in the period.
        Requires module `finance.ledger` and permission `reports.financial.read`.
        Purchases book: see `GET /api/contabilidad/libro-iva-compras` (#306).
        - `format=preview` (default): JSON totals and record counts.
        - `format=txt`: ZIP download with `CBTV.txt` + `ALICUOTAS.txt` (comma-separated, RG 3685 layout).
        - `format=xlsx`: internal review workbook (not an ARCA substitute).
        ARCA validator confirmation may be pending; structural consistency is covered by tests.
      parameters:
        - name: periodo
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
            example: '2026-05'
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [preview, txt, xlsx]
            default: preview
      responses:
        '200':
          description: Preview JSON or file download
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibroIvaVentasPreviewEnvelope'
            application/zip:
              schema:
                type: string
                format: binary
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/contabilidad/libro-iva-compras:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: Libro IVA Compras export (ARCA RG 3685 — #306)
      description: |
        Generates **Libro IVA Compras** from persisted `ComprobanteCompra` fiscal fields (types A/B/C, netos, IVA, total, estado).
        Requires module `finance.ledger` and permission `reports.financial.read`.
        Data entry: `POST /api/comprobantes-compra` (supplier fiscal voucher).
        - `format=preview` (default): JSON totals and record counts.
        - `format=txt`: ZIP download with `CBTU.txt` + `ALICUOTAS.txt` (comma-separated, RG 3685 layout).
        - `format=xlsx`: internal review workbook (not an ARCA substitute).
      parameters:
        - name: periodo
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
            example: '2026-05'
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [preview, txt, xlsx]
            default: preview
      responses:
        '200':
          description: Preview JSON or file download
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibroIvaComprasPreviewEnvelope'
            application/zip:
              schema:
                type: string
                format: binary
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comprobantes-compra:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [contabilidad]
      summary: Register supplier fiscal purchase voucher (#306)
      description: |
        Persists a supplier fiscal voucher with netos/IVA breakdown for Libro IVA Compras.
        Requires module `finance.ledger` and permission `reports.financial.read`.
        Unique per tenant on `tipo` + `prefijo` + `numero`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComprobanteCompraInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComprobanteCompraEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          $ref: '#/components/responses/ConflictError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/procesar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [contabilidad]
      summary: Upload purchase document for extraction preview (#277)
      description: |
        Stores the original PDF/image on local filesystem and returns an editable preview.
        **Tier 1:** decodes AFIP/ARCA FE QR locally from embedded PDF URL text or image scan (`jsQR` + `sharp`) — no portal/API verification.
        **Tier 2:** extracts text from digital PDF (`unpdf`) and applies bundled YAML regex templates (Argentina `generic-arca-ar`) when confidence ≥ 0.7.
        **Tier 3:** preprocesses images (`sharp`) and runs local OCR (`tesseract.js`, `spa+eng`), then applies the same templates when confidence ≥ 0.6.
        **Tier 4:** optional local Ollama (`OLLAMA_URL`, model `OLLAMA_MODEL` default `nuextract`) when tiers 1–3 fail.
        Falls back to empty manual preview (tier 0) when extraction fails.
        Requires modules `finance.ledger` and `logistics.purchases`, permission `reports.financial.read`.
        Multipart field `file` — pdf, jpg, png, webp, heic (max 10 MB).
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Document stored; preview pending review
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraImportadoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/procesar-lote:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [contabilidad]
      summary: Batch upload purchase documents for extraction (#277 Phase E)
      description: |
        Processes up to 20 PDF/image files sequentially (same tier pipeline as `procesar`).
        Multipart field `files` — pdf, jpg, png, webp, heic (max 10 MB each).
        Requires `finance.ledger`, `logistics.purchases`, `reports.financial.read`.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [files]
              properties:
                files:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: binary
      responses:
        '201':
          description: All documents stored; previews pending review
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraImportadoListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/verificar-duplicado:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: proveedorId
        in: query
        required: true
        schema:
          type: integer
          minimum: 1
      - name: tipo
        in: query
        required: true
        schema:
          type: string
          minLength: 1
      - name: prefijo
        in: query
        required: true
        schema:
          type: string
          minLength: 1
      - name: numero
        in: query
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [contabilidad]
      summary: Check duplicate purchase voucher for supplier (#277 Fase G)
      description: |
        Returns whether an active `ComprobanteCompra` already exists for the same supplier, tipo, prefijo and numero.
        Requires `finance.ledger`, `logistics.purchases`, `reports.financial.read`.
      responses:
        '200':
          description: Duplicate check result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraDuplicadoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/cola:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: Purchase document import queue status (#277 Phase E)
      description: |
        Returns counts by estado and the 20 most recent import rows for the current user.
        Requires `finance.ledger`, `logistics.purchases`, `reports.financial.read`.
      responses:
        '200':
          description: Queue snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraColaEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/templates:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [contabilidad]
      summary: List purchase document extraction templates (#277 Phase E)
      description: |
        Bundled Argentina templates plus tenant custom YAML under `data/documentos-compra-templates/{tenantId}/`.
        Requires `finance.ledger`, `logistics.purchases`, `reports.financial.read`.
      responses:
        '200':
          description: Template catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraTemplateListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [contabilidad]
      summary: Save tenant custom extraction template (#277 Phase E)
      description: |
        Persists invoice2data-style YAML for the current tenant. Requires `settings.fiscal.manage`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentoCompraTemplateInput'
      responses:
        '201':
          description: Template saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraTemplateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/confirmar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [contabilidad]
      summary: Confirm imported document and create ComprobanteCompra (#277 Phase A)
      description: |
        Persists `ComprobanteCompra` from reviewed preview and links the import record.
        Duplicate `proveedorId` + `tipo` + `prefijo` + `numero` returns 409.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentoCompraConfirmInput'
      responses:
        '201':
          description: Confirmed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentoCompraConfirmEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          $ref: '#/components/responses/ConflictError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/documentos-compra/{id}/original:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [contabilidad]
      summary: Download original imported purchase document (#277 Phase A)
      description: Returns the stored PDF/image bytes. Requires `finance.ledger`, `logistics.purchases`, `reports.financial.read`.
      responses:
        '200':
          description: Original file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/formas-pago:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [formasPago]
      summary: List payment methods
      responses:
        '200':
          description: List of payment methods
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FormaPagoListEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/formas-pago/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    patch:
      tags: [formasPago]
      summary: Update payment method flags (esEfectivo)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FormaPagoPatchInput'
      responses:
        '200':
          description: Updated payment method
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FormaPagoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/users:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [users]
      summary: List users for the current tenant
      description: Returns all users belonging to the authenticated user's tenant. Requires `users.manage` permission.
      security:
        - cookieAuth: []
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppUserListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [users]
      summary: Create a new user
      description: Creates a new user in the current tenant. Requires `users.manage` and `roles.assign` permissions.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppUserInput'
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppUserEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '409':
          $ref: '#/components/responses/ConflictError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/users/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    put:
      tags: [users]
      summary: Update a user
      description: Updates role, active flag, or scope for a user in the current tenant. Requires `users.manage`.
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/BizcodeChannelHeader'
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppUserUpdateInput'
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppUserEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/auth/change-password:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [auth]
      summary: Change own password
      description: Allows the authenticated user to change their password by supplying the current password.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordInput'
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    const: true
                  data:
                    type: object
                    properties:
                      changed:
                        type: boolean
                        const: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/reportes/aging:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [reportes]
      summary: Accounts receivable aging report
      description: |
        Groups active invoices (`estado = A`) into aging buckets using each customer's `creditDays`.
        Due date = invoice `fecha` + `creditDays`; days past due drive the bucket assignment.
        Each invoice contributes its full `total` to exactly one bucket (no payment allocation per invoice).
        Requires permission `reports.financial.read`.
      security:
        - cookieSession: []
      responses:
        '200':
          description: AR aging buckets and executive summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgingArEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/reportes/cuenta-corriente/{clienteId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [reportes]
      summary: Customer account statement (running balance)
      description: |
        Legacy wrapper over the persisted customer ledger (`MovimientoClienteCC`): chronological movements
        with debit/credit columns and running `saldo`. Requires permission `reports.financial.read`.
      security:
        - cookieSession: []
      parameters:
        - name: clienteId
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Account statement lines
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CuentaCorrienteEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Customer not found for tenant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/reportes/ventas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [reportes]
      summary: Sales by period
      description: |
        Aggregates active invoices (`estado = A`) in the date range using server local calendar.
        Grouping `agrupar`: `dia` (YYYY-MM-DD), `semana` (Monday of week as YYYY-MM-DD), `mes` (YYYY-MM).
        Requires permission `reports.operational.read`.
        Send `Accept: text/csv` for CSV export (raw CSV body, not JSON envelope).
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: agrupar
          in: query
          schema:
            type: string
            enum: [dia, semana, mes]
            default: dia
      responses:
        '200':
          description: Sales rows or CSV export
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReporteVentasListEnvelope'
            text/csv:
              schema:
                type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/reportes/stock-critico:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [reportes]
      summary: Critical stock (stock at or below minimum)
      description: |
        Active physical articles (`tipo=articulo`) with `stock <= minimo`. Services are excluded (#244).
        `deficit = minimo - stock`.
        Requires permission `reports.operational.read`.
        Send `Accept: text/csv` for CSV export.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Critical stock rows or CSV export
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockCriticoListEnvelope'
            text/csv:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/reportes/ventas-por-tipo:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [reportes]
      summary: Sales totals by item tipo (product vs service)
      description: |
        Sums invoice line subtotals for active invoices in the date range, split into
        `productos` (catalog tipo=articulo) and `servicios` (catalog tipo=servicio or ad-hoc lines) (#244).
        Requires permission `reports.operational.read`.
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Product vs service sales totals
          content:
            application/json:
              schema:
                type: object
                required: [success, data]
                properties:
                  success:
                    type: boolean
                    const: true
                  data:
                    type: object
                    required: [productos, servicios]
                    properties:
                      productos:
                        type: string
                      servicios:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/reportes/cobranzas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [reportes]
      summary: Collections by period
      description: |
        Customer payments grouped by calendar day with breakdown by payment method.
        Requires permission `reports.financial.read`.
        Send `Accept: text/csv` for CSV export.
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Collections rows or CSV export
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReporteCobranzasListEnvelope'
            text/csv:
              schema:
                type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/dashboard/summary:
    get:
      tags: [dashboard]
      summary: Dashboard operational summary
      security:
        - cookieSession: []
      responses:
        '200':
          description: Aggregated dashboard metrics for the authenticated tenant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardSummaryEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/dashboard/ventas-historico:
    get:
      tags: [dashboard]
      summary: Historical sales analytics for charts and CSV export (#138)
      description: |
        Aggregates active invoices (`estado = A`) in PostgreSQL by period, top 10 articles, and sales by seller (via linked `Pedido`).
        Requires permission `reports.operational.read`. JSON by default; send `Accept: text/csv` for period series export.
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            pattern: '^\\d{4}-\\d{2}-\\d{2}$'
        - name: to
          in: query
          required: true
          schema:
            type: string
            pattern: '^\\d{4}-\\d{2}-\\d{2}$'
        - name: groupBy
          in: query
          schema:
            type: string
            enum: [day, week, month]
            default: day
        - name: vendedorId
          in: query
          schema:
            type: integer
            minimum: 1
        - name: deliveryZoneId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Aggregated analytics or CSV series
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardVentasHistoricoEnvelope'
            text/csv:
              schema:
                type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/audit-events:
    get:
      tags: [audit]
      summary: List audit events for the authenticated tenant
      description: |
        Read-only list scoped to the session tenant. Requires permission `audit.read`.
        Default page size is **50** rows (maximum **500**). Optional filters restrict by actor, action, resource type, and `createdAt` bounds (`startDate` / `endDate` as ISO-8601 timestamps; inclusive range).
      security:
        - cookieSession: []
      parameters:
        - name: userId
          in: query
          description: Filter by author user id (`AuditEvent.userId`)
          schema:
            type: integer
            minimum: 1
        - name: action
          in: query
          description: Exact match on `AuditEvent.action`
          schema:
            type: string
        - name: resource
          in: query
          description: Exact match on `AuditEvent.resource`
          schema:
            type: string
        - name: startDate
          in: query
          description: Lower bound on `createdAt` (inclusive)
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          description: Upper bound on `createdAt` (inclusive)
          schema:
            type: string
            format: date-time
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated audit events, newest first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEventListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/notifications:
    get:
      tags: [notifications]
      summary: List unread notifications for the authenticated user
      security:
        - cookieSession: []
      responses:
        '200':
          description: Unread notifications, newest first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/notifications/read-all:
    put:
      tags: [notifications]
      summary: Mark all unread notifications as read
      security:
        - cookieSession: []
      responses:
        '200':
          description: Updated notification count
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationReadAllEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/notifications/{id}/read:
    put:
      tags: [notifications]
      summary: Mark a single notification as read
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Updated notification
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          description: Notification not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/notifications/channels:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [notifications]
      summary: Report which external notification channels are configured
      description: Returns boolean flags for each channel. No sensitive values are exposed.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Channel status
          content:
            application/json:
              schema:
                type: object
                required: [success, data]
                properties:
                  success:
                    type: boolean
                    const: true
                  data:
                    type: object
                    required: [inApp, email, whatsapp]
                    properties:
                      inApp:
                        type: boolean
                      email:
                        type: boolean
                      whatsapp:
                        type: boolean
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /api/chat/conversations:
    get:
      tags: [chat]
      summary: List chat conversations for authenticated user
      security:
        - cookieSession: []
      responses:
        '200':
          description: Conversations with unread counts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatConversationListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/chat/messages:
    post:
      tags: [chat]
      summary: Send a chat message to another user
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatMessageCreateInput'
      responses:
        '201':
          description: Message created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatMessageEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          description: Recipient not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/chat/messages/{userId}:
    get:
      tags: [chat]
      summary: Get paginated message history with a user
      security:
        - cookieSession: []
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: integer }
        - in: query
          name: limit
          required: false
          schema: { type: integer, minimum: 1, maximum: 100 }
        - in: query
          name: before
          required: false
          schema: { type: integer, minimum: 1 }
      responses:
        '200':
          description: Conversation history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatMessageListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/empresa:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [settings]
      summary: Get company settings for the authenticated tenant
      security:
        - cookieSession: []
      responses:
        '200':
          description: Company settings (persisted row or tenant defaults)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmpresaEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [settings]
      summary: Update company settings
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmpresaInput'
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmpresaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal-config:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [portal]
      summary: Get B2B portal settings for the authenticated tenant (#240)
      security:
        - cookieSession: []
      responses:
        '200':
          description: Portal configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalConfigEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [portal]
      summary: Update B2B portal settings (#240)
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortalConfigInput'
      responses:
        '200':
          description: Portal configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalConfigEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/configuracion/mercadopago:
    get:
      tags: [mercadopago]
      summary: Mercado Pago config status (metadata only) (#174)
      description: Requires `settings.business.manage` and tenant integration `mercadopago`. Does not return access token or webhook secret.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Mercado Pago config status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoConfigStatusEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    put:
      tags: [mercadopago]
      summary: Upsert tenant Mercado Pago credentials (#174)
      description: Requires `settings.business.manage` and tenant integration `mercadopago`. Access token and webhook secret encrypted at rest.
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MercadoPagoConfigInput'
      responses:
        '200':
          description: Config saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoConfigEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/configuracion/mercadopago/test:
    post:
      tags: [mercadopago]
      summary: Verify Mercado Pago credentials (#174)
      description: Requires `settings.business.manage` and tenant integration `mercadopago`. Calls Mercado Pago `users/me` with stored access token.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Credentials valid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoTestEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Mercado Pago not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/configuracion/mercadopago/qr-estatico:
    get:
      tags: [mercadopago]
      summary: Tenant static Mercado Pago QR (#177)
      description: Requires `settings.business.manage` and tenant integration `mercadopago`. Returns configured static QR payload and PNG base64 when `staticQrData` is set on `MercadoPagoConfig`.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Static QR data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoStaticQrEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Static QR not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/mercadopago/pagos-sin-reconciliar:
    get:
      tags: [mercadopago]
      summary: List unreconciled Mercado Pago payments (#178)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`. Returns pending reconciliation queue entries.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Pending payments
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoReconciliationListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/mercadopago/reconciliar:
    post:
      tags: [mercadopago]
      summary: Manually reconcile Mercado Pago payment (#178)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`. Payment amount must match invoice open balance exactly.
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MercadoPagoReconcileBody'
      responses:
        '200':
          description: Payment reconciled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoReconciliationEntryEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          $ref: '#/components/responses/ConflictError'
        '422':
          description: Amount mismatch or validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/mercadopago/ignorar:
    post:
      tags: [mercadopago]
      summary: Ignore unreconciled Mercado Pago payment (#178)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`. Marks a pending payment as not reconciliable.
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MercadoPagoIgnoreBody'
      responses:
        '200':
          description: Payment ignored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoIgnoreEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/mercadopago/reconciliacion/run:
    post:
      tags: [mercadopago]
      summary: Run Mercado Pago reconciliation job (#178)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`. Forces reconciliation scan for the current tenant (ignores daily time slot).
      security:
        - cookieSession: []
      responses:
        '200':
          description: Job summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoReconciliationJobEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/facturas/{id}/mp/reembolso:
    get:
      tags: [facturas, mercadopago]
      summary: Get Mercado Pago refund status for invoice (#179, #344)
      description: Requires `sales.cancel`, tenant integration `mercadopago`, and module `billing.credit_notes`. Returns refundable balance and refund history.
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Refund timeline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoRefundStatusEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [facturas, mercadopago]
      summary: Request Mercado Pago refund (#179, #344)
      description: |
        Requires `sales.cancel`, tenant integration `mercadopago`, and module `billing.credit_notes`.
        Supports **full** and **partial** refunds. Omit `monto` to refund the remaining refundable balance.
        Partial refunds create a partial credit note (#344) without voiding the invoice.
        Full refunds void receipt, invoice, and credit note (#146), setting `mpEstado: refunded`.
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MercadoPagoRefundInput'
      responses:
        '201':
          description: Refund completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoRefundEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          $ref: '#/components/responses/ConflictError'
        '422':
          description: Partial refund not supported or invoice not eligible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '502':
          description: Mercado Pago refund API error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/mercadopago/contracargos:
    get:
      tags: [mercadopago]
      summary: List pending Mercado Pago chargebacks (#179)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`.
      security:
        - cookieSession: []
      responses:
        '200':
          description: Pending chargeback queue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoChargebackListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/mercadopago/contracargos/{id}:
    patch:
      tags: [mercadopago]
      summary: Update Mercado Pago chargeback status (#179)
      description: Requires `reports.financial.read` and tenant integration `mercadopago`. Mark `resuelto` or `ignorado`.
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MercadoPagoChargebackPatchInput'
      responses:
        '200':
          description: Updated chargeback
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MercadoPagoChargebackEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          $ref: '#/components/responses/ConflictError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/branding:
    get:
      tags: [portal]
      summary: Public portal branding for login shell (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
      responses:
        '200':
          description: Branding payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalBrandingEnvelope'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/auth/magic-link:
    post:
      tags: [portal]
      summary: Request magic-link email for portal login (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortalMagicLinkInput'
      responses:
        '200':
          description: Generic success (anti-enumeration)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalMagicLinkSentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/auth/verify:
    get:
      tags: [portal]
      summary: Verify magic link and issue portal session cookie (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
        - name: token
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Session established
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalVerifyEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/auth/logout:
    post:
      tags: [portal]
      summary: Revoke portal session (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalLogoutEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/me:
    get:
      tags: [portal]
      summary: Current portal customer profile (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
      responses:
        '200':
          description: Customer profile and branding
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalMeEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/facturas:
    get:
      tags: [portal]
      summary: List customer invoices in portal scope (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
        - name: estado
          in: query
          schema:
            type: string
            enum: [pagada, pendiente, vencida]
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Invoice list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalFacturaListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/facturas/{id}/pdf:
    get:
      tags: [portal]
      summary: Download invoice PDF (portal scope) (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Invoice PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '422':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/cuenta-corriente:
    get:
      tags: [portal]
      summary: Customer ledger in portal scope (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Ledger statement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClienteCuentaCorrienteEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/cuenta-corriente/estado-de-cuenta/pdf:
    get:
      tags: [portal]
      summary: Download account statement PDF (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
      responses:
        '200':
          description: Account statement PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/portal/{tenantSlug}/pedidos:
    get:
      tags: [portal]
      summary: List customer orders in portal scope (#240)
      parameters:
        - $ref: '#/components/parameters/PortalTenantSlug'
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Order list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortalPedidoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/zonas-entrega:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [logistics]
      summary: List delivery zones for the authenticated tenant
      security:
        - cookieSession: []
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: List of delivery zones
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [logistics]
      summary: Create a delivery zone
      security:
        - cookieSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeliveryZoneInput'
      responses:
        '201':
          description: Zone created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/zonas-entrega/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    put:
      tags: [logistics]
      summary: Update a delivery zone
      security:
        - cookieSession: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeliveryZoneUpdateInput'
      responses:
        '200':
          description: Zone updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryZoneEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Zone not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-entrega:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [logistics]
      summary: List delivery orders
      description: |
        Requires `logistics.read`, `orders.deliver.confirm`, or `orders.pick`. Drivers only see their own orders
        (`driverId` forced to session user).
      parameters:
        - name: estado
          in: query
          schema:
            type: string
            enum: [pending, picking, ready, assigned, in_transit, delivered, failed, cancelled]
        - name: zonaId
          in: query
          schema:
            type: integer
            minimum: 1
        - name: driverId
          in: query
          schema:
            type: integer
            minimum: 1
        - name: fecha
          in: query
          schema:
            type: string
            format: date
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated delivery orders
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenEntregaListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [logistics]
      summary: Create a delivery order
      description: Requires `orders.create`. Initial estado is `pending`, or `assigned` when `driverId` is set.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenEntregaCreateInput'
      responses:
        '201':
          description: Order created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenEntregaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-entrega/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    put:
      tags: [logistics]
      summary: Update delivery order status
      description: |
        Requires `orders.dispatch` for transitions to `assigned`, `in_transit`, or `failed`, and
        `orders.deliver.confirm` for `delivered`. Drivers may only confirm their own `in_transit` orders.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrdenEntregaUpdateInput'
      responses:
        '200':
          description: Order updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenEntregaEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Order not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-entrega/{id}/iniciar-picking:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [logistics]
      summary: Start warehouse picking for a delivery order
      description: |
        Requires `orders.pick` and tenant module `logistics.picking`. Transitions `pending` → `picking`
        and assigns the session user as picker. Returns 409 when another user already holds the order in picking.
      responses:
        '200':
          description: Picking started (or idempotent for same picker)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenEntregaEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Order not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Order is being picked by another user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/ordenes-entrega/{id}/lista:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [logistics]
      summary: Mark delivery order ready after picking
      description: |
        Requires `orders.pick` and tenant module `logistics.picking`. Transitions `picking` → `ready`.
        Only the assigned picker may complete, except `warehouse_lead` who may override.
      responses:
        '200':
          description: Order marked ready for dispatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrdenEntregaEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Order not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '409':
          description: Order is assigned to another picker
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [repartos]
      summary: List delivery routes
      description: Requires `logistics.read`. Filter by `fecha`, `choferId`, `estado`.
      parameters:
        - name: fecha
          in: query
          schema:
            type: string
            format: date
        - name: choferId
          in: query
          schema:
            type: integer
            minimum: 1
        - name: estado
          in: query
          schema:
            type: string
            enum: [planned, on_route, completed, cancelled]
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
      responses:
        '200':
          description: Paginated delivery routes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoListEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [repartos]
      summary: Create delivery route
      description: |
        Requires `orders.dispatch`. Groups delivery orders in estado `ready`; assigns driver and sets OEs to `assigned`.
        Rejects when an OE is already on an active route (`planned` or `on_route`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RepartoCreateInput'
      responses:
        '201':
          description: Route created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '422':
          description: Invalid line item or OE already on active route
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/activos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [repartos]
      summary: List on-route deliveries with last GPS position
      description: |
        Requires tenant module `logistics.gps`, permission `logistics.read`, and role
        `owner`, `manager`, or `logistics_planner`. Returns repartos in estado `on_route`
        with `ultimaUbicacion`, `progress`, and `currentStop` (first pending item).
      responses:
        '200':
          description: Active routes for live tracking
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoActivoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [repartos]
      summary: Get delivery route by id
      description: Requires `logistics.read`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Route detail with ordered items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Route not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}/iniciar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [repartos]
      summary: Start delivery route
      description: |
        Requires `orders.dispatch`. `planned` → `on_route`; pending items' OEs → `in_transit`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Route started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Route not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}/ubicacion:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [repartos]
      summary: Record driver GPS position
      description: |
        Requires tenant module `logistics.gps` and permission `orders.deliver.confirm`.
        Only the assigned driver on a reparto in estado `on_route`. Purges locations older than 7 days.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RepartoUbicacionInput'
      responses:
        '200':
          description: Location recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoUbicacionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Route not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}/ubicacion/ultima:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [repartos]
      summary: Get last GPS position for a route
      description: |
        Requires tenant module `logistics.gps` and permission `logistics.read`.
        Planners (`owner`, `manager`, `logistics_planner`) on any route; drivers only on their own route.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Last recorded position or null
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoUbicacionNullableEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Route not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}/cerrar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [repartos]
      summary: Close delivery route
      description: |
        Requires `orders.dispatch`. `on_route` → `completed`; pending items → `not_delivered` and OEs → `failed`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Route closed with summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoCloseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Route not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}/items/{itemId}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    put:
      tags: [repartos]
      summary: Record delivery proof (POD) for a route item
      description: |
        Requires `orders.deliver.confirm`. Route must be `on_route`; item `pending`.
        Driver may only update items on their own route. Delivered outcome requires `receptorNombre` and non-empty `firmaBase64` within size limits.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RepartoItemPodInput'
      responses:
        '200':
          description: Item updated (public fields only; no pod blobs)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoItemLineEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Route or item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '422':
          description: Invalid state, signature, or motivo
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/repartos/{id}/items/{itemId}/pod:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [repartos]
      summary: Get full delivery proof for a route item
      description: |
        Requires `logistics.read` and role `owner`, `manager`, or `logistics_planner` (not `driver`).
        Returns signature/photo blobs in `podMedia`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
        - name: itemId
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Full POD detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RepartoItemPodEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: Item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/logistica/kpis:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [logistica]
      summary: Logistics KPIs for a date range
      description: |
        Aggregated logistics metrics using DB-side queries (#145).
        Requires tenant module `logistics.dispatches` and permission `logistics.read`.
        Optional `choferId` filters all metrics to one driver.
        Uses `OrdenEntrega.dispatchedAt` (ADR-0011) as dispatch baseline.
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: choferId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: KPI aggregate object
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogisticaKpisEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/logistica/reporte-choferes:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [logistica]
      summary: Driver productivity report
      description: |
        Rows grouped by driver and dispatch day. Requires `logistics.dispatches` and `logistics.read`.
        Send `Accept: text/csv` for CSV export.
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: choferId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Driver rows or CSV
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogisticaChoferesListEnvelope'
            text/csv:
              schema:
                type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/logistica/reporte-zonas:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [logistica]
      summary: Delivery coverage by zone
      description: |
        Rows grouped by delivery zone. Requires `logistics.dispatches` and `logistics.read`.
        Send `Accept: text/csv` for CSV export.
      security:
        - cookieSession: []
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: choferId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Zone rows or CSV
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogisticaZonasListEnvelope'
            text/csv:
              schema:
                type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/depositos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [depositos]
      summary: List deposits
      description: Requires module `inventory.warehouses`.
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: activo
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Paginated deposits
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DepositoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [depositos]
      summary: Create deposit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DepositoCreateInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DepositoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '409':
          description: Duplicate codigo
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/depositos/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [depositos]
      summary: Get deposit by id
      responses:
        '200':
          description: Deposit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DepositoEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: [depositos]
      summary: Update deposit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DepositoPatchInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DepositoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags: [depositos]
      summary: Delete deposit
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '422':
          description: Cannot delete (default or has stock)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/articulos/{id}/stock-depositos:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [depositos]
      summary: Stock breakdown by deposit for an article
      description: Requires module `inventory.warehouses`.
      responses:
        '200':
          description: Stock by deposit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArticuloStockPorDepositoResponse'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/transferencias-deposito:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [depositos]
      summary: List inter-warehouse transfers
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: estado
          in: query
          schema:
            type: string
            enum: [pendiente, en_transito, recibida, anulada]
      responses:
        '200':
          description: Paginated transfers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferenciaDepositoListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [depositos]
      summary: Create transfer (pendiente)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferenciaDepositoCreateInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferenciaDepositoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/transferencias-deposito/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [depositos]
      summary: Get transfer by id
      responses:
        '200':
          description: Transfer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferenciaDepositoEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/transferencias-deposito/{id}/en-transito:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [depositos]
      summary: Mark transfer in transit (debit origin)
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferenciaDepositoEnvelope'
        '422':
          description: Invalid state or insufficient stock
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/transferencias-deposito/{id}/recibir:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [depositos]
      summary: Receive transfer (credit destination)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferenciaDepositoRecibirInput'
      responses:
        '200':
          description: Received
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferenciaDepositoEnvelope'
        '422':
          description: Invalid state or quantities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/settings:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [comisiones]
      summary: Get commission accrual default mode
      description: Requires module `finance.commissions`.
      responses:
        '200':
          description: Settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComisionesSettingsEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    patch:
      tags: [comisiones]
      summary: Update commission accrual default mode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComisionesSettingsInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComisionesSettingsEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/configs:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [comisiones]
      summary: List commission configs
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: vendedorId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Paginated configs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigComisionListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    post:
      tags: [comisiones]
      summary: Create commission config
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigComisionCreateInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigComisionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/configs/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    patch:
      tags: [comisiones]
      summary: Update commission config
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigComisionPatchInput'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigComisionEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'
    delete:
      tags: [comisiones]
      summary: Delete commission config
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/liquidaciones:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [comisiones]
      summary: List commission settlements
      parameters:
        - $ref: '#/components/parameters/ListLimit'
        - $ref: '#/components/parameters/ListOffset'
        - name: periodo
          in: query
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
        - name: estado
          in: query
          schema:
            type: string
            enum: [borrador, aprobada, pagada]
        - name: vendedorId
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Paginated settlements
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiquidacionComisionListEnvelope'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/liquidaciones/generar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [comisiones]
      summary: Generate draft settlements for a period
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LiquidacionGenerarInput'
      responses:
        '201':
          description: Generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiquidacionGenerarEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/liquidaciones/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [comisiones]
      summary: Get settlement with detail lines
      responses:
        '200':
          description: Settlement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiquidacionComisionEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/liquidaciones/{id}/aprobar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [comisiones]
      summary: Approve draft settlement
      responses:
        '200':
          description: Approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiquidacionComisionEnvelope'
        '422':
          description: Not draft
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/liquidaciones/{id}/pagar:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [comisiones]
      summary: Mark approved settlement as paid
      responses:
        '200':
          description: Paid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiquidacionComisionEnvelope'
        '422':
          description: Not approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/liquidaciones/{id}/export.csv:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [comisiones]
      summary: Export settlement CSV
      responses:
        '200':
          description: CSV file
          content:
            text/csv:
              schema:
                type: string
        '422':
          description: Still draft
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/ranking:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [comisiones]
      summary: Ranking of sellers by commission for a period
      parameters:
        - name: periodo
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
      responses:
        '200':
          description: Ranking rows
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComisionRankingEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/comisiones/mias:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    get:
      tags: [comisiones]
      summary: Current seller estimate and settlement history
      description: Restricted to the authenticated user (`commissions.self.read`).
      parameters:
        - name: periodo
          in: query
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
      responses:
        '200':
          description: Self view
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MisComisionesResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/importaciones/templates/{entity}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: entity
        in: path
        required: true
        schema:
          type: string
          enum: [articulos, clientes, proveedores, saldos]
      - name: format
        in: query
        schema:
          type: string
          enum: [csv, xlsx]
          default: csv
    get:
      tags: [importaciones]
      summary: Download import template CSV or XLSX (#238)
      responses:
        '200':
          description: Template file
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/importaciones/validate:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [importaciones]
      summary: Dry-run validate import file without persisting (#238)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, entity]
              properties:
                file:
                  type: string
                  format: binary
                entity:
                  type: string
                  enum: [articulos, clientes, proveedores, saldos]
                duplicateMode:
                  type: string
                  enum: [update, skip]
      responses:
        '200':
          description: Validation summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkImportValidateEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/importaciones/jobs:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
    post:
      tags: [importaciones]
      summary: Start async import job (#238)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, entity]
              properties:
                file:
                  type: string
                  format: binary
                entity:
                  type: string
                  enum: [articulos, clientes, proveedores, saldos]
                modo:
                  type: string
                  enum: [mejores_esfuerzos, todo_o_nada]
                duplicateMode:
                  type: string
                  enum: [update, skip]
      responses:
        '201':
          description: Job created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportJobEnvelope'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/importaciones/jobs/{id}:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [importaciones]
      summary: Get import job status (#238)
      responses:
        '200':
          description: Job
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportJobEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/importaciones/jobs/{id}/events:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [importaciones]
      summary: SSE progress stream for import job (#238)
      responses:
        '200':
          description: text/event-stream
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/importaciones/jobs/{id}/report:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    get:
      tags: [importaciones]
      summary: Download import job report CSV (#238)
      responses:
        '200':
          description: Report CSV
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'

  /api/transferencias-deposito/{id}/anular:
    parameters:
      - $ref: '#/components/parameters/BizcodeChannelHeader'
      - name: id
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
    post:
      tags: [depositos]
      summary: Cancel transfer
      responses:
        '200':
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransferenciaDepositoEnvelope'
        '422':
          description: Not cancellable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorEnvelope'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '500':
          $ref: '#/components/responses/ServerError'

components:
  securitySchemes:
    cookieSession:
      type: apiKey
      in: cookie
      name: bizcode_session

  parameters:
    BizcodeChannelHeader:
      name: x-bizcode-channel
      in: header
      required: false
      description: Optional channel scope selector. When provided, it must be allowed by the authenticated user's `claims.scope.channels`.
      schema:
        type: string
        enum: [counter, field, backoffice, warehouse, delivery]

    XRequestId:
      name: X-Request-Id
      in: header
      required: false
      description: Optional client-supplied correlation id (trimmed, max 128 chars). Echoed unchanged when valid; otherwise the server substitutes a UUID.
      schema:
        type: string
        maxLength: 128

    TenantIdPath:
      name: tenantId
      in: path
      required: true
      schema:
        type: integer
        minimum: 1

    ListLimit:
      name: limit
      in: query
      required: false
      description: Page size for list endpoints (default 100, max 500)
      schema:
        type: integer
        minimum: 1
        maximum: 500
        default: 100

    ListOffset:
      name: offset
      in: query
      required: false
      description: Number of rows to skip before returning results
      schema:
        type: integer
        minimum: 0
        default: 0

    PortalTenantSlug:
      name: tenantSlug
      in: path
      required: true
      description: Tenant slug for portal routes (MVP `/portal/:tenantSlug`)
      schema:
        type: string
        maxLength: 80

  schemas:
    HealthResponse:
      type: object
      required: [status, timestamp]
      properties:
        status:
          type: string
          example: ok
        timestamp:
          type: string
          format: date-time
        db:
          type: object
          required: [ok, latencyMs]
          properties:
            ok:
              type: boolean
            latencyMs:
              type: number
              minimum: 0
        uptimeSeconds:
          type: number
          minimum: 0
        version:
          type: string

    MetricsEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/MetricsSnapshot'

    MetricsSnapshot:
      type: object
      required:
        - startedAt
        - uptimeSeconds
        - appEnv
        - totals
        - requestsByMethod
        - responsesByStatus
        - requestsByRoute
      properties:
        startedAt:
          type: string
          format: date-time
        uptimeSeconds:
          type: integer
          minimum: 0
        appEnv:
          type: string
        appVersion:
          type: string
          nullable: true
        totals:
          type: object
          required: [requests, errors4xx, errors5xx, averageDurationMs]
          properties:
            requests:
              type: integer
              minimum: 0
            errors4xx:
              type: integer
              minimum: 0
            errors5xx:
              type: integer
              minimum: 0
            averageDurationMs:
              type: number
              minimum: 0
        requestsByMethod:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        responsesByStatus:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        requestsByRoute:
          type: object
          additionalProperties:
            type: integer
            minimum: 0

    ApiErrorEnvelope:
      type: object
      required: [success, error]
      properties:
        success:
          type: boolean
          const: false
        error:
          type: string

    SetupOwnerInput:
      type: object
      required: [tenantName, tenantSlug, username, password]
      properties:
        tenantName:
          type: string
          minLength: 2
        tenantSlug:
          type: string
          minLength: 2
        username:
          type: string
          minLength: 3
        password:
          type: string
          minLength: 8

    SetupOwnerResult:
      type: object
      required: [tenantId, userId, role]
      properties:
        tenantId:
          type: integer
        userId:
          type: integer
        role:
          type: string

    SetupOwnerEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/SetupOwnerResult'

    LoginInput:
      type: object
      required: [tenantSlug, username, password]
      properties:
        tenantSlug:
          type: string
        username:
          type: string
        password:
          type: string

    LoginResult:
      type: object
      required: [userId, tenantId, username, role]
      properties:
        userId:
          type: integer
        tenantId:
          type: integer
        username:
          type: string
        role:
          type: string

    LoginEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/LoginResult'

    LogoutResult:
      type: object
      required: [loggedOut]
      properties:
        loggedOut:
          type: boolean
          const: true

    LogoutEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/LogoutResult'

    AuthScope:
      type: object
      required: [tenantId, branchIds, warehouseIds, routeIds, channels]
      properties:
        tenantId:
          type: integer
        branchIds:
          type: array
          items:
            type: integer
        warehouseIds:
          type: array
          items:
            type: integer
        routeIds:
          type: array
          items:
            type: integer
        channels:
          type: array
          items:
            type: string

    AuthClaims:
      type: object
      required: [userId, tenantId, username, role, permissions, scope]
      properties:
        userId:
          type: integer
        tenantId:
          type: integer
        username:
          type: string
        role:
          type: string
        permissions:
          type: array
          items:
            type: string
        scope:
          $ref: '#/components/schemas/AuthScope'

    AuthClaimsEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/AuthClaims'

    Cliente:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        codigo:
          type: integer
        rsocial:
          type: string
        fantasia:
          type: string
          nullable: true
        cuit:
          type: string
          nullable: true
        condIva:
          type: string
        domicilio:
          type: string
          nullable: true
        localidad:
          type: string
          nullable: true
        cpost:
          type: string
          nullable: true
        telef:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        activo:
          type: boolean
        creditLimit:
          type: number
          nullable: true
          description: Credit limit in ARS. null = no limit.
        creditDays:
          type: integer
          default: 0
          description: Usual credit days for this customer.
        balance:
          type: number
          default: 0
          description: Accumulated balance (incremented on each new factura).
        balanceInicial:
          type: number
          default: 0
          description: Opening balance at migration time.
        score:
          type: integer
          minimum: 0
          maximum: 100
          default: 50
          description: Payment score 0-100 (50=neutral, 0=high risk, 100=perfect).
        suspended:
          type: boolean
          default: false
          description: When true, POST /api/facturas returns 422 CLIENT_SUSPENDED.
        deliveryZoneId:
          type: integer
          nullable: true
          description: FK to DeliveryZone. Assign a delivery zone to this customer.

    ClienteInput:
      type: object
      required: [codigo, rsocial, condIva, activo]
      properties:
        codigo:
          type: integer
          minimum: 1
        rsocial:
          type: string
          minLength: 3
          maxLength: 30
        fantasia:
          type: string
          maxLength: 30
        cuit:
          type: string
        condIva:
          type: string
          enum: [RI, Mono, CF, Exento]
        domicilio:
          type: string
          maxLength: 40
        localidad:
          type: string
          maxLength: 25
        cpost:
          type: string
          maxLength: 8
        telef:
          type: string
          maxLength: 25
        email:
          type: string
          format: email
        activo:
          type: boolean
        creditLimit:
          type: number
          nullable: true
        creditDays:
          type: integer
          minimum: 0
        suspended:
          type: boolean
        deliveryZoneId:
          type: integer
          nullable: true

    ClienteImportRowError:
      type: object
      required: [row, message]
      properties:
        row:
          type: integer
          minimum: 2
          description: Data row number in the file (row 1 is the header)
        message:
          type: string

    ClienteImportResult:
      type: object
      required: [created, skipped, errors]
      properties:
        created:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
          description: Rows updated via upsert (legacy DBF migration endpoints)
        skipped:
          type: integer
          minimum: 0
        errors:
          type: array
          items:
            $ref: '#/components/schemas/ClienteImportRowError'

    ClienteImportEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ClienteImportResult'

    ListPaginationMeta:
      type: object
      required: [total, limit, offset]
      properties:
        total:
          type: integer
          minimum: 0
          description: Row count matching the list filter (before limit/offset)
        limit:
          type: integer
          minimum: 1
          maximum: 500
          description: Effective page size (same semantics as query `limit`)
        offset:
          type: integer
          minimum: 0
          description: Effective skip (same semantics as query `offset`)

    ClienteListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Cliente'
        - $ref: '#/components/schemas/ListPaginationMeta'

    ClienteEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Cliente'

    ClienteNullableEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          anyOf:
            - $ref: '#/components/schemas/Cliente'
            - type: 'null'

    Cobro:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        clienteId:
          type: integer
        fecha:
          type: string
          format: date-time
        monto:
          type: number
        formaPagoId:
          type: integer
          nullable: true
        referencia:
          type: string
          nullable: true
        nota:
          type: string
          nullable: true
        cliente:
          type: object
          additionalProperties: true
          properties:
            id:
              type: integer
            codigo:
              type: integer
            rsocial:
              type: string

    CobroInput:
      type: object
      required: [clienteId, fecha, monto]
      properties:
        clienteId:
          type: integer
          minimum: 1
        fecha:
          type: string
          description: YYYY-MM-DD or ISO-8601
        monto:
          type: number
          exclusiveMinimum: 0
          description: Net amount received (#229); CC decremented by bruto when retenciones present.
        formaPagoId:
          type: integer
          nullable: true
        referencia:
          type: string
          maxLength: 60
        nota:
          type: string
          maxLength: 200
        retenciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoRetencionInput'
        chequeId:
          type: integer
          nullable: true
          description: Link existing portfolio check (#231).
        chequeNuevo:
          $ref: '#/components/schemas/ChequeInput'
          description: Create received check when recording collection (#231).

    CobroRetencion:
      type: object
      required: [id, regimenId, regimenNombre, tipo, baseImponible, alicuota, importe]
      properties:
        id:
          type: integer
        regimenId:
          type: integer
        regimenNombre:
          type: string
        tipo:
          type: string
        baseImponible:
          type: string
        alicuota:
          type: string
        importe:
          type: string
        constanciaNum:
          type: string
          nullable: true

    CobroListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Cobro'
        - $ref: '#/components/schemas/ListPaginationMeta'

    CobroEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Cobro'

    CobroCreateData:
      type: object
      required: [cobro, updatedCliente, retenciones, montoBruto]
      properties:
        cobro:
          $ref: '#/components/schemas/Cobro'
        updatedCliente:
          type: object
          additionalProperties: true
          required: [id, score]
          properties:
            id:
              type: integer
            rsocial:
              type: string
            balance:
              type: number
            creditLimit:
              type: number
              nullable: true
            score:
              type: integer
              minimum: 0
              maximum: 100
        retenciones:
          type: array
          items:
            $ref: '#/components/schemas/CobroRetencion'
        montoBruto:
          type: string
          description: Bruto applied to customer balance (neto + retenciones).

    CobroCreateEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/CobroCreateData'

    FacturaVencidaRow:
      type: object
      required: [facturaId, clienteId, rsocial, total, fecha, diasMora]
      properties:
        facturaId:
          type: integer
        clienteId:
          type: integer
        rsocial:
          type: string
        total:
          type: string
        fecha:
          type: string
          format: date-time
        diasMora:
          type: integer
          minimum: 1

    CobranzasVencidasEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/FacturaVencidaRow'

    CobranzaRecordatorioInput:
      type: object
      required: [facturaId]
      properties:
        facturaId:
          type: integer
          minimum: 1
        canal:
          type: string
          maxLength: 20
          default: email

    CobranzaRecordatorioEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [id]
          properties:
            id:
              type: integer

    OrdenEntrega:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        facturaId:
          type: integer
          nullable: true
        clienteId:
          type: integer
        zonaId:
          type: integer
          nullable: true
        driverId:
          type: integer
          nullable: true
        pickerUserId:
          type: integer
          nullable: true
        pickingIniciadoAt:
          type: string
          format: date-time
          nullable: true
        pickingListoAt:
          type: string
          format: date-time
          nullable: true
        fecha:
          type: string
          format: date-time
        estado:
          type: string
          enum: [pending, picking, ready, assigned, in_transit, delivered, failed, cancelled]
        nota:
          type: string
          nullable: true
        dispatchedAt:
          type: string
          format: date-time
          nullable: true
          description: When the order entered in_transit (ADR-0011).
        dispatchTimestampSource:
          type: string
          nullable: true
          enum: [event, estimated]
          description: How dispatchedAt was derived (audit event vs estimated from updatedAt).
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrdenEntregaLineItem'
        cliente:
          type: object
          additionalProperties: true
        zona:
          type: object
          additionalProperties: true
          nullable: true
        driver:
          type: object
          additionalProperties: true
          nullable: true
        factura:
          type: object
          additionalProperties: true
          nullable: true
        picker:
          type: object
          additionalProperties: true
          nullable: true

    OrdenEntregaLineItem:
      type: object
      required: [id, cantidad, articulo]
      properties:
        id:
          type: integer
        cantidad:
          type: integer
        articulo:
          type: object
          required: [id, codigo, descripcion]
          properties:
            id:
              type: integer
            codigo:
              type: integer
            descripcion:
              type: string

    OrdenEntregaCreateInput:
      type: object
      required: [clienteId, fecha]
      properties:
        clienteId:
          type: integer
          minimum: 1
        fecha:
          type: string
        facturaId:
          type: integer
          nullable: true
        zonaId:
          type: integer
          nullable: true
        driverId:
          type: integer
          nullable: true
        nota:
          type: string
          maxLength: 200

    OrdenEntregaUpdateInput:
      type: object
      required: [estado]
      properties:
        estado:
          type: string
          enum: [pending, picking, ready, assigned, in_transit, delivered, failed, cancelled]
        driverId:
          type: integer
          nullable: true
        zonaId:
          type: integer
          nullable: true
        nota:
          type: string
          maxLength: 200

    OrdenEntregaListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/OrdenEntrega'

    OrdenEntregaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/OrdenEntrega'

    AgingBucket:
      type: object
      required: [label, count, total]
      properties:
        label:
          type: string
          enum: ['0-30d', '31-60d', '61-90d', '>90d']
        count:
          type: integer
          minimum: 0
        total:
          type: string
          description: Decimal amount as string (two fractional digits)

    AgingArResumen:
      type: object
      required: [deudaVencida, deudaPorVencer, porcentajeMora, clientesSuspendidos]
      properties:
        deudaVencida:
          type: string
        deudaPorVencer:
          type: string
        porcentajeMora:
          type: string
        clientesSuspendidos:
          type: integer
          minimum: 0

    AgingAr:
      type: object
      required: [buckets, totalDeuda, resumen]
      properties:
        buckets:
          type: array
          items:
            $ref: '#/components/schemas/AgingBucket'
        totalDeuda:
          type: string
        resumen:
          $ref: '#/components/schemas/AgingArResumen'

    AgingArEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/AgingAr'

    ReporteVentasRow:
      type: object
      required: [periodo, count, total, neto1, neto2, iva1, iva2]
      properties:
        periodo:
          type: string
          description: Bucket key (YYYY-MM-DD, YYYY-MM, or week Monday date)
        count:
          type: integer
          minimum: 0
        total:
          type: string
        neto1:
          type: string
        neto2:
          type: string
        iva1:
          type: string
        iva2:
          type: string

    ReporteVentasListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/ReporteVentasRow'

    LogisticaReturnReasonRow:
      type: object
      required: [motivo, count]
      properties:
        motivo:
          type: string
        count:
          type: integer
          minimum: 0

    LogisticaKpis:
      type: object
      required:
        - dispatchedCount
        - firstVisitDeliveredCount
        - firstVisitRate
        - avgDeliveryMinutes
        - returnsByReason
        - overdueCount
      properties:
        dispatchedCount:
          type: integer
          minimum: 0
        firstVisitDeliveredCount:
          type: integer
          minimum: 0
        firstVisitRate:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
        avgDeliveryMinutes:
          type: number
          nullable: true
        returnsByReason:
          type: array
          items:
            $ref: '#/components/schemas/LogisticaReturnReasonRow'
        overdueCount:
          type: integer
          minimum: 0

    LogisticaKpisEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/LogisticaKpis'

    LogisticaChoferRow:
      type: object
      required: [choferId, choferUsername, day, dispatched, delivered, notDelivered]
      properties:
        choferId:
          type: integer
        choferUsername:
          type: string
        day:
          type: string
          format: date
        dispatched:
          type: integer
          minimum: 0
        delivered:
          type: integer
          minimum: 0
        notDelivered:
          type: integer
          minimum: 0

    LogisticaChoferesListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/LogisticaChoferRow'

    LogisticaZonaRow:
      type: object
      required: [zonaId, zonaNombre, dispatched, delivered, notDelivered]
      properties:
        zonaId:
          type: integer
          nullable: true
        zonaNombre:
          type: string
        dispatched:
          type: integer
          minimum: 0
        delivered:
          type: integer
          minimum: 0
        notDelivered:
          type: integer
          minimum: 0

    LogisticaZonasListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/LogisticaZonaRow'

    StockCriticoArticulo:
      type: object
      required: [id, codigo, descripcion]
      properties:
        id:
          type: integer
        codigo:
          type: integer
        descripcion:
          type: string

    StockCriticoRow:
      type: object
      required: [articulo, stock, minimo, deficit]
      properties:
        articulo:
          $ref: '#/components/schemas/StockCriticoArticulo'
        stock:
          type: integer
        minimo:
          type: integer
        deficit:
          type: integer
          minimum: 0

    StockCriticoListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/StockCriticoRow'

    CobranzasPorFormaPago:
      type: object
      required: [descripcion, total]
      properties:
        formaPagoId:
          type: integer
          nullable: true
        descripcion:
          type: string
        total:
          type: string

    ReporteCobranzasRow:
      type: object
      required: [fecha, count, total, porFormaPago]
      properties:
        fecha:
          type: string
          description: Calendar day YYYY-MM-DD (server local)
        count:
          type: integer
          minimum: 0
        total:
          type: string
        porFormaPago:
          type: array
          items:
            $ref: '#/components/schemas/CobranzasPorFormaPago'

    ReporteCobranzasListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/ReporteCobranzasRow'

    CuentaCorrienteLine:
      type: object
      required: [tipo, fecha, referencia, debito, credito, saldo]
      properties:
        tipo:
          type: string
          enum: [factura, cobro, saldo_inicial]
        fecha:
          type: string
          description: ISO-8601 timestamp (empty for saldo_inicial)
        referencia:
          type: string
        debito:
          type: string
        credito:
          type: string
        saldo:
          type: string
        facturaId:
          type: integer
        cobroId:
          type: integer

    CuentaCorriente:
      type: object
      required: [clienteId, codigo, rsocial, balanceActual, lineas]
      properties:
        clienteId:
          type: integer
        codigo:
          type: integer
        rsocial:
          type: string
        balanceActual:
          type: string
        lineas:
          type: array
          items:
            $ref: '#/components/schemas/CuentaCorrienteLine'

    CuentaCorrienteEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/CuentaCorriente'

    MovimientoProveedorCC:
      type: object
      required: [id, tipo, monto, saldoPost, fecha, usuarioId]
      properties:
        id:
          type: integer
        tipo:
          type: string
          enum: [factura_compra, pago, nc_proveedor, ajuste]
        referencia:
          type: string
          nullable: true
        monto:
          type: string
        saldoPost:
          type: string
        fecha:
          type: string
          format: date-time
        usuarioId:
          type: integer
        notas:
          type: string
          nullable: true

    MovimientoClienteCC:
      type: object
      required: [id, tipo, monto, saldoPost, fecha, usuarioId]
      properties:
        id:
          type: integer
        tipo:
          type: string
          enum: [saldo_inicial, factura, nota_credito, cobro, retencion, percepcion, cheque_rechazado, ajuste]
        referencia:
          type: string
          nullable: true
        monto:
          type: string
        saldoPost:
          type: string
        fecha:
          type: string
          format: date-time
        usuarioId:
          type: integer
        notas:
          type: string
          nullable: true
        facturaId:
          type: integer
          nullable: true
        cobroId:
          type: integer
          nullable: true
        notaCreditoId:
          type: integer
          nullable: true
        chequeId:
          type: integer
          nullable: true
        retencionAplicadaId:
          type: integer
          nullable: true

    ClienteCuentaCorrienteChartPoint:
      type: object
      required: [period, saldo]
      properties:
        period:
          type: string
        saldo:
          type: string

    ClienteCuentaCorriente:
      type: object
      required: [clienteId, codigo, rsocial, saldo, excedeLimite, movimientos, serie, total, limit, offset]
      properties:
        clienteId:
          type: integer
        codigo:
          type: integer
        rsocial:
          type: string
        saldo:
          type: string
        creditLimit:
          type: string
          nullable: true
        excedeLimite:
          type: boolean
        movimientos:
          type: array
          items:
            $ref: '#/components/schemas/MovimientoClienteCC'
        serie:
          type: array
          items:
            $ref: '#/components/schemas/ClienteCuentaCorrienteChartPoint'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    ClienteCuentaCorrienteSaldo:
      type: object
      required: [clienteId, saldo, excedeLimite]
      properties:
        clienteId:
          type: integer
        saldo:
          type: string
        creditLimit:
          type: string
          nullable: true
        excedeLimite:
          type: boolean

    ClienteCuentaCorrienteAntiguedadBucket:
      type: object
      required: [label, total]
      properties:
        label:
          type: string
          enum: ['0-30', '31-60', '61-90', '+90']
        total:
          type: string

    ClienteCuentaCorrienteAntiguedad:
      type: object
      required: [clienteId, buckets, totalPendiente]
      properties:
        clienteId:
          type: integer
        buckets:
          type: array
          items:
            $ref: '#/components/schemas/ClienteCuentaCorrienteAntiguedadBucket'
        totalPendiente:
          type: string

    ClienteCuentaCorrienteAjusteInput:
      type: object
      required: [monto, motivo]
      properties:
        monto:
          type: number
        motivo:
          type: string
          maxLength: 500

    ClienteCuentaCorrienteEnviarInput:
      type: object
      properties:
        email:
          type: string
          format: email
        desde:
          type: string
          format: date-time
        hasta:
          type: string
          format: date-time

    ProveedorCuentaCorrienteChartPoint:
      type: object
      required: [period, saldo]
      properties:
        period:
          type: string
          description: YYYY-MM month bucket
        saldo:
          type: string

    ProveedorCuentaCorriente:
      type: object
      required: [proveedorId, codigo, rsocial, saldo, excedeLimite, movimientos, serie]
      properties:
        proveedorId:
          type: integer
        codigo:
          type: integer
        rsocial:
          type: string
        saldo:
          type: string
        limiteCredito:
          type: string
          nullable: true
        excedeLimite:
          type: boolean
        movimientos:
          type: array
          items:
            $ref: '#/components/schemas/MovimientoProveedorCC'
        serie:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorCuentaCorrienteChartPoint'

    ProveedorCuentaCorrienteSaldo:
      type: object
      required: [proveedorId, saldo, excedeLimite]
      properties:
        proveedorId:
          type: integer
        saldo:
          type: string
        limiteCredito:
          type: string
          nullable: true
        excedeLimite:
          type: boolean

    ProveedorCuentaCorrienteAjusteInput:
      type: object
      required: [monto, motivo]
      properties:
        monto:
          type: number
          description: Non-zero; positive increases debt, negative reduces debt
        motivo:
          type: string
          maxLength: 500

    ProveedorCompraEstadoPago:
      type: string
      enum: [pendiente, parcial, pagada, n_a]

    ProveedorCompraRow:
      type: object
      required: [tipo, id, fecha, referencia, total, estadoPago]
      properties:
        tipo:
          type: string
          enum: [orden_compra, comprobante]
        id:
          type: integer
        fecha:
          type: string
          format: date-time
        referencia:
          type: string
        total:
          type: string
        estadoPago:
          $ref: '#/components/schemas/ProveedorCompraEstadoPago'
        ordenCompraId:
          type: integer
        estado:
          type: string
          description: OC lifecycle state when tipo is orden_compra

    ProveedorHistorialTopArticulo:
      type: object
      required: [articuloId, codigo, descripcion, cantidadTotal, montoTotal]
      properties:
        articuloId:
          type: integer
        codigo:
          type: string
        descripcion:
          type: string
        cantidadTotal:
          type: integer
        montoTotal:
          type: string

    ProveedorHistorialResumen:
      type: object
      required:
        - periodoDias
        - totalComprado
        - frecuenciaCompraDias
        - cantidadCompras
        - topArticulos
        - compras
      properties:
        periodoDias:
          type: integer
          enum: [30, 90, 180, 365]
        totalComprado:
          type: string
        frecuenciaCompraDias:
          type: integer
          nullable: true
        cantidadCompras:
          type: integer
        topArticulos:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorHistorialTopArticulo'
        compras:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorCompraRow'

    ProveedorHistorialEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorHistorialResumen'

    ProveedorArticuloPrecioPunto:
      type: object
      required: [fecha, precioUnitario, cantidad]
      properties:
        fecha:
          type: string
          format: date-time
        precioUnitario:
          type: string
        cantidad:
          type: integer

    ProveedorArticuloHistorialRow:
      type: object
      required:
        - articuloId
        - codigo
        - descripcion
        - cantidadTotal
        - precioPromedioPonderado
        - montoTotal
        - evolucionPrecios
      properties:
        articuloId:
          type: integer
        codigo:
          type: string
        descripcion:
          type: string
        cantidadTotal:
          type: integer
        precioPromedioPonderado:
          type: string
        montoTotal:
          type: string
        evolucionPrecios:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorArticuloPrecioPunto'

    ProveedorArticulosHistorialData:
      type: object
      required: [articulos]
      properties:
        articulos:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorArticuloHistorialRow'

    ProveedorArticulosHistorialEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorArticulosHistorialData'

    ProveedorCatalogoArticuloRef:
      type: object
      required: [id, codigo, descripcion]
      properties:
        id:
          type: integer
        codigo:
          type: integer
        descripcion:
          type: string

    ProveedorCatalogoRow:
      type: object
      required:
        - id
        - articuloId
        - codigoProveedor
        - descripcion
        - precioLista
        - precioListaFecha
        - unidadCompra
        - multiplo
        - activo
        - articulo
      properties:
        id:
          type: integer
        articuloId:
          type: integer
        codigoProveedor:
          type: string
        descripcion:
          type: string
          nullable: true
        precioLista:
          type: string
          nullable: true
        precioListaFecha:
          type: string
          format: date-time
          nullable: true
        unidadCompra:
          type: string
          nullable: true
        multiplo:
          type: string
        activo:
          type: boolean
        articulo:
          $ref: '#/components/schemas/ProveedorCatalogoArticuloRef'

    ProveedorCatalogoListData:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorCatalogoRow'

    ProveedorCatalogoListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorCatalogoListData'

    ProveedorCatalogoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorCatalogoRow'

    ProveedorCatalogoInput:
      type: object
      required: [articuloId, codigoProveedor]
      properties:
        articuloId:
          type: integer
          minimum: 1
        codigoProveedor:
          type: string
          minLength: 1
          maxLength: 50
        descripcion:
          type: string
          maxLength: 120
          nullable: true
        precioLista:
          type: number
          minimum: 0
          nullable: true
        unidadCompra:
          type: string
          maxLength: 30
          nullable: true
        multiplo:
          type: number
          exclusiveMinimum: 0
        activo:
          type: boolean

    ProveedorCatalogoUpdateInput:
      type: object
      properties:
        codigoProveedor:
          type: string
          minLength: 1
          maxLength: 50
        descripcion:
          type: string
          maxLength: 120
          nullable: true
        precioLista:
          type: number
          minimum: 0
          nullable: true
        unidadCompra:
          type: string
          maxLength: 30
          nullable: true
        multiplo:
          type: number
          exclusiveMinimum: 0
        activo:
          type: boolean

    ProveedorCatalogoImportError:
      type: object
      required: [row, message]
      properties:
        row:
          type: integer
        message:
          type: string

    ProveedorCatalogoImportData:
      type: object
      required: [created, updated, skipped, errors]
      properties:
        created:
          type: integer
        updated:
          type: integer
        skipped:
          type: integer
        errors:
          type: array
          items:
            $ref: '#/components/schemas/ProveedorCatalogoImportError'

    ProveedorCatalogoImportEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorCatalogoImportData'

    ArticuloProveedorComparadorRow:
      type: object
      required:
        - proveedorId
        - proveedorCodigo
        - proveedorRsocial
        - codigoProveedor
        - descripcionProveedor
        - precioLista
        - precioListaFecha
        - precioDesactualizado
        - ultimaCompraFecha
        - esMasBarato
      properties:
        proveedorId:
          type: integer
        proveedorCodigo:
          type: integer
        proveedorRsocial:
          type: string
        codigoProveedor:
          type: string
        descripcionProveedor:
          type: string
          nullable: true
        precioLista:
          type: string
          nullable: true
        precioListaFecha:
          type: string
          format: date-time
          nullable: true
        precioDesactualizado:
          type: boolean
        ultimaCompraFecha:
          type: string
          format: date-time
          nullable: true
        esMasBarato:
          type: boolean

    ArticuloProveedoresComparadorData:
      type: object
      required:
        - articuloId
        - articuloCodigo
        - articuloDescripcion
        - proveedorMasBaratoId
        - proveedores
      properties:
        articuloId:
          type: integer
        articuloCodigo:
          type: integer
        articuloDescripcion:
          type: string
        proveedorMasBaratoId:
          type: integer
          nullable: true
        proveedores:
          type: array
          items:
            $ref: '#/components/schemas/ArticuloProveedorComparadorRow'

    ArticuloProveedoresComparadorEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ArticuloProveedoresComparadorData'

    ClienteCuentaCorrienteEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ClienteCuentaCorriente'

    ClienteCuentaCorrienteSaldoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ClienteCuentaCorrienteSaldo'

    ClienteCuentaCorrienteAntiguedadEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ClienteCuentaCorrienteAntiguedad'

    MovimientoClienteCCEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/MovimientoClienteCC'

    ClienteCuentaCorrienteEnviarEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [sent, email]
          properties:
            sent:
              type: boolean
            email:
              type: string

    ProveedorCuentaCorrienteEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorCuentaCorriente'

    ProveedorCuentaCorrienteSaldoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ProveedorCuentaCorrienteSaldo'

    MovimientoProveedorCCEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/MovimientoProveedorCC'

    ReciboPagoFactura:
      type: object
      required: [id, facturaRef, monto]
      properties:
        id:
          type: integer
        comprobanteCompraId:
          type: integer
          nullable: true
        facturaRef:
          type: string
        monto:
          type: string

    ReciboPago:
      type: object
      required:
        - id
        - numero
        - proveedorId
        - fecha
        - total
        - metodoPago
        - estado
        - usuarioId
        - proveedor
        - usuario
        - facturas
        - retenciones
        - totalBruto
        - createdAt
      properties:
        id:
          type: integer
        numero:
          type: integer
        proveedorId:
          type: integer
        fecha:
          type: string
          format: date-time
        total:
          type: string
        totalBruto:
          type: string
        metodoPago:
          type: string
          enum: [transferencia, cheque, efectivo, echeq]
        cbu:
          type: string
          nullable: true
        referencia:
          type: string
          nullable: true
        estado:
          type: string
          enum: [emitido, anulado]
        notas:
          type: string
          nullable: true
        usuarioId:
          type: integer
        proveedor:
          type: object
          required: [id, codigo, rsocial]
          properties:
            id:
              type: integer
            codigo:
              type: integer
            rsocial:
              type: string
            cuit:
              type: string
              nullable: true
        usuario:
          type: object
          required: [id, username]
          properties:
            id:
              type: integer
            username:
              type: string
        facturas:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoFactura'
        retenciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoRetencion'
        createdAt:
          type: string
          format: date-time

    ReciboPagoInput:
      type: object
      required: [fecha, total, metodoPago, facturas]
      properties:
        fecha:
          type: string
        total:
          type: number
          exclusiveMinimum: 0
        metodoPago:
          type: string
          enum: [transferencia, cheque, efectivo, echeq]
        cbu:
          type: string
          maxLength: 22
          nullable: true
        referencia:
          type: string
          maxLength: 60
          nullable: true
        notas:
          type: string
          maxLength: 500
          nullable: true
        chequeId:
          type: integer
          nullable: true
          description: Portfolio check to endorse when metodoPago is cheque or echeq (#231).
        facturas:
          type: array
          minItems: 1
          items:
            type: object
            required: [facturaRef, monto]
            properties:
              comprobanteCompraId:
                type: integer
                nullable: true
              facturaRef:
                type: string
              monto:
                type: number
                exclusiveMinimum: 0
        retenciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoRetencionInput'

    ReciboPagoRetencionInput:
      type: object
      required: [regimenId, baseImponible, alicuota, importe]
      properties:
        regimenId:
          type: integer
          minimum: 1
        baseImponible:
          type: number
          exclusiveMinimum: 0
        alicuota:
          type: number
          minimum: 0
          maximum: 100
        importe:
          type: number
          exclusiveMinimum: 0

    ReciboPagoRetencion:
      type: object
      required:
        - id
        - regimenId
        - regimenNombre
        - tipo
        - baseImponible
        - alicuota
        - importe
        - constanciaNum
      properties:
        id:
          type: integer
        regimenId:
          type: integer
        regimenNombre:
          type: string
        tipo:
          type: string
        baseImponible:
          type: string
        alicuota:
          type: string
        importe:
          type: string
        constanciaNum:
          type: string
          nullable: true

    ComprobantePendiente:
      type: object
      required: [comprobanteCompraId, facturaRef, fecha, total, pagado, pendiente]
      properties:
        comprobanteCompraId:
          type: integer
        facturaRef:
          type: string
        fecha:
          type: string
          format: date-time
        total:
          type: string
        pagado:
          type: string
        pendiente:
          type: string

    ReciboPagoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/ReciboPago'
        - $ref: '#/components/schemas/ListPaginationMeta'

    FacturaPendienteCliente:
      type: object
      required: [facturaId, facturaRef, fecha, total, pagado, pendiente]
      properties:
        facturaId:
          type: integer
        facturaRef:
          type: string
        fecha:
          type: string
          format: date-time
        total:
          type: string
        pagado:
          type: string
        pendiente:
          type: string

    FacturaPendienteClienteListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/FacturaPendienteCliente'

    ReciboCobroForma:
      type: object
      required: [id, tipo, importe]
      properties:
        id:
          type: integer
        tipo:
          type: string
          enum: [efectivo, transferencia, cheque, mercadopago, tarjeta, otro]
        importe:
          type: string
        chequeId:
          type: integer
          nullable: true
        referencia:
          type: string
          nullable: true
        banco:
          type: string
          nullable: true
        chequeNumero:
          type: string
          nullable: true
        chequeBanco:
          type: string
          nullable: true

    ReciboCobroImputacion:
      type: object
      required: [id, facturaId, facturaRef, importe, saldoPrevio, saldoPostPago]
      properties:
        id:
          type: integer
        facturaId:
          type: integer
        facturaRef:
          type: string
        importe:
          type: string
        saldoPrevio:
          type: string
        saldoPostPago:
          type: string

    ReciboCobro:
      type: object
      required:
        - id
        - numero
        - clienteId
        - fecha
        - totalCobrado
        - totalBruto
        - estado
        - usuarioId
        - cliente
        - usuario
        - formas
        - imputaciones
        - retenciones
        - createdAt
      properties:
        id:
          type: integer
        numero:
          type: integer
        clienteId:
          type: integer
        fecha:
          type: string
          format: date-time
        totalCobrado:
          type: string
        totalBruto:
          type: string
        concepto:
          type: string
          nullable: true
        estado:
          type: string
          enum: [emitido, anulado]
        anulacionMotivo:
          type: string
          nullable: true
        usuarioId:
          type: integer
        cliente:
          type: object
          required: [id, codigo, rsocial]
          properties:
            id:
              type: integer
            codigo:
              type: integer
            rsocial:
              type: string
            cuit:
              type: string
              nullable: true
        usuario:
          type: object
          required: [id, username]
          properties:
            id:
              type: integer
            username:
              type: string
        formas:
          type: array
          items:
            $ref: '#/components/schemas/ReciboCobroForma'
        imputaciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboCobroImputacion'
        retenciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoRetencion'
        createdAt:
          type: string
          format: date-time

    ReciboCobroInput:
      type: object
      required: [fecha, totalCobrado, formas]
      properties:
        fecha:
          type: string
        totalCobrado:
          type: number
          exclusiveMinimum: 0
        concepto:
          type: string
          maxLength: 500
          nullable: true
        fifo:
          type: boolean
          default: true
        formas:
          type: array
          minItems: 1
          items:
            type: object
            required: [tipo, importe]
            properties:
              tipo:
                type: string
                enum: [efectivo, transferencia, cheque, mercadopago, tarjeta, otro]
              importe:
                type: number
                exclusiveMinimum: 0
              chequeId:
                type: integer
                nullable: true
              referencia:
                type: string
                nullable: true
              banco:
                type: string
                nullable: true
        imputaciones:
          type: array
          items:
            type: object
            required: [facturaId, importe]
            properties:
              facturaId:
                type: integer
                minimum: 1
              importe:
                type: number
                exclusiveMinimum: 0
        retenciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoRetencionInput'

    ReciboCobroVoidInput:
      type: object
      required: [anulacionMotivo]
      properties:
        anulacionMotivo:
          type: string
          minLength: 3
          maxLength: 500

    ReciboCobroListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/ReciboCobro'
        - $ref: '#/components/schemas/ListPaginationMeta'

    ReciboCobroEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ReciboCobro'

    ReciboPagoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ReciboPago'

    ComprobantePendienteListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/ComprobantePendiente'

    Articulo:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        codigo:
          type: integer
        descripcion:
          type: string
        rubroId:
          type: integer
        rubro:
          $ref: '#/components/schemas/Rubro'
        condIva:
          type: string
        umedida:
          type: string
        tipo:
          type: string
          enum: [articulo, servicio]
          description: Catalog item kind (#244). Default articulo.
        unidadServicio:
          type: string
          enum: [hora, dia, mes, proyecto, km, unidad, otro]
          nullable: true
          description: Required when tipo is servicio (#244).
        mesesGarantia:
          type: integer
          nullable: true
          minimum: 1
          description: Warranty months for physical articles; null = no warranty (#251).
        precioLista1:
          type: number
        precioLista2:
          type: number
        costo:
          type: number
        stock:
          type: integer
        minimo:
          type: integer
        activo:
          type: boolean

    ArticuloInput:
      type: object
      required: [codigo, descripcion, rubroId, condIva, umedida, precioLista1, precioLista2, costo, stock, minimo, activo]
      properties:
        codigo:
          type: integer
          minimum: 1
        descripcion:
          type: string
          minLength: 3
          maxLength: 30
        rubroId:
          type: integer
        condIva:
          type: string
          enum: ['1', '2', '3']
        umedida:
          type: string
          minLength: 2
          maxLength: 6
        tipo:
          type: string
          enum: [articulo, servicio]
          default: articulo
        unidadServicio:
          type: string
          enum: [hora, dia, mes, proyecto, km, unidad, otro]
          nullable: true
        mesesGarantia:
          type: integer
          nullable: true
          minimum: 1
          description: Warranty months; ignored for servicio (#251).
        precioLista1:
          type: number
          minimum: 0.01
        precioLista2:
          type: number
          minimum: 0.01
        costo:
          type: number
          minimum: 0.01
        stock:
          type: integer
          minimum: 0
          description: Must be 0 when tipo is servicio (#244).
        minimo:
          type: integer
          minimum: 0
          description: Must be 0 when tipo is servicio (#244).
        activo:
          type: boolean

    ArticuloListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Articulo'
        - $ref: '#/components/schemas/ListPaginationMeta'

    ArticuloEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Articulo'

    ArticuloNullableEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          anyOf:
            - $ref: '#/components/schemas/Articulo'
            - type: 'null'

    StockAjusteInput:
      type: object
      required: [cantidad, motivo]
      properties:
        cantidad:
          type: integer
        motivo:
          type: string
          minLength: 1
          maxLength: 100

    StockAjusteUser:
      type: object
      properties:
        id:
          type: integer
        username:
          type: string

    StockAjuste:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        articuloId:
          type: integer
        cantidad:
          type: integer
        motivo:
          type: string
        userId:
          type: integer
        createdAt:
          type: string
          format: date-time
        user:
          $ref: '#/components/schemas/StockAjusteUser'

    StockAdjustData:
      type: object
      additionalProperties: true
      properties:
        ajuste:
          $ref: '#/components/schemas/StockAjuste'
        articulo:
          type: object
          additionalProperties: true
        stockBefore:
          type: integer
        stockAfter:
          type: integer

    StockAdjustEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/StockAdjustData'

    StockAjusteListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/StockAjuste'
        - $ref: '#/components/schemas/ListPaginationMeta'

    Rubro:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        codigo:
          type: integer
        nombre:
          type: string

    RubroInput:
      type: object
      required: [codigo, nombre]
      properties:
        codigo:
          type: integer
          minimum: 1
        nombre:
          type: string
          maxLength: 20

    RubroListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Rubro'
        - $ref: '#/components/schemas/ListPaginationMeta'

    RubroEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Rubro'

    ProveedorInput:
      type: object
      required: [codigo, rsocial, condIva, activo]
      properties:
        codigo:
          type: integer
          minimum: 1
        rsocial:
          type: string
          minLength: 3
          maxLength: 30
        fantasia:
          type: string
          nullable: true
        cuit:
          type: string
          nullable: true
        condIva:
          type: string
          enum: [RI, Mono, CF, Exento]
        telef:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        activo:
          type: boolean
        cbu:
          type: string
          nullable: true
          maxLength: 22
        alias:
          type: string
          nullable: true
        banco:
          type: string
          nullable: true
        tipoCuenta:
          type: string
          nullable: true
          enum: [cc, ca]
        moneda:
          type: string
          default: ARS
        condicionPago:
          type: string
          nullable: true
          enum: [contado, 15dias, 30dias, 60dias, otro]
        plazoHabitual:
          type: integer
          nullable: true
          minimum: 0
        descuentoPct:
          type: number
          nullable: true
        limiteCredito:
          type: number
          nullable: true
        categoria:
          type: string
          nullable: true
          enum: [materia_prima, insumos, servicios, logistica]
        contactoNombre:
          type: string
          nullable: true
        contactoEmail:
          type: string
          nullable: true
        contactoTel:
          type: string
          nullable: true
        notas:
          type: string
          nullable: true

    Proveedor:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        codigo:
          type: integer
        rsocial:
          type: string
        fantasia:
          type: string
          nullable: true
        cuit:
          type: string
          nullable: true
        condIva:
          type: string
        telef:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        activo:
          type: boolean
        cbu:
          type: string
          nullable: true
        alias:
          type: string
          nullable: true
        banco:
          type: string
          nullable: true
        tipoCuenta:
          type: string
          nullable: true
        moneda:
          type: string
        condicionPago:
          type: string
          nullable: true
        plazoHabitual:
          type: integer
          nullable: true
        descuentoPct:
          type: number
          nullable: true
        limiteCredito:
          type: number
          nullable: true
        categoria:
          type: string
          nullable: true
        contactoNombre:
          type: string
          nullable: true
        contactoEmail:
          type: string
          nullable: true
        contactoTel:
          type: string
          nullable: true
        notas:
          type: string
          nullable: true

    ProveedorListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Proveedor'
        - $ref: '#/components/schemas/ListPaginationMeta'

    OrdenCompraItemLine:
      type: object
      required: [id, articuloId, cantidad, cantidadRecibida, costoUnitario, subtotal]
      properties:
        id:
          type: integer
        articuloId:
          type: integer
        codigoProveedor:
          type: string
          nullable: true
          description: Supplier catalog code snapshot (#323)
        descripcionProveedor:
          type: string
          nullable: true
          description: Supplier catalog description snapshot (#323)
        cantidad:
          type: integer
        cantidadRecibida:
          type: integer
        costoUnitario:
          type: string
        subtotal:
          type: string
        articulo:
          type: object
          properties:
            id:
              type: integer
            codigo:
              type: integer
            descripcion:
              type: string

    OrdenCompra:
      type: object
      required: [id, tenantId, proveedorId, estado, total]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        proveedorId:
          type: integer
        estado:
          type: string
          enum: [draft, sent, received, cancelled]
        total:
          type: string
        fechaEstimada:
          type: string
          format: date-time
          nullable: true
        nota:
          type: string
          nullable: true
        proveedor:
          type: object
          properties:
            id:
              type: integer
            codigo:
              type: integer
            rsocial:
              type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrdenCompraItemLine'

    OrdenCompraListEnvelope:
      allOf:
        - $ref: '#/components/schemas/ListPaginationMeta'
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/OrdenCompra'

    OrdenCompraEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/OrdenCompra'

    OrdenCompraItemInput:
      type: object
      required: [articuloId, cantidad, costoUnitario]
      properties:
        articuloId:
          type: integer
        cantidad:
          type: integer
          minimum: 1
        costoUnitario:
          type: number
          minimum: 0

    OrdenCompraCreateInput:
      type: object
      required: [proveedorId, items]
      properties:
        proveedorId:
          type: integer
        fechaEstimada:
          type: string
          format: date
          nullable: true
        nota:
          type: string
          maxLength: 200
          nullable: true
        items:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OrdenCompraItemInput'

    OrdenCompraUpdateInput:
      type: object
      properties:
        proveedorId:
          type: integer
        fechaEstimada:
          type: string
          format: date
          nullable: true
        nota:
          type: string
          maxLength: 200
          nullable: true
        items:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OrdenCompraItemInput'

    OrdenCompraReceiveInput:
      type: object
      required: [lines]
      properties:
        lines:
          type: array
          minItems: 1
          items:
            type: object
            required: [itemId, cantidad]
            properties:
              itemId:
                type: integer
              cantidad:
                type: integer
                minimum: 1

    RecuentoItemLine:
      type: object
      required: [id, articuloId, cantSistema]
      properties:
        id:
          type: integer
        articuloId:
          type: integer
        cantSistema:
          type: integer
        cantFisica:
          type: integer
          nullable: true
        articulo:
          type: object
          properties:
            id:
              type: integer
            codigo:
              type: integer
            descripcion:
              type: string

    Recuento:
      type: object
      required: [id, operadorId, estado, fecha, items]
      properties:
        id:
          type: integer
        operadorId:
          type: integer
        estado:
          type: string
          enum: [in_progress, closed]
        fecha:
          type: string
          format: date-time
        closedAt:
          type: string
          format: date-time
          nullable: true
        operador:
          type: object
          properties:
            id:
              type: integer
            username:
              type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/RecuentoItemLine'

    RecuentoListEnvelope:
      allOf:
        - $ref: '#/components/schemas/ListPaginationMeta'
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Recuento'

    RecuentoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Recuento'

    RecuentoItemsInput:
      type: object
      required: [lines]
      properties:
        lines:
          type: array
          minItems: 1
          items:
            type: object
            required: [articuloId, cantFisica]
            properties:
              articuloId:
                type: integer
                minimum: 1
              cantFisica:
                type: integer
                minimum: 0

    RepartoProgress:
      type: object
      required: [total, delivered, pending]
      properties:
        total:
          type: integer
          minimum: 0
        delivered:
          type: integer
          minimum: 0
        pending:
          type: integer
          minimum: 0

    RepartoItemLine:
      type: object
      required: [id, ordenEntregaId, secuencia, estado, hasPod, ordenEntrega]
      properties:
        id:
          type: integer
        ordenEntregaId:
          type: integer
        secuencia:
          type: integer
        estado:
          type: string
          enum: [pending, delivered, not_delivered, returned]
        entregadoAt:
          type: string
          format: date-time
          nullable: true
        motivoNoEntrega:
          type: string
          nullable: true
        receptorNombre:
          type: string
          nullable: true
        receptorDni:
          type: string
          nullable: true
        notasEntrega:
          type: string
          nullable: true
        hasPod:
          type: boolean
        ordenEntrega:
          $ref: '#/components/schemas/OrdenEntrega'

    RepartoItemPodInput:
      type: object
      required: [outcome]
      properties:
        outcome:
          type: string
          enum: [delivered, not_delivered]
        receptorNombre:
          type: string
          maxLength: 120
          nullable: true
        receptorDni:
          type: string
          maxLength: 20
          nullable: true
        firmaBase64:
          type: string
          nullable: true
          description: Data URL or base64 signature; max ~50KB decoded when delivered.
        fotoBase64:
          type: string
          nullable: true
          description: Data URL or base64 photo; max ~200KB decoded.
        notasEntrega:
          type: string
          maxLength: 500
          nullable: true
        motivoNoEntrega:
          type: string
          nullable: true
          enum: [ausente, rechazo, domicilio_incorrecto, producto_dañado, otro]

    RepartoItemPodMedia:
      type: object
      properties:
        firmaBase64:
          type: string
          nullable: true
        fotoBase64:
          type: string
          nullable: true

    RepartoItemPod:
      allOf:
        - $ref: '#/components/schemas/RepartoItemLine'
        - type: object
          required: [podMedia]
          properties:
            podMedia:
              $ref: '#/components/schemas/RepartoItemPodMedia'
              nullable: true

    RepartoItemPodEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/RepartoItemPod'

    RepartoItemLineEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/RepartoItemLine'

    Reparto:
      type: object
      required: [id, tenantId, fecha, choferId, estado, items, progress]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        fecha:
          type: string
          format: date-time
        choferId:
          type: integer
        estado:
          type: string
          enum: [planned, on_route, completed, cancelled]
        vehiculo:
          type: string
          nullable: true
        observaciones:
          type: string
          nullable: true
        closedAt:
          type: string
          format: date-time
          nullable: true
        chofer:
          type: object
          properties:
            id:
              type: integer
            username:
              type: string
            role:
              type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/RepartoItemLine'
        progress:
          $ref: '#/components/schemas/RepartoProgress'

    RepartoCreateInput:
      type: object
      required: [fecha, choferId, ordenEntregaIds]
      properties:
        fecha:
          type: string
          format: date
        choferId:
          type: integer
          minimum: 1
        vehiculo:
          type: string
          maxLength: 60
          nullable: true
        observaciones:
          type: string
          maxLength: 500
          nullable: true
        ordenEntregaIds:
          type: array
          minItems: 1
          items:
            type: integer
            minimum: 1

    RepartoListEnvelope:
      allOf:
        - $ref: '#/components/schemas/ListPaginationMeta'
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Reparto'

    RepartoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Reparto'

    RepartoCloseSummary:
      type: object
      required: [pendingClosed, delivered, notDelivered, returned]
      properties:
        pendingClosed:
          type: integer
          minimum: 0
        delivered:
          type: integer
          minimum: 0
        notDelivered:
          type: integer
          minimum: 0
        returned:
          type: integer
          minimum: 0

    RepartoCloseEnvelope:
      type: object
      required: [success, data, summary]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Reparto'
        summary:
          $ref: '#/components/schemas/RepartoCloseSummary'

    RepartoUbicacionInput:
      type: object
      required: [lat, lng]
      properties:
        lat:
          type: number
          minimum: -90
          maximum: 90
        lng:
          type: number
          minimum: -180
          maximum: 180

    RepartoUbicacion:
      type: object
      required: [lat, lng, recordedAt]
      properties:
        lat:
          type: number
        lng:
          type: number
        recordedAt:
          type: string
          format: date-time

    RepartoUbicacionEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/RepartoUbicacion'

    RepartoUbicacionNullableEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          oneOf:
            - $ref: '#/components/schemas/RepartoUbicacion'
            - type: 'null'

    RepartoActivoCurrentStop:
      type: object
      required: [secuencia, cliente]
      properties:
        secuencia:
          type: integer
          minimum: 1
        cliente:
          type: object
          required: [id, codigo, rsocial]
          properties:
            id:
              type: integer
            codigo:
              type: integer
            rsocial:
              type: string
            domicilio:
              type: string
              nullable: true
        zona:
          type: object
          nullable: true
          properties:
            id:
              type: integer
            nombre:
              type: string

    RepartoActivo:
      type: object
      required: [id, tenantId, fecha, choferId, estado, chofer, progress, ultimaUbicacion, currentStop]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        fecha:
          type: string
          format: date-time
        choferId:
          type: integer
        estado:
          type: string
        vehiculo:
          type: string
          nullable: true
        observaciones:
          type: string
          nullable: true
        chofer:
          type: object
          required: [id, username, role]
          properties:
            id:
              type: integer
            username:
              type: string
            role:
              type: string
        progress:
          $ref: '#/components/schemas/RepartoProgress'
        ultimaUbicacion:
          oneOf:
            - $ref: '#/components/schemas/RepartoUbicacion'
            - type: 'null'
        currentStop:
          oneOf:
            - $ref: '#/components/schemas/RepartoActivoCurrentStop'
            - type: 'null'

    RepartoActivoListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/RepartoActivo'

    ProveedorEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Proveedor'

    ProveedorNullableEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          anyOf:
            - $ref: '#/components/schemas/Proveedor'
            - type: 'null'

    Factura:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        fecha:
          type: string
          format: date-time
        tipo:
          type: string
        prefijo:
          type: string
        numero:
          type: integer
        clienteId:
          type: integer
        formaPagoId:
          type: integer
          nullable: true
        neto1:
          type: number
        neto2:
          type: number
        neto3:
          type: number
        iva1:
          type: number
        iva2:
          type: number
        total:
          type: number
        estado:
          type: string
        cae:
          type: string
          nullable: true
        caeVto:
          type: string
          format: date-time
          nullable: true
        estadoCae:
          type: string
          enum: [pending, issued, failed]
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/FacturaItem'

    FacturaItem:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        articuloId:
          type: integer
          nullable: true
          description: Null for ad-hoc service lines (#244).
        descripcion:
          type: string
          maxLength: 120
          description: Line snapshot (catalog or free-text) (#244).
        condIva:
          type: string
          enum: ['1', '2', '3']
        unidadServicio:
          type: string
          enum: [hora, dia, mes, proyecto, km, unidad, otro]
          nullable: true
        articulo:
          $ref: '#/components/schemas/Articulo'
        cantidad:
          type: number
        precio:
          type: number
        dscto:
          type: number
        subtotal:
          type: number

    RemitoItemInput:
      type: object
      required: [articuloId, descripcion, cantidad, unidad]
      properties:
        articuloId:
          type: integer
        descripcion:
          type: string
        cantidad:
          type: integer
          minimum: 1
        unidad:
          type: string

    RemitoInput:
      type: object
      required: [tipo, items]
      properties:
        tipo:
          type: string
          enum: [remito_x, remito_ingreso]
        clienteId:
          type: integer
          nullable: true
        proveedorId:
          type: integer
          nullable: true
        facturaId:
          type: integer
          nullable: true
        pedidoId:
          type: integer
          nullable: true
        ordenEntregaId:
          type: integer
          nullable: true
        fecha:
          type: string
          format: date
        observaciones:
          type: string
          nullable: true
        items:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/RemitoItemInput'

    RemitoUpdateInput:
      type: object
      properties:
        clienteId:
          type: integer
          nullable: true
        proveedorId:
          type: integer
          nullable: true
        observaciones:
          type: string
          nullable: true
        items:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/RemitoItemInput'

    RemitoEntregarInput:
      type: object
      required: [firmadoPor]
      properties:
        firmadoPor:
          type: string
          minLength: 2
          maxLength: 120
        fechaEntrega:
          type: string
          format: date

    Remito:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        referencia:
          type: string
        prefijo:
          type: string
          nullable: true
        numero:
          type: integer
          nullable: true
        tipo:
          type: string
        estado:
          type: string
          enum: [borrador, emitido, entregado, anulado]
        clienteId:
          type: integer
          nullable: true
        facturaId:
          type: integer
          nullable: true
        pedidoId:
          type: integer
          nullable: true
        fecha:
          type: string
          format: date-time
        fechaEntrega:
          type: string
          format: date-time
          nullable: true
        observaciones:
          type: string
          nullable: true
        firmadoPor:
          type: string
          nullable: true
        items:
          type: array
          items:
            type: object
            additionalProperties: true

    RemitoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
        data:
          $ref: '#/components/schemas/Remito'

    RemitoListEnvelope:
      type: object
      required: [success, data, total, limit, offset]
      properties:
        success:
          type: boolean
        data:
          type: array
          items:
            $ref: '#/components/schemas/Remito'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    Cheque:
      type: object
      required:
        - id
        - tipo
        - modalidad
        - numero
        - banco
        - libradorNombre
        - monto
        - moneda
        - fechaEmision
        - fechaVencimiento
        - estado
      properties:
        id:
          type: integer
        tipo:
          type: string
          enum: [recibido, emitido]
        modalidad:
          type: string
          enum: [fisico, echeq]
        numero:
          type: string
        banco:
          type: string
        sucursal:
          type: string
          nullable: true
        cbuOrigen:
          type: string
          nullable: true
        libradorNombre:
          type: string
        libradorCuit:
          type: string
          nullable: true
        monto:
          type: string
        moneda:
          type: string
        fechaEmision:
          type: string
          format: date-time
        fechaVencimiento:
          type: string
          format: date-time
        estado:
          type: string
          enum: [en_cartera, emitido, depositado, endosado, descontado, cobrado, rechazado, anulado]
        clienteId:
          type: integer
          nullable: true
        proveedorId:
          type: integer
          nullable: true
        observaciones:
          type: string
          nullable: true

    ChequeInput:
      type: object
      required:
        - tipo
        - modalidad
        - numero
        - banco
        - libradorNombre
        - monto
        - fechaEmision
        - fechaVencimiento
      properties:
        tipo:
          type: string
          enum: [recibido, emitido]
        modalidad:
          type: string
          enum: [fisico, echeq]
        numero:
          type: string
        banco:
          type: string
        sucursal:
          type: string
          nullable: true
        cbuOrigen:
          type: string
          nullable: true
        libradorNombre:
          type: string
        libradorCuit:
          type: string
          nullable: true
        monto:
          type: number
        moneda:
          type: string
        fechaEmision:
          type: string
          format: date
        fechaVencimiento:
          type: string
          format: date
        clienteId:
          type: integer
          nullable: true
        proveedorId:
          type: integer
          nullable: true
        observaciones:
          type: string
          nullable: true

    ChequeUpdateInput:
      type: object
      properties:
        banco:
          type: string
        sucursal:
          type: string
          nullable: true
        cbuOrigen:
          type: string
          nullable: true
        libradorNombre:
          type: string
        libradorCuit:
          type: string
          nullable: true
        fechaVencimiento:
          type: string
          format: date
        observaciones:
          type: string
          nullable: true

    ChequeTransicionInput:
      type: object
      properties:
        destino:
          type: string
          nullable: true
        nota:
          type: string
          nullable: true
        proveedorId:
          type: integer
          nullable: true
        monto:
          type: number
          nullable: true

    ChequeEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
        data:
          $ref: '#/components/schemas/Cheque'

    ChequeListEnvelope:
      type: object
      required: [success, data, total, limit, offset]
      properties:
        success:
          type: boolean
        data:
          type: array
          items:
            $ref: '#/components/schemas/Cheque'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    ChequeResumenEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
        data:
          type: object
          required: [enCartera, proximosVencer, rechazados]
          properties:
            enCartera:
              type: object
              properties:
                count:
                  type: integer
                total:
                  type: string
            proximosVencer:
              type: object
              properties:
                count:
                  type: integer
                total:
                  type: string
            rechazados:
              type: object
              properties:
                count:
                  type: integer
                total:
                  type: string

    VoidInput:
      type: object
      required: [motivo]
      properties:
        motivo:
          type: string
          minLength: 10
          maxLength: 500
          description: Reason for voiding the invoice (persisted on credit note and AuditEvent metadata).
    FacturaInput:
      type: object
      required: [fecha, tipo, numero, clienteId, neto1, neto2, neto3, iva1, iva2, total, items]
      properties:
        fecha:
          type: string
          format: date
        tipo:
          type: string
          enum: [A, B]
        prefijo:
          type: string
        numero:
          type: integer
        clienteId:
          type: integer
        formaPagoId:
          type: integer
          nullable: true
        neto1:
          type: number
        neto2:
          type: number
        neto3:
          type: number
        iva1:
          type: number
        iva2:
          type: number
        total:
          type: number
          description: Must equal netos + IVA + sum(percepciones) when percepciones are sent (#229).
        percepciones:
          type: array
          items:
            $ref: '#/components/schemas/ReciboPagoRetencionInput'
        items:
          type: array
          items:
            type: object
            required: [cantidad, precio, dscto, subtotal]
            description: |
              Catalog line requires articuloId. Ad-hoc service line omits articuloId and requires
              descripcion + condIva (#244).
            properties:
              articuloId:
                type: integer
                nullable: true
                minimum: 1
              descripcion:
                type: string
                minLength: 1
                maxLength: 120
              condIva:
                type: string
                enum: ['1', '2', '3']
              unidadServicio:
                type: string
                enum: [hora, dia, mes, proyecto, km, unidad, otro]
                nullable: true
              cantidad:
                type: number
              precio:
                type: number
              dscto:
                type: number
              subtotal:
                type: number

    FacturaListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Factura'
        - $ref: '#/components/schemas/ListPaginationMeta'

    FacturaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Factura'

    FacturaVoidBalanceCliente:
      type: object
      required: [id, rsocial, balance, creditLimit]
      properties:
        id:
          type: integer
        rsocial:
          type: string
        balance:
          description: Customer balance after decrement (Prisma Decimal may serialize as string in JSON)
          oneOf:
            - type: number
            - type: string
        creditLimit:
          type: number
          nullable: true

    NotaCreditoFacturaOrigen:
      type: object
      additionalProperties: true
      description: Originating invoice header (selected columns)
      properties:
        id:
          type: integer
        tipo:
          type: string
        prefijo:
          type: string
        numero:
          type: integer
        clienteId:
          type: integer
        fecha:
          type: string
          format: date-time
        total:
          oneOf:
            - type: number
            - type: string
        estado:
          type: string

    NotaCredito:
      type: object
      additionalProperties: true
      required: [id, tenantId, facturaOrigenId, motivo, monto, estadoCae, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        facturaOrigenId:
          type: integer
        motivo:
          type: string
        monto:
          oneOf:
            - type: number
            - type: string
        cae:
          type: string
          nullable: true
        caeVto:
          type: string
          format: date-time
          nullable: true
        estadoCae:
          type: string
          enum: [pending, issued, failed, not_required]
        createdById:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time

    NotaCreditoDetail:
      type: object
      additionalProperties: true
      required: [id, tenantId, facturaOrigenId, motivo, monto, estadoCae, createdAt, facturaOrigen]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        facturaOrigenId:
          type: integer
        motivo:
          type: string
        monto:
          oneOf:
            - type: number
            - type: string
        cae:
          type: string
          nullable: true
        caeVto:
          type: string
          format: date-time
          nullable: true
        estadoCae:
          type: string
          enum: [pending, issued, failed, not_required]
        createdById:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        facturaOrigen:
          $ref: '#/components/schemas/NotaCreditoFacturaOrigen'

    NotaCreditoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/NotaCreditoDetail'
        - $ref: '#/components/schemas/ListPaginationMeta'

    NotaCreditoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/NotaCreditoDetail'

    LibroIvaVentasAlicuotaTotal:
      type: object
      required: [alicuotaCode, neto, iva]
      properties:
        alicuotaCode:
          type: string
        neto:
          type: number
        iva:
          type: number

    LibroIvaVentasPreview:
      type: object
      required:
        - periodo
        - recordCountCbtv
        - recordCountAlicuotas
        - totalsByAlicuota
        - totalNeto
        - totalIva
        - totalExento
        - totalGeneral
        - arcaValidationPending
      properties:
        periodo:
          type: string
          pattern: '^\d{4}-\d{2}$'
        recordCountCbtv:
          type: integer
          minimum: 0
        recordCountAlicuotas:
          type: integer
          minimum: 0
        totalsByAlicuota:
          type: array
          items:
            $ref: '#/components/schemas/LibroIvaVentasAlicuotaTotal'
        totalNeto:
          type: number
        totalIva:
          type: number
        totalExento:
          type: number
        totalGeneral:
          type: number
        arcaValidationPending:
          type: boolean
          const: true

    LibroIvaVentasPreviewEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/LibroIvaVentasPreview'

    LibroIvaComprasPreview:
      type: object
      required:
        - periodo
        - recordCountCbtu
        - recordCountAlicuotas
        - totalsByAlicuota
        - totalNeto
        - totalIva
        - totalExento
        - totalGeneral
        - arcaValidationPending
      properties:
        periodo:
          type: string
          pattern: '^\d{4}-\d{2}$'
        recordCountCbtu:
          type: integer
          minimum: 0
        recordCountAlicuotas:
          type: integer
          minimum: 0
        totalsByAlicuota:
          type: array
          items:
            $ref: '#/components/schemas/LibroIvaVentasAlicuotaTotal'
        totalNeto:
          type: number
        totalIva:
          type: number
        totalExento:
          type: number
        totalGeneral:
          type: number
        arcaValidationPending:
          type: boolean
          const: true

    LibroIvaComprasPreviewEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/LibroIvaComprasPreview'

    ComprobanteCompraInput:
      type: object
      required:
        - fecha
        - tipo
        - prefijo
        - numero
        - proveedorId
        - neto1
        - neto2
        - neto3
        - iva1
        - iva2
        - total
      properties:
        fecha:
          type: string
          format: date-time
        tipo:
          type: string
          enum: [A, B, C]
        prefijo:
          type: string
          maxLength: 4
        numero:
          type: integer
          minimum: 1
        proveedorId:
          type: integer
          minimum: 1
        ordenCompraId:
          type: integer
          minimum: 1
        neto1:
          type: number
          minimum: 0
        neto2:
          type: number
          minimum: 0
        neto3:
          type: number
          minimum: 0
        iva1:
          type: number
          minimum: 0
        iva2:
          type: number
          minimum: 0
        total:
          type: number
          minimum: 0
        cae:
          type: string
          maxLength: 20
        caeVto:
          type: string
          format: date-time
        vencimiento:
          type: string
          format: date-time
          description: Explicit due date; when omitted, derived from supplier payment terms (#275).

    ComprobanteCompra:
      type: object
      required:
        - id
        - tenantId
        - proveedorId
        - fecha
        - tipo
        - prefijo
        - numero
        - neto1
        - neto2
        - neto3
        - iva1
        - iva2
        - total
        - estado
        - createdAt
        - updatedAt
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        proveedorId:
          type: integer
        ordenCompraId:
          type: integer
          nullable: true
        fecha:
          type: string
          format: date-time
        tipo:
          type: string
        prefijo:
          type: string
        numero:
          type: integer
        neto1:
          type: number
        neto2:
          type: number
        neto3:
          type: number
        iva1:
          type: number
        iva2:
          type: number
        total:
          type: number
        cae:
          type: string
          nullable: true
        caeVto:
          type: string
          format: date-time
          nullable: true
        estado:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ComprobanteCompraEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ComprobanteCompra'

    DocumentoCompraItemPreview:
      type: object
      required: [descripcion, cantidad, precioUnitario, subtotal]
      properties:
        descripcion:
          type: string
        cantidad:
          type: number
        precioUnitario:
          type: number
        subtotal:
          type: number
        articuloId:
          type: integer
          nullable: true
        confianza:
          type: number
          minimum: 0
          maximum: 1

    DocumentoCompraPreviewData:
      type: object
      properties:
        proveedorId:
          type: integer
          nullable: true
        cuitExtracted:
          type: string
          nullable: true
          description: Supplier tax id digits extracted when no match (#277 Fase F)
        rsocialExtracted:
          type: string
          nullable: true
          description: Supplier name hint from OCR/LLM (#277 Fase F)
        fecha:
          type: string
          format: date-time
          nullable: true
        vencimiento:
          type: string
          format: date-time
          nullable: true
        tipo:
          anyOf:
            - type: string
              enum: [A, B, C]
            - type: 'null'
        prefijo:
          type: string
          nullable: true
        numero:
          type: integer
          nullable: true
        neto1:
          type: number
        neto2:
          type: number
        neto3:
          type: number
        iva1:
          type: number
        iva2:
          type: number
        total:
          type: number
          nullable: true
        cae:
          type: string
          nullable: true
        caeVto:
          type: string
          format: date-time
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/DocumentoCompraItemPreview'
        fieldConfidence:
          type: object
          additionalProperties:
            type: number

    DocumentoCompraImportado:
      type: object
      required:
        - id
        - tenantId
        - usuarioId
        - archivoNombre
        - archivoMime
        - archivoPath
        - tipoArchivo
        - tier
        - confianza
        - estado
        - datosExtraidos
        - createdAt
        - updatedAt
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        usuarioId:
          type: integer
        archivoNombre:
          type: string
        archivoMime:
          type: string
        archivoPath:
          type: string
        tipoArchivo:
          type: string
        tier:
          type: integer
        confianza:
          type: number
        estado:
          type: string
          enum: [procesando, pendiente_revision, confirmado, descartado]
        datosExtraidos:
          $ref: '#/components/schemas/DocumentoCompraPreviewData'
        comprobanteCompraId:
          type: integer
          nullable: true
        errores:
          type: object
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DocumentoCompraImportadoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DocumentoCompraImportado'

    DocumentoCompraImportadoListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/DocumentoCompraImportado'

    DocumentoCompraColaEstado:
      type: object
      required:
        - procesando
        - pendiente_revision
        - confirmado
        - descartado
        - documentos
      properties:
        procesando:
          type: integer
          minimum: 0
        pendiente_revision:
          type: integer
          minimum: 0
        confirmado:
          type: integer
          minimum: 0
        descartado:
          type: integer
          minimum: 0
        documentos:
          type: array
          items:
            $ref: '#/components/schemas/DocumentoCompraImportado'

    DocumentoCompraColaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DocumentoCompraColaEstado'

    DocumentoCompraDuplicadoResult:
      type: object
      required: [duplicado, comprobanteCompraId]
      properties:
        duplicado:
          type: boolean
        comprobanteCompraId:
          type: ['integer', 'null']
          minimum: 1

    DocumentoCompraDuplicadoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DocumentoCompraDuplicadoResult'

    RegimenRetencion:
      type: object
      required:
        - id
        - tenantId
        - tipo
        - subtipo
        - nombre
        - alicuota
        - alicuotaMin
        - provincia
        - activo
        - createdAt
        - updatedAt
      properties:
        id:
          type: integer
          minimum: 1
        tenantId:
          type: integer
          minimum: 1
        tipo:
          type: string
          enum: [ganancias, iva, iibb]
        subtipo:
          type: string
          enum: [retencion, percepcion]
        nombre:
          type: string
        alicuota:
          type: string
        alicuotaMin:
          type: ['string', 'null']
        provincia:
          type: ['string', 'null']
        activo:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RegimenRetencionInput:
      type: object
      required: [tipo, subtipo, nombre, alicuota]
      properties:
        tipo:
          type: string
          enum: [ganancias, iva, iibb]
        subtipo:
          type: string
          enum: [retencion, percepcion]
        nombre:
          type: string
          minLength: 1
          maxLength: 80
        alicuota:
          type: number
          minimum: 0
          maximum: 100
        alicuotaMin:
          type: ['number', 'null']
          minimum: 0
        provincia:
          type: ['string', 'null']
          maxLength: 10
        activo:
          type: boolean

    RegimenRetencionUpdateInput:
      type: object
      properties:
        nombre:
          type: string
          minLength: 1
          maxLength: 80
        alicuota:
          type: number
          minimum: 0
          maximum: 100
        alicuotaMin:
          type: ['number', 'null']
          minimum: 0
        provincia:
          type: ['string', 'null']
          maxLength: 10
        activo:
          type: boolean

    RegimenRetencionEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/RegimenRetencion'

    RegimenRetencionListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/RegimenRetencion'

    FiscalRetencionesConfig:
      type: object
      required:
        - esAgenteRetencionGanancias
        - esAgenteRetencionIVA
        - esAgenteRetencionIIBB
      properties:
        esAgenteRetencionGanancias:
          type: boolean
        esAgenteRetencionIVA:
          type: boolean
        esAgenteRetencionIIBB:
          type: boolean

    FiscalRetencionesConfigInput:
      type: object
      properties:
        esAgenteRetencionGanancias:
          type: boolean
        esAgenteRetencionIVA:
          type: boolean
        esAgenteRetencionIIBB:
          type: boolean

    FiscalRetencionesConfigEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/FiscalRetencionesConfig'

    PresentacionRetencionInput:
      type: object
      required: [formato, periodo]
      properties:
        formato:
          type: string
          enum: [sicore, sifere]
        periodo:
          type: string
          pattern: '^\\d{4}-\\d{2}$'

    PresentacionWarning:
      type: object
      required: [code, retencionId, message]
      properties:
        code:
          type: string
          enum: [missing_cuit, invalid_cuit, zero_importe_excluded]
        retencionId:
          type: integer
          minimum: 1
        message:
          type: string

    PresentacionFila:
      type: object
      required:
        - retencionId
        - fecha
        - cuit
        - denominacion
        - regimenNombre
        - regimenTipo
        - operacionTipo
        - baseImponible
        - alicuota
        - importe
        - incluida
      properties:
        retencionId:
          type: integer
          minimum: 1
        fecha:
          type: string
          format: date-time
        cuit:
          type: string
        denominacion:
          type: string
        regimenNombre:
          type: string
        regimenTipo:
          type: string
        operacionTipo:
          type: string
          enum: [retencion, percepcion]
        provincia:
          type: ['string', 'null']
        baseImponible:
          type: string
        alicuota:
          type: string
        importe:
          type: string
        incluida:
          type: boolean

    PresentacionTotalRegimen:
      type: object
      required: [regimenNombre, operaciones, totalImporte]
      properties:
        regimenNombre:
          type: string
        operaciones:
          type: integer
          minimum: 0
        totalImporte:
          type: string

    PresentacionPreview:
      type: object
      required: [formato, periodo, filas, totalesPorRegimen, warnings, canGenerate]
      properties:
        formato:
          type: string
          enum: [sicore, sifere]
        periodo:
          type: string
        filas:
          type: array
          items:
            $ref: '#/components/schemas/PresentacionFila'
        totalesPorRegimen:
          type: array
          items:
            $ref: '#/components/schemas/PresentacionTotalRegimen'
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/PresentacionWarning'
        canGenerate:
          type: boolean

    PresentacionPreviewEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/PresentacionPreview'

    PresentacionRetencion:
      type: object
      required:
        - id
        - formato
        - periodo
        - totalOperaciones
        - totalImporte
        - createdAt
      properties:
        id:
          type: integer
          minimum: 1
        formato:
          type: string
          enum: [sicore, sifere]
        periodo:
          type: string
        totalOperaciones:
          type: integer
          minimum: 0
        totalImporte:
          type: string
        archivoHash:
          type: ['string', 'null']
        presentadoAt:
          type: ['string', 'null']
          format: date-time
        createdAt:
          type: string
          format: date-time

    PresentacionRetencionEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/PresentacionRetencion'

    PresentacionRetencionListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/PresentacionRetencion'

    RetencionAplicada:
      type: object
      required:
        - id
        - regimenId
        - regimenNombre
        - tipo
        - entidadTipo
        - entidadId
        - baseImponible
        - alicuota
        - importe
        - createdAt
      properties:
        id:
          type: integer
          minimum: 1
        regimenId:
          type: integer
          minimum: 1
        regimenNombre:
          type: string
        tipo:
          type: string
        entidadTipo:
          type: string
          enum: [cliente, proveedor]
        entidadId:
          type: integer
          minimum: 1
        facturaId:
          type: ['integer', 'null']
          minimum: 1
        cobroId:
          type: ['integer', 'null']
          minimum: 1
        reciboPagoId:
          type: ['integer', 'null']
          minimum: 1
        baseImponible:
          type: string
        alicuota:
          type: string
        importe:
          type: string
        constanciaNum:
          type: ['string', 'null']
        createdAt:
          type: string
          format: date-time

    RetencionAplicadaListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/RetencionAplicada'
        - $ref: '#/components/schemas/ListPaginationMeta'

    RetencionesPreviewResult:
      type: object
      required: [retenciones]
      properties:
        retenciones:
          type: array
          items:
            type: object
            required: [regimenId, nombre, tipo, alicuota, baseImponible, importe]
            properties:
              regimenId:
                type: integer
              nombre:
                type: string
              tipo:
                type: string
              alicuota:
                type: string
              baseImponible:
                type: string
              importe:
                type: string

    RetencionesPreviewEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/RetencionesPreviewResult'

    DocumentoCompraTemplateSummary:
      type: object
      required: [issuer, keywords, source]
      properties:
        issuer:
          type: string
        keywords:
          type: array
          items:
            type: string
        source:
          type: string
          enum: [bundled, custom]

    DocumentoCompraTemplateListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/DocumentoCompraTemplateSummary'

    DocumentoCompraTemplateInput:
      type: object
      required: [content]
      properties:
        content:
          type: string
          minLength: 20

    DocumentoCompraTemplate:
      type: object
      required: [issuer, keywords, fields]
      properties:
        issuer:
          type: string
        keywords:
          type: array
          items:
            type: string
        fields:
          type: object
          additionalProperties: true

    DocumentoCompraTemplateEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DocumentoCompraTemplate'

    DocumentoCompraConfirmInput:
      allOf:
        - $ref: '#/components/schemas/ComprobanteCompraInput'
        - type: object
          required: [documentoId]
          properties:
            documentoId:
              type: integer
              minimum: 1
            items:
              type: array
              items:
                $ref: '#/components/schemas/DocumentoCompraItemPreview'

    DocumentoCompraConfirmResult:
      type: object
      required: [documento, comprobanteCompra]
      properties:
        documento:
          $ref: '#/components/schemas/DocumentoCompraImportado'
        comprobanteCompra:
          $ref: '#/components/schemas/ComprobanteCompra'

    DocumentoCompraConfirmEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DocumentoCompraConfirmResult'

    FacturaVoidResult:
      type: object
      required: [factura, notaCredito, updatedCliente]
      properties:
        factura:
          $ref: '#/components/schemas/Factura'
        notaCredito:
          $ref: '#/components/schemas/NotaCredito'
        updatedCliente:
          $ref: '#/components/schemas/FacturaVoidBalanceCliente'

    FacturaVoidEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/FacturaVoidResult'

    FacturaPrintInput:
      type: object
      required: [device]
      properties:
        device:
          type: string
          enum: [pdf, fiscal, thermal]

    FacturaPrintResult:
      type: object
      required: [device, channel, fallbackToPdf]
      properties:
        device:
          type: string
          enum: [pdf, fiscal, thermal]
        channel:
          type: string
          enum: [pdf, fiscal_mock, thermal_mock]
        fallbackToPdf:
          type: boolean
        downloadPath:
          type: string
        jobId:
          type: string
        transport:
          type: string
          enum: [mock-serial]

    FacturaPrintEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/FacturaPrintResult'

    PrintingStatus:
      type: object
      required: [fiscalPrinterEnabled, thermalPrinterEnabled, fiscalMode, thermalMode]
      properties:
        fiscalPrinterEnabled:
          type: boolean
          description: Opt-in fiscal controller (RS-232/USB); false uses PDF fallback.
        thermalPrinterEnabled:
          type: boolean
          description: Opt-in 80mm thermal ESC/POS path; false uses PDF fallback.
        fiscalMode:
          type: string
          enum: [mock]
        thermalMode:
          type: string
          enum: [mock]

    PrintingStatusEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/PrintingStatus'

    PrintingTestInput:
      type: object
      required: [device]
      properties:
        device:
          type: string
          enum: [fiscal, thermal]

    PrintingTestResult:
      type: object
      required: [device, channel, fallbackToPdf]
      properties:
        device:
          type: string
          enum: [fiscal, thermal]
        channel:
          type: string
          enum: [pdf, fiscal_mock, thermal_mock]
        fallbackToPdf:
          type: boolean
        jobId:
          type: string
        transport:
          type: string
          enum: [mock-serial]

    PrintingTestEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/PrintingTestResult'

    ArcaConfigInput:
      type: object
      required: [cuit, certificate, privateKey]
      properties:
        cuit: { type: string }
        certificate: { type: string }
        privateKey: { type: string }
        ambiente:
          type: string
          enum: [homologacion, produccion]

    ArcaConfigStatus:
      type: object
      required: [configured]
      properties:
        configured: { type: boolean }
        cuit: { type: string }
        ambiente: { type: string }

    ArcaConfigStatusEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/ArcaConfigStatus'

    ArcaConfigEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: object
          properties:
            configured: { type: boolean }

    MercadoPagoConfigStatus:
      type: object
      required: [configured]
      properties:
        configured: { type: boolean }
        publicKey: { type: string }
        sandboxMode: { type: boolean }
        activo: { type: boolean }
        accessTokenLast4: { type: string }
        webhookSecretSet: { type: boolean }
        collectorId: { type: string }
        externalPosId: { type: string }
        staticQrConfigured: { type: boolean }

    MercadoPagoConfigInput:
      type: object
      required: [publicKey]
      properties:
        accessToken: { type: string }
        publicKey: { type: string }
        webhookSecret: { type: string }
        sandboxMode: { type: boolean }
        activo: { type: boolean }
        collectorId: { type: string }
        externalPosId: { type: string }
        staticQrData: { type: string }

    MercadoPagoConfigStatusEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoConfigStatus'

    MercadoPagoConfigEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: object
          properties:
            configured: { type: boolean }

    MercadoPagoTestEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: object
          required: [accountName]
          properties:
            accountName: { type: string }
            email: { type: string }

    MercadoPagoFacturaEstado:
      type: string
      enum: [none, pending, approved, rejected, cancelled, expired, refunded]

    MercadoPagoFacturaPayment:
      type: object
      required: [estado]
      properties:
        estado:
          $ref: '#/components/schemas/MercadoPagoFacturaEstado'
        channel:
          type: string
          enum: [none, link, qr]
        preferenceId:
          type: string
        paymentLink:
          type: string
          format: uri
        expiresAt:
          type: string
          format: date-time
        pagadoAt:
          type: string
          format: date-time
        amount:
          type: string
        facturaRef:
          type: string
        qrData:
          type: string
        qrImageBase64:
          type: string
        qrExpiresAt:
          type: string
          format: date-time
        qrOrderId:
          type: string

    MercadoPagoStaticQr:
      type: object
      required: [qrData, qrImageBase64]
      properties:
        qrData: { type: string }
        qrImageBase64: { type: string }

    MercadoPagoStaticQrEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoStaticQr'

    MercadoPagoReconciliationEntry:
      type: object
      required:
        - mpPaymentId
        - transactionAmount
        - currencyId
        - paymentDate
        - payerName
        - payerEmail
        - payerIdentification
        - preferenceId
        - externalReference
        - createdAt
      properties:
        mpPaymentId: { type: string }
        transactionAmount: { type: string }
        currencyId: { type: string }
        paymentDate: { type: string, format: date-time }
        payerName: { type: string, nullable: true }
        payerEmail: { type: string, nullable: true }
        payerIdentification: { type: string, nullable: true }
        preferenceId: { type: string, nullable: true }
        externalReference: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    MercadoPagoReconciliationListEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: array
          items:
            $ref: '#/components/schemas/MercadoPagoReconciliationEntry'

    MercadoPagoReconciliationEntryEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoReconciliationEntry'

    MercadoPagoReconcileBody:
      type: object
      required: [mpPaymentId, facturaId]
      properties:
        mpPaymentId: { type: string, minLength: 1, maxLength: 60 }
        facturaId: { type: integer, minimum: 1 }

    MercadoPagoIgnoreBody:
      type: object
      required: [mpPaymentId]
      properties:
        mpPaymentId: { type: string, minLength: 1, maxLength: 60 }

    MercadoPagoIgnoreResult:
      type: object
      required: [mpPaymentId]
      properties:
        mpPaymentId: { type: string }

    MercadoPagoIgnoreEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoIgnoreResult'

    MercadoPagoReconciliationJobSummary:
      type: object
      required: [processed, autoReconciled, queued, skipped]
      properties:
        processed: { type: integer, minimum: 0 }
        autoReconciled: { type: integer, minimum: 0 }
        queued: { type: integer, minimum: 0 }
        skipped: { type: integer, minimum: 0 }

    MercadoPagoReconciliationJobEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoReconciliationJobSummary'

    MercadoPagoRefundInput:
      type: object
      required: [motivo]
      properties:
        motivo:
          type: string
          minLength: 10
          maxLength: 500
        monto:
          type: number
          exclusiveMinimum: 0
          description: Optional partial amount; defaults to remaining refundable balance.

    MercadoPagoRefundStatus:
      type: object
      required: [originalPaymentAmount, refundableBalance, refunds]
      properties:
        originalPaymentAmount: { type: string }
        refundableBalance: { type: string }
        refunds:
          type: array
          items:
            $ref: '#/components/schemas/MercadoPagoRefund'

    MercadoPagoRefundStatusEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoRefundStatus'

    MercadoPagoRefund:
      type: object
      required: [id, facturaId, mpPaymentId, monto, motivo, estado, createdAt, updatedAt]
      properties:
        id: { type: integer, minimum: 1 }
        facturaId: { type: integer, minimum: 1 }
        mpPaymentId: { type: string }
        mpRefundId: { type: string, nullable: true }
        monto: { type: string }
        motivo: { type: string }
        estado:
          type: string
          enum: [iniciado, procesando, completado, fallido]
        notaCreditoId: { type: integer, minimum: 1, nullable: true }
        reciboCobroId: { type: integer, minimum: 1, nullable: true }
        errorMessage: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    MercadoPagoRefundEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoRefund'

    MercadoPagoRefundNullableEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          oneOf:
            - $ref: '#/components/schemas/MercadoPagoRefund'
            - type: 'null'

    MercadoPagoChargeback:
      type: object
      required: [id, mpChargebackId, estado, createdAt]
      properties:
        id: { type: integer, minimum: 1 }
        mpChargebackId: { type: string }
        mpPaymentId: { type: string, nullable: true }
        facturaId: { type: integer, minimum: 1, nullable: true }
        estado:
          type: string
          enum: [pendiente, resuelto, ignorado]
        notifiedAt: { type: string, format: date-time, nullable: true }
        resolvedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }

    MercadoPagoChargebackListEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: array
          items:
            $ref: '#/components/schemas/MercadoPagoChargeback'

    MercadoPagoChargebackEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoChargeback'

    MercadoPagoChargebackPatchInput:
      type: object
      required: [estado]
      properties:
        estado:
          type: string
          enum: [resuelto, ignorado]

    MercadoPagoFacturaPaymentEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          $ref: '#/components/schemas/MercadoPagoFacturaPayment'

    MercadoPagoWebhookBody:
      type: object
      properties:
        action: { type: string }
        type: { type: string }
        data:
          type: object
          properties:
            id:
              oneOf:
                - type: string
                - type: integer
        id:
          oneOf:
            - type: string
            - type: integer
        live_mode: { type: boolean }
        user_id:
          oneOf:
            - type: string
            - type: integer

    MercadoPagoWebhookAckEnvelope:
      type: object
      required: [success]
      properties:
        success: { type: boolean, const: true }

    ArcaTaEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: object
          properties:
            token: { type: string }
            sign: { type: string }
            expiration: { type: string, format: date-time }

    ArcaCaeInput:
      type: object
      required: [facturaId]
      properties:
        facturaId: { type: integer, minimum: 1 }

    ArcaCaeEnvelope:
      type: object
      required: [success, data]
      properties:
        success: { type: boolean, const: true }
        data:
          type: object
          properties:
            cae: { type: string }
            caeVto: { type: string, format: date-time }

    PedidoItem:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        articuloId:
          type: integer
        cantidad:
          type: integer
        precio:
          type: number
        dscto:
          type: number
        subtotal:
          type: number

    Pedido:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        clienteId:
          type: integer
        vendedorId:
          type: integer
          nullable: true
        estado:
          type: string
          enum: [draft, confirmed, invoiced, cancelled]
        total:
          type: number
        validUntil:
          type: string
          format: date-time
          nullable: true
        facturaId:
          type: integer
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/PedidoItem'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PedidoInput:
      type: object
      required: [clienteId, items]
      properties:
        clienteId:
          type: integer
          minimum: 1
        vendedorId:
          type: integer
          nullable: true
        validUntil:
          type: string
          nullable: true
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [cantidad, precio]
            description: |
              Catalog line requires articuloId. Ad-hoc service line omits articuloId and requires
              descripcion + condIva (#244).
            properties:
              articuloId:
                type: integer
                nullable: true
                minimum: 1
              descripcion:
                type: string
                minLength: 1
                maxLength: 120
              condIva:
                type: string
                enum: ['1', '2', '3']
              unidadServicio:
                type: string
                enum: [hora, dia, mes, proyecto, km, unidad, otro]
                nullable: true
              cantidad:
                type: integer
                minimum: 1
              precio:
                type: number
                minimum: 0
              dscto:
                type: number
                minimum: 0
                maximum: 100

    PedidoInvoiceInput:
      type: object
      required: [fecha, tipo, numero]
      properties:
        fecha:
          type: string
        tipo:
          type: string
          enum: [A, B]
        numero:
          type: integer
          minimum: 1
        prefijo:
          type: string
        formaPagoId:
          type: integer
          nullable: true

    PedidoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Pedido'

    PedidoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Pedido'
        - $ref: '#/components/schemas/ListPaginationMeta'

    ContratoFrecuencia:
      type: string
      enum: [mensual, bimestral, trimestral, semestral, anual]

    ContratoEstado:
      type: string
      enum: [activo, pausado, finalizado, cancelado]

    ContratoItem:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        articuloId:
          type: integer
          nullable: true
        descripcion:
          type: string
        condIva:
          type: string
          enum: ['1', '2', '3']
        unidadServicio:
          type: string
          nullable: true
        cantidad:
          type: integer
          minimum: 1
        precioUnit:
          type: number
        dscto:
          type: number

    Contrato:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        numero:
          type: integer
        clienteId:
          type: integer
        nombre:
          type: string
        descripcion:
          type: string
          nullable: true
        estado:
          $ref: '#/components/schemas/ContratoEstado'
        frecuencia:
          $ref: '#/components/schemas/ContratoFrecuencia'
        diaDelMes:
          type: integer
          minimum: 1
          maximum: 31
        fechaInicio:
          type: string
          format: date-time
        fechaFin:
          type: string
          format: date-time
          nullable: true
        proximaFact:
          type: string
          format: date-time
        montoBase:
          type: number
        moneda:
          type: string
        modoEmision:
          type: string
          enum: [auto, revision]
        tipoFactura:
          type: string
          enum: [A, B]
        prefijo:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/ContratoItem'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ContratoInput:
      type: object
      required: [clienteId, nombre, frecuencia, diaDelMes, fechaInicio, items]
      properties:
        clienteId:
          type: integer
          minimum: 1
        nombre:
          type: string
          minLength: 1
          maxLength: 120
        descripcion:
          type: string
          nullable: true
          maxLength: 500
        frecuencia:
          $ref: '#/components/schemas/ContratoFrecuencia'
        diaDelMes:
          type: integer
          minimum: 1
          maximum: 31
        fechaInicio:
          type: string
        fechaFin:
          type: string
          nullable: true
        proximaFact:
          type: string
        moneda:
          type: string
          default: ARS
        incluyeIVA:
          type: boolean
        ivaAlicuota:
          type: number
        modoEmision:
          type: string
          enum: [auto, revision]
        tipoFactura:
          type: string
          enum: [A, B]
        prefijo:
          type: string
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [descripcion, cantidad, precioUnit]
            properties:
              articuloId:
                type: integer
                nullable: true
                minimum: 1
              descripcion:
                type: string
                minLength: 1
                maxLength: 120
              condIva:
                type: string
                enum: ['1', '2', '3']
              unidadServicio:
                type: string
                nullable: true
              cantidad:
                type: integer
                minimum: 1
              precioUnit:
                type: number
                minimum: 0
              dscto:
                type: number
                minimum: 0
                maximum: 100
        ajuste:
          type: object
          nullable: true
          required: [tipo, frecuenciaAjuste, proximoAjuste]
          properties:
            tipo:
              type: string
              enum: [porcentaje_fijo, manual]
            porcentaje:
              type: number
              nullable: true
            frecuenciaAjuste:
              $ref: '#/components/schemas/ContratoFrecuencia'
            proximoAjuste:
              type: string

    ContratoUpdateInput:
      allOf:
        - $ref: '#/components/schemas/ContratoInput'
        - type: object
          properties:
            estado:
              $ref: '#/components/schemas/ContratoEstado'

    ContratoAjusteManualInput:
      type: object
      required: [porcentaje]
      properties:
        porcentaje:
          type: number
          description: Non-zero percentage to apply to all line unit prices

    ContratoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Contrato'

    ContratoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Contrato'
        - $ref: '#/components/schemas/ListPaginationMeta'

    ContratoFacturasEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            type: object
            additionalProperties: true
            properties:
              id:
                type: integer
              fecha:
                type: string
                format: date-time
              tipo:
                type: string
              prefijo:
                type: string
              numero:
                type: integer
              total:
                type: number
              estadoCae:
                type: string

    OrdenTrabajoEstado:
      type: string
      enum:
        - recibido
        - diagnosticado
        - presupuestado
        - aprobado
        - en_reparacion
        - listo
        - entregado
        - facturado
        - cancelado
        - sin_reparacion

    OrdenTrabajoPrioridad:
      type: string
      enum: [baja, normal, alta, urgente]

    OrdenTrabajoItemTipo:
      type: string
      enum: [mano_de_obra, repuesto, servicio]

    OrdenTrabajoItem:
      type: object
      required: [tipo, descripcion, cantidad, precioUnit]
      properties:
        tipo:
          $ref: '#/components/schemas/OrdenTrabajoItemTipo'
        descripcion:
          type: string
          minLength: 1
          maxLength: 120
        articuloId:
          type: integer
          nullable: true
          minimum: 1
        cantidad:
          type: number
          exclusiveMinimum: 0
        precioUnit:
          type: number
          minimum: 0
        condIva:
          type: string
          enum: ['1', '2', '3']

    OrdenTrabajo:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        numero:
          type: integer
        clienteId:
          type: integer
        tecnicoId:
          type: integer
          nullable: true
        estado:
          $ref: '#/components/schemas/OrdenTrabajoEstado'
        prioridad:
          $ref: '#/components/schemas/OrdenTrabajoPrioridad'
        equipoDescripcion:
          type: string
        sintomaReportado:
          type: string
        enGarantia:
          type: boolean
        presupuesto:
          type: number
          nullable: true
        facturaId:
          type: integer
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrdenTrabajoItem'

    OrdenTrabajoInput:
      type: object
      required: [clienteId, equipoDescripcion, sintomaReportado]
      properties:
        clienteId:
          type: integer
          minimum: 1
        tecnicoId:
          type: integer
          nullable: true
        prioridad:
          $ref: '#/components/schemas/OrdenTrabajoPrioridad'
        equipoMarca:
          type: string
          nullable: true
        equipoModelo:
          type: string
          nullable: true
        equipoNroSerie:
          type: string
          nullable: true
        equipoDescripcion:
          type: string
          minLength: 1
          maxLength: 200
        sintomaReportado:
          type: string
          minLength: 1
          maxLength: 500
        diagnostico:
          type: string
          nullable: true
        trabajoRealizado:
          type: string
          nullable: true
        fechaPromesa:
          type: string
          nullable: true
        observaciones:
          type: string
          nullable: true
        enGarantia:
          type: boolean
        garantiaVence:
          type: string
          nullable: true
        otGarantiaId:
          type: integer
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrdenTrabajoItem'

    OrdenTrabajoUpdateInput:
      allOf:
        - $ref: '#/components/schemas/OrdenTrabajoInput'
        - type: object
          properties:
            estado:
              $ref: '#/components/schemas/OrdenTrabajoEstado'
            fechaEntrega:
              type: string
              nullable: true
            presupuesto:
              type: number
              nullable: true

    OrdenTrabajoTransitionInput:
      type: object
      required: [estado]
      properties:
        estado:
          $ref: '#/components/schemas/OrdenTrabajoEstado'
        diagnostico:
          type: string
          nullable: true
        trabajoRealizado:
          type: string
          nullable: true
        fechaPromesa:
          type: string
          nullable: true
        fechaEntrega:
          type: string
          nullable: true
        tecnicoId:
          type: integer
          nullable: true
        observaciones:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrdenTrabajoItem'

    OrdenTrabajoFacturarInput:
      type: object
      properties:
        tipo:
          type: string
          enum: [A, B]
        prefijo:
          type: string
        skipArcaCae:
          type: boolean
          default: true

    OrdenTrabajoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/OrdenTrabajo'

    OrdenTrabajoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/OrdenTrabajo'
            counts:
              type: object
              additionalProperties:
                type: integer
        - $ref: '#/components/schemas/ListPaginationMeta'

    OrdenTrabajoFacturarEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [orden, facturaId]
          properties:
            orden:
              $ref: '#/components/schemas/OrdenTrabajo'
            facturaId:
              type: integer

    GarantiaEstado:
      type: string
      enum: [vigente, vencida, anulada]

    GarantiaUso:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        garantiaId:
          type: integer
        otId:
          type: integer
          nullable: true
        descripcion:
          type: string
        fecha:
          type: string
          format: date-time
        userId:
          type: integer

    Garantia:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        articuloId:
          type: integer
        facturaId:
          type: integer
          nullable: true
        facturaItemId:
          type: integer
          nullable: true
        nroSerie:
          type: string
          nullable: true
        nroImei:
          type: string
          nullable: true
        descripcionEquipo:
          type: string
          nullable: true
        clienteId:
          type: integer
        fechaVenta:
          type: string
          format: date-time
        mesesGarantia:
          type: integer
        fechaVencimiento:
          type: string
          format: date-time
        estado:
          $ref: '#/components/schemas/GarantiaEstado'
        usos:
          type: array
          items:
            $ref: '#/components/schemas/GarantiaUso'

    GarantiaRegisterInput:
      type: object
      required: [articuloId, clienteId]
      properties:
        articuloId:
          type: integer
          minimum: 1
        clienteId:
          type: integer
          minimum: 1
        facturaId:
          type: integer
          nullable: true
        facturaItemId:
          type: integer
          nullable: true
        nroSerie:
          type: string
          nullable: true
        nroImei:
          type: string
          nullable: true
        descripcionEquipo:
          type: string
          nullable: true
        fechaVenta:
          type: string
          nullable: true
        mesesGarantia:
          type: integer
          minimum: 1
          nullable: true

    GarantiaUsoInput:
      type: object
      required: [descripcion]
      properties:
        otId:
          type: integer
          nullable: true
          minimum: 1
        descripcion:
          type: string
          minLength: 1
          maxLength: 500

    GarantiaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Garantia'

    GarantiaListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/Garantia'
            counts:
              type: object
              additionalProperties:
                type: integer
        - $ref: '#/components/schemas/ListPaginationMeta'

    GarantiaLookupEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [status]
          properties:
            status:
              type: string
              enum: [vigente, vencida, sin_registro]
            garantia:
              $ref: '#/components/schemas/Garantia'

    FormaPago:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        codigo:
          type: integer
        descripcion:
          type: string
        vto_dias:
          type: integer
        esEfectivo:
          type: boolean
          description: When true, posts MovimientoCaja on open cash shift (#247).

    FormaPagoPatchInput:
      type: object
      required: [esEfectivo]
      properties:
        esEfectivo:
          type: boolean

    FormaPagoListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/FormaPago'

    FormaPagoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/FormaPago'

    Caja:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        nombre:
          type: string
        activa:
          type: boolean

    CajaListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/Caja'

    CajaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Caja'

    TurnoCaja:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        cajaId:
          type: integer
        cajeroId:
          type: integer
        estado:
          type: string
          enum: [abierto, cerrado]
        montoApertura:
          type: number
        diferencia:
          type: number
          nullable: true

    TurnoCajaOpenInput:
      type: object
      required: [cajaId, montoApertura]
      properties:
        cajaId:
          type: integer
          minimum: 1
        montoApertura:
          type: number
          minimum: 0

    TurnoCajaCloseInput:
      type: object
      required: [conteo]
      properties:
        conteo:
          type: object
          additionalProperties: true
        observaciones:
          type: string
          nullable: true

    MovimientoCajaManualInput:
      type: object
      required: [tipo, importe]
      properties:
        tipo:
          type: string
          enum: [egreso, ingreso_extra]
        importe:
          type: number
          exclusiveMinimum: 0
        concepto:
          type: string
          nullable: true
        formaPago:
          type: string
          enum: [efectivo, tarjeta, mp, transferencia, otro]

    TurnoCajaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TurnoCaja'

    TurnoCajaListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/TurnoCaja'
            counts:
              type: object
              additionalProperties:
                type: number
        - $ref: '#/components/schemas/ListPaginationMeta'

    PrecioEscalonado:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        listaPrecioItemId:
          type: integer
        cantidadDesde:
          type: number
        cantidadHasta:
          type: number
          nullable: true
        precio:
          type: number

    ListaPrecioItem:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        listaPrecioId:
          type: integer
        articuloId:
          type: integer
        tipoPrecio:
          type: string
          enum: [fijo, porcentaje_sobre_base]
        precio:
          type: number
          nullable: true
        porcentaje:
          type: number
          nullable: true
        escalonados:
          type: array
          items:
            $ref: '#/components/schemas/PrecioEscalonado'

    ListaPrecio:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        nombre:
          type: string
        moneda:
          type: string
        activa:
          type: boolean
        esDefault:
          type: boolean
        vigenciaHasta:
          type: string
          format: date-time
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/ListaPrecioItem'

    ListaPrecioCreateInput:
      type: object
      required: [nombre]
      properties:
        nombre:
          type: string
          minLength: 1
          maxLength: 80
        moneda:
          type: string
          minLength: 3
          maxLength: 3
        activa:
          type: boolean
        esDefault:
          type: boolean
        vigenciaHasta:
          type: string
          format: date-time
          nullable: true

    ListaPrecioPatchInput:
      type: object
      minProperties: 1
      properties:
        nombre:
          type: string
          minLength: 1
          maxLength: 80
        moneda:
          type: string
          minLength: 3
          maxLength: 3
        activa:
          type: boolean
        esDefault:
          type: boolean
        vigenciaHasta:
          type: string
          format: date-time
          nullable: true

    ListaPrecioItemInput:
      type: object
      required: [articuloId, tipoPrecio]
      properties:
        articuloId:
          type: integer
          minimum: 1
        tipoPrecio:
          type: string
          enum: [fijo, porcentaje_sobre_base]
        precio:
          type: number
          minimum: 0
          nullable: true
        porcentaje:
          type: number
          nullable: true
        escalonados:
          type: array
          items:
            type: object
            required: [cantidadDesde, precio]
            properties:
              cantidadDesde:
                type: number
                minimum: 0
              cantidadHasta:
                type: number
                exclusiveMinimum: 0
                nullable: true
              precio:
                type: number
                minimum: 0

    ListaPrecioBulkUpdateInput:
      type: object
      required: [porcentaje]
      properties:
        porcentaje:
          type: number
        preview:
          type: boolean

    ListaPrecioBulkUpdateResult:
      type: object
      required: [success, preview, afectados, ejemplos]
      properties:
        success:
          type: boolean
          const: true
        preview:
          type: boolean
        afectados:
          type: integer
        ejemplos:
          type: array
          items:
            type: object
            properties:
              listaPrecioItemId:
                type: integer
              articuloId:
                type: integer
              descripcion:
                type: string
              precioActual:
                type: number
                nullable: true
              precioNuevo:
                type: number
                nullable: true

    PrecioEfectivoResponse:
      type: object
      required: [success, articuloId, cantidad, precioBase, precio, origen, moneda]
      properties:
        success:
          type: boolean
          const: true
        articuloId:
          type: integer
        listaPrecioId:
          type: integer
          nullable: true
        cantidad:
          type: number
        precioBase:
          type: number
        precio:
          type: number
        origen:
          type: string
          enum:
            [
              escalonado,
              fijo,
              porcentaje_sobre_base,
              base,
              oferta,
              override_variante,
              precio_subfamilia,
              precio_familia,
            ]
        moneda:
          type: string

    ListaPrecioEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ListaPrecio'

    ListaPrecioItemEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ListaPrecioItem'

    ListaPrecioListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/ListaPrecio'
        - $ref: '#/components/schemas/ListPaginationMeta'

    CategoriaArticulo:
      type: object
      required: [id, tenantId, nombre, padreId, precioDefault, activo, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        nombre:
          type: string
        codigo:
          type: string
          nullable: true
        padreId:
          type: integer
          nullable: true
        precioDefault:
          type: number
          nullable: true
        activo:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Deposito:
      type: object
      required: [id, tenantId, nombre, codigo, tipo, direccion, responsableId, activo, esDefault, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        nombre:
          type: string
        codigo:
          type: string
        tipo:
          type: string
          enum: [central, sucursal, externo, picking, transito]
        direccion:
          type: string
          nullable: true
        responsableId:
          type: integer
          nullable: true
        activo:
          type: boolean
        esDefault:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DepositoCreateInput:
      type: object
      required: [nombre, codigo, tipo]
      properties:
        nombre:
          type: string
          maxLength: 80
        codigo:
          type: string
          maxLength: 20
        tipo:
          type: string
          enum: [central, sucursal, externo, picking, transito]
        direccion:
          type: string
          nullable: true
          maxLength: 200
        responsableId:
          type: integer
          nullable: true
          minimum: 1
        activo:
          type: boolean
        esDefault:
          type: boolean

    DepositoPatchInput:
      type: object
      properties:
        nombre:
          type: string
          maxLength: 80
        codigo:
          type: string
          maxLength: 20
        tipo:
          type: string
          enum: [central, sucursal, externo, picking, transito]
        direccion:
          type: string
          nullable: true
        responsableId:
          type: integer
          nullable: true
        activo:
          type: boolean
        esDefault:
          type: boolean

    DepositoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/Deposito'

    DepositoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              enum: [true]
            data:
              type: array
              items:
                $ref: '#/components/schemas/Deposito'
        - $ref: '#/components/schemas/ListPaginationMeta'

    StockDeposito:
      type: object
      required: [id, tenantId, articuloId, depositoId, cantidad, stockMin, stockMax, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        articuloId:
          type: integer
        depositoId:
          type: integer
        depositoCodigo:
          type: string
        depositoNombre:
          type: string
        cantidad:
          type: integer
        stockMin:
          type: integer
        stockMax:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ArticuloStockPorDepositoResponse:
      type: object
      required: [success, articuloId, stockTotal, enTransito, depositos]
      properties:
        success:
          type: boolean
          enum: [true]
        articuloId:
          type: integer
        stockTotal:
          type: integer
        enTransito:
          type: integer
        depositos:
          type: array
          items:
            $ref: '#/components/schemas/StockDeposito'

    TransferenciaDepositoItem:
      type: object
      required: [id, transferenciaId, articuloId, cantidadEnviada, cantidadRecibida]
      properties:
        id:
          type: integer
        transferenciaId:
          type: integer
        articuloId:
          type: integer
        cantidadEnviada:
          type: integer
        cantidadRecibida:
          type: integer
          nullable: true
        articuloCodigo:
          type: integer
        articuloDescripcion:
          type: string

    TransferenciaDeposito:
      type: object
      required: [id, tenantId, numero, origenId, destinoId, estado, solicitadoPorId, aprobadoPorId, fechaEnvio, fechaRecepcion, nota, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        numero:
          type: integer
        origenId:
          type: integer
        destinoId:
          type: integer
        estado:
          type: string
          enum: [pendiente, en_transito, recibida, anulada]
        solicitadoPorId:
          type: integer
        aprobadoPorId:
          type: integer
          nullable: true
        fechaEnvio:
          type: string
          format: date-time
          nullable: true
        fechaRecepcion:
          type: string
          format: date-time
          nullable: true
        nota:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        origenCodigo:
          type: string
        destinoCodigo:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/TransferenciaDepositoItem'

    TransferenciaDepositoCreateInput:
      type: object
      required: [origenId, destinoId, items]
      properties:
        origenId:
          type: integer
          minimum: 1
        destinoId:
          type: integer
          minimum: 1
        nota:
          type: string
          nullable: true
          maxLength: 200
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [articuloId, cantidadEnviada]
            properties:
              articuloId:
                type: integer
                minimum: 1
              cantidadEnviada:
                type: integer
                minimum: 1

    TransferenciaDepositoRecibirInput:
      type: object
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [articuloId, cantidadRecibida]
            properties:
              articuloId:
                type: integer
                minimum: 1
              cantidadRecibida:
                type: integer
                minimum: 0

    TransferenciaDepositoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/TransferenciaDeposito'

    TransferenciaDepositoListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              enum: [true]
            data:
              type: array
              items:
                $ref: '#/components/schemas/TransferenciaDeposito'
        - $ref: '#/components/schemas/ListPaginationMeta'

    ComisionTipo:
      type: string
      enum: [porcentaje_cobrado, porcentaje_facturado, importe_fijo_por_venta]

    ComisionesSettingsInput:
      type: object
      required: [modoDevengo]
      properties:
        modoDevengo:
          $ref: '#/components/schemas/ComisionTipo'

    ComisionesSettings:
      type: object
      required: [modoDevengo]
      properties:
        modoDevengo:
          $ref: '#/components/schemas/ComisionTipo'

    ComisionesSettingsEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/ComisionesSettings'

    ConfigComision:
      type: object
      required:
        [id, tenantId, vendedorId, tipo, alicuota, vigenciaDesde, vigenciaHasta, articuloCategoriaId, clienteId, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        vendedorId:
          type: integer
        tipo:
          $ref: '#/components/schemas/ComisionTipo'
        alicuota:
          type: number
        vigenciaDesde:
          type: string
          format: date-time
        vigenciaHasta:
          type: string
          format: date-time
          nullable: true
        articuloCategoriaId:
          type: integer
          nullable: true
        clienteId:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        vendedorUsername:
          type: string

    ConfigComisionCreateInput:
      type: object
      required: [vendedorId, tipo, alicuota, vigenciaDesde]
      properties:
        vendedorId:
          type: integer
          minimum: 1
        tipo:
          $ref: '#/components/schemas/ComisionTipo'
        alicuota:
          type: number
          minimum: 0
        vigenciaDesde:
          type: string
          format: date-time
        vigenciaHasta:
          type: string
          format: date-time
          nullable: true
        articuloCategoriaId:
          type: integer
          minimum: 1
          nullable: true
        clienteId:
          type: integer
          minimum: 1
          nullable: true

    ConfigComisionPatchInput:
      type: object
      properties:
        vendedorId:
          type: integer
          minimum: 1
        tipo:
          $ref: '#/components/schemas/ComisionTipo'
        alicuota:
          type: number
          minimum: 0
        vigenciaDesde:
          type: string
          format: date-time
        vigenciaHasta:
          type: string
          format: date-time
          nullable: true
        articuloCategoriaId:
          type: integer
          minimum: 1
          nullable: true
        clienteId:
          type: integer
          minimum: 1
          nullable: true

    ConfigComisionEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/ConfigComision'

    ConfigComisionListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              enum: [true]
            data:
              type: array
              items:
                $ref: '#/components/schemas/ConfigComision'
        - $ref: '#/components/schemas/ListPaginationMeta'

    LiquidacionComisionDetalle:
      type: object
      required: [id, liquidacionId, facturaId, reciboCobroId, imputacionId, montoBase, alicuota, comision, concepto]
      properties:
        id:
          type: integer
        liquidacionId:
          type: integer
        facturaId:
          type: integer
          nullable: true
        reciboCobroId:
          type: integer
          nullable: true
        imputacionId:
          type: integer
          nullable: true
        montoBase:
          type: number
        alicuota:
          type: number
        comision:
          type: number
        concepto:
          type: string

    LiquidacionComision:
      type: object
      required:
        [id, tenantId, vendedorId, periodo, totalVentas, totalComision, estado, aprobadoPorId, pagadoEn, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        vendedorId:
          type: integer
        periodo:
          type: string
          pattern: '^\d{4}-\d{2}$'
        totalVentas:
          type: number
        totalComision:
          type: number
        estado:
          type: string
          enum: [borrador, aprobada, pagada]
        aprobadoPorId:
          type: integer
          nullable: true
        pagadoEn:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        vendedorUsername:
          type: string
        detalle:
          type: array
          items:
            $ref: '#/components/schemas/LiquidacionComisionDetalle'

    LiquidacionComisionEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/LiquidacionComision'

    LiquidacionComisionListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              enum: [true]
            data:
              type: array
              items:
                $ref: '#/components/schemas/LiquidacionComision'
        - $ref: '#/components/schemas/ListPaginationMeta'

    LiquidacionGenerarInput:
      type: object
      required: [periodo]
      properties:
        periodo:
          type: string
          pattern: '^\d{4}-\d{2}$'
        vendedorId:
          type: integer
          minimum: 1

    LiquidacionGenerarEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [created, skipped]
          properties:
            created:
              type: array
              items:
                $ref: '#/components/schemas/LiquidacionComision'
            skipped:
              type: integer

    ComisionRankingRow:
      type: object
      required: [vendedorId, vendedorUsername, totalVentas, totalComision, liquidacionId, estado]
      properties:
        vendedorId:
          type: integer
        vendedorUsername:
          type: string
        totalVentas:
          type: number
        totalComision:
          type: number
        liquidacionId:
          type: integer
          nullable: true
        estado:
          type: string
          nullable: true
          enum: [borrador, aprobada, pagada]

    ComisionRankingEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: '#/components/schemas/ComisionRankingRow'

    MisComisionesResponse:
      type: object
      required: [success, periodo, estimacion, liquidaciones]
      properties:
        success:
          type: boolean
          enum: [true]
        periodo:
          type: string
        estimacion:
          type: object
          required: [totalVentas, totalComision, lineas]
          properties:
            totalVentas:
              type: number
            totalComision:
              type: number
            lineas:
              type: array
              items:
                type: object
                required: [concepto, montoBase, alicuota, comision, facturaId]
                properties:
                  concepto:
                    type: string
                  montoBase:
                    type: number
                  alicuota:
                    type: number
                  comision:
                    type: number
                  facturaId:
                    type: integer
                    nullable: true
        liquidaciones:
          type: array
          items:
            $ref: '#/components/schemas/LiquidacionComision'

    BulkImportValidateSummary:
      type: object
      required: [entity, totalRows, okCount, errorCount, duplicateCount, issues]
      properties:
        entity:
          type: string
          enum: [articulos, clientes, proveedores, saldos]
        totalRows:
          type: integer
        okCount:
          type: integer
        errorCount:
          type: integer
        duplicateCount:
          type: integer
        issues:
          type: array
          items:
            type: object
            required: [row, code, message, kind]
            properties:
              row:
                type: integer
              code:
                type: string
              message:
                type: string
              kind:
                type: string
                enum: [error, duplicate]

    BulkImportValidateEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/BulkImportValidateSummary'

    ImportJob:
      type: object
      required:
        [
          id,
          tenantId,
          entity,
          estado,
          modo,
          duplicateMode,
          totalRows,
          processedRows,
          okCount,
          errorCount,
          duplicateCount,
          createdCount,
          updatedCount,
          skippedCount,
          createdById,
          createdAt,
          updatedAt,
          completedAt,
        ]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        entity:
          type: string
          enum: [articulos, clientes, proveedores, saldos]
        estado:
          type: string
          enum: [validating, ready, running, completed, failed, cancelled]
        modo:
          type: string
          enum: [mejores_esfuerzos, todo_o_nada]
        duplicateMode:
          type: string
          enum: [update, skip]
        totalRows:
          type: integer
        processedRows:
          type: integer
        okCount:
          type: integer
        errorCount:
          type: integer
        duplicateCount:
          type: integer
        createdCount:
          type: integer
        updatedCount:
          type: integer
        skippedCount:
          type: integer
        createdById:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true

    ImportJobEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          enum: [true]
        data:
          $ref: '#/components/schemas/ImportJob'

    CategoriaArticuloCreateInput:
      type: object
      required: [nombre]
      properties:
        nombre:
          type: string
          maxLength: 80
        codigo:
          type: string
          maxLength: 20
          nullable: true
        padreId:
          type: integer
          minimum: 1
          nullable: true
        precioDefault:
          type: number
          minimum: 0
          nullable: true
        activo:
          type: boolean

    CategoriaArticuloPatchInput:
      type: object
      minProperties: 1
      properties:
        nombre:
          type: string
          maxLength: 80
        codigo:
          type: string
          maxLength: 20
          nullable: true
        padreId:
          type: integer
          minimum: 1
          nullable: true
        precioDefault:
          type: number
          minimum: 0
          nullable: true
        activo:
          type: boolean

    CategoriaArticuloEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/CategoriaArticulo'

    CategoriaArticuloListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/CategoriaArticulo'
        - $ref: '#/components/schemas/ListPaginationMeta'

    PrecioCatalogoEfectivoResponse:
      type: object
      required: [success, articuloId, precio, origen, ofertaId]
      properties:
        success:
          type: boolean
          const: true
        articuloId:
          type: integer
        precio:
          type: number
        origen:
          type: string
          enum: [oferta, override_variante, precio_subfamilia, precio_familia, precio_lista1]
        ofertaId:
          type: integer
          nullable: true

    AppUser:
      type: object
      additionalProperties: false
      properties:
        id:
          type: integer
        username:
          type: string
        role:
          type: string
          enum: [super_admin, owner, manager, seller, backoffice, warehouse_op, warehouse_lead, logistics_planner, driver, billing, cashier, collections, finance, auditor]
        active:
          type: boolean
        scopeChannels:
          type: array
          items:
            type: string
            enum: [counter, field, backoffice, warehouse, delivery]
        scopeBranchIds:
          type: array
          items:
            type: integer
        scopeWarehouseIds:
          type: array
          items:
            type: integer
        scopeRouteIds:
          type: array
          items:
            type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AppUserInput:
      type: object
      required: [username, password, role]
      properties:
        username:
          type: string
          minLength: 3
        password:
          type: string
          minLength: 8
        role:
          type: string
          enum: [super_admin, owner, manager, seller, backoffice, warehouse_op, warehouse_lead, logistics_planner, driver, billing, cashier, collections, finance, auditor]
        active:
          type: boolean
          default: true
        scopeChannels:
          type: array
          items:
            type: string
            enum: [counter, field, backoffice, warehouse, delivery]
        scopeBranchIds:
          type: array
          items:
            type: integer
        scopeWarehouseIds:
          type: array
          items:
            type: integer
        scopeRouteIds:
          type: array
          items:
            type: integer

    AppUserUpdateInput:
      type: object
      properties:
        role:
          type: string
          enum: [super_admin, owner, manager, seller, backoffice, warehouse_op, warehouse_lead, logistics_planner, driver, billing, cashier, collections, finance, auditor]
        active:
          type: boolean
        scopeChannels:
          type: array
          items:
            type: string
            enum: [counter, field, backoffice, warehouse, delivery]
        scopeBranchIds:
          type: array
          items:
            type: integer
        scopeWarehouseIds:
          type: array
          items:
            type: integer
        scopeRouteIds:
          type: array
          items:
            type: integer

    AppUserListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/AppUser'

    AppUserEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/AppUser'

    ChangePasswordInput:
      type: object
      required: [currentPassword, newPassword]
      properties:
        currentPassword:
          type: string
        newPassword:
          type: string
          minLength: 8

    ChatMessage:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        fromUserId:
          type: integer
        toUserId:
          type: integer
        content:
          type: string
        createdAt:
          type: string
          format: date-time

    ChatMessageCreateInput:
      type: object
      required: [toUserId, content]
      properties:
        toUserId:
          type: integer
          minimum: 1
        content:
          type: string
          minLength: 1
          maxLength: 1000

    ChatConversation:
      type: object
      properties:
        user:
          type: object
          properties:
            id:
              type: integer
            username:
              type: string
            role:
              type: string
        unreadCount:
          type: integer
          minimum: 0
        lastMessage:
          anyOf:
            - type: 'null'
            - type: object
              properties:
                id:
                  type: integer
                fromUserId:
                  type: integer
                toUserId:
                  type: integer
                preview:
                  type: string
                createdAt:
                  type: string
                  format: date-time

    ChatConversationListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/ChatConversation'

    ChatMessageListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/ChatMessage'

    ChatMessageEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ChatMessage'

    DeliveryZone:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        nombre:
          type: string
        tipo:
          type: string
          enum: [barrio, manual, predefinida]
          default: barrio
        diasEntrega:
          type: string
          nullable: true
          description: Comma-separated delivery day numbers, e.g. "1,3,5"
        horario:
          type: string
          nullable: true
          description: Preferred time window, e.g. "08:00-12:00"
        activo:
          type: boolean
          default: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DeliveryZoneInput:
      type: object
      required: [nombre]
      properties:
        nombre:
          type: string
          minLength: 1
          maxLength: 60
        tipo:
          type: string
          enum: [barrio, manual, predefinida]
          default: barrio
        diasEntrega:
          type: string
          maxLength: 20
        horario:
          type: string
          maxLength: 30

    DeliveryZoneUpdateInput:
      type: object
      properties:
        nombre:
          type: string
          minLength: 1
          maxLength: 60
        tipo:
          type: string
          enum: [barrio, manual, predefinida]
        diasEntrega:
          type: string
          maxLength: 20
        horario:
          type: string
          maxLength: 30
        activo:
          type: boolean

    DeliveryZoneListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/DeliveryZone'
        - $ref: '#/components/schemas/ListPaginationMeta'

    DeliveryZoneEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DeliveryZone'

    EmpresaConfig:
      type: object
      required: [nombre, cuit, puntoVenta, tipoFactura, prefijoFactura]
      properties:
        id:
          type: integer
          nullable: true
          description: Null when settings have not been saved yet (defaults only).
        nombre:
          type: string
          maxLength: 40
        cuit:
          type: string
          maxLength: 14
        domicilio:
          type: string
          nullable: true
          maxLength: 40
        puntoVenta:
          type: integer
          minimum: 1
          maximum: 9999
        tipoFactura:
          type: string
          enum: [A, B, C]
        logoUrl:
          type: string
          nullable: true
          maxLength: 255
        prefijoFactura:
          type: string
          description: Four-digit invoice prefix derived from puntoVenta.
          pattern: '^[0-9]{4}$'
        recordatorioDiasGracia:
          type: integer
          minimum: 0
          maximum: 365
          description: Grace days after due date before an invoice is eligible for collection reminders.
        timezone:
          type: string
          maxLength: 64
          description: IANA time zone for the daily reminder job and business-hour window.
          example: America/Argentina/Buenos_Aires
        recordatorioHoraInicio:
          type: integer
          minimum: 0
          maximum: 23
          description: Business-hour window start (tenant local hour, inclusive).
        recordatorioHoraFin:
          type: integer
          minimum: 1
          maximum: 24
          description: Business-hour window end (tenant local hour, exclusive).
        condicionIva:
          type: string
          enum: [RI, Mono, CF, Exento]
          description: Issuer VAT condition for legal invoice PDF header (#148).
        ingresosBrutos:
          type: string
          nullable: true
          maxLength: 30
        fechaInicioActividades:
          type: string
          nullable: true
          format: date
          description: Activity start date (YYYY-MM-DD).

    EmpresaInput:
      type: object
      required: [nombre, cuit, puntoVenta, tipoFactura]
      properties:
        nombre:
          type: string
          maxLength: 40
        cuit:
          type: string
          maxLength: 14
        domicilio:
          type: string
          nullable: true
          maxLength: 40
        puntoVenta:
          type: integer
          minimum: 1
          maximum: 9999
        tipoFactura:
          type: string
          enum: [A, B, C]
        logoUrl:
          type: string
          nullable: true
          maxLength: 255
        recordatorioDiasGracia:
          type: integer
          minimum: 0
          maximum: 365
        timezone:
          type: string
          maxLength: 64
        recordatorioHoraInicio:
          type: integer
          minimum: 0
          maximum: 23
        recordatorioHoraFin:
          type: integer
          minimum: 1
          maximum: 24
        condicionIva:
          type: string
          enum: [RI, Mono, CF, Exento]
        ingresosBrutos:
          type: string
          nullable: true
          maxLength: 30
        fechaInicioActividades:
          type: string
          nullable: true
          format: date

    EmpresaEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/EmpresaConfig'

    PortalConfig:
      type: object
      required: [enabled, showPedidos, logoUrl, primaryColor, footerText]
      properties:
        enabled:
          type: boolean
        showPedidos:
          type: boolean
        logoUrl:
          type: string
          nullable: true
        primaryColor:
          type: string
          nullable: true
        footerText:
          type: string
          nullable: true

    PortalConfigInput:
      type: object
      properties:
        enabled:
          type: boolean
        showPedidos:
          type: boolean
        logoUrl:
          type: string
          nullable: true
        primaryColor:
          type: string
          nullable: true
          pattern: '^#[0-9A-Fa-f]{6}$'
        footerText:
          type: string
          nullable: true

    PortalConfigEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/PortalConfig'

    PortalBranding:
      type: object
      required: [tenantName, tenantSlug, enabled, showPedidos, logoUrl, primaryColor, footerText]
      properties:
        tenantName:
          type: string
        tenantSlug:
          type: string
        enabled:
          type: boolean
        showPedidos:
          type: boolean
        logoUrl:
          type: string
          nullable: true
        primaryColor:
          type: string
          nullable: true
        footerText:
          type: string
          nullable: true

    PortalBrandingEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/PortalBranding'

    PortalMagicLinkInput:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email

    PortalMagicLinkSentEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [sent]
          properties:
            sent:
              type: boolean
              const: true

    PortalMe:
      type: object
      required: [clienteId, codigo, rsocial, fantasia, email, telef, domicilio, localidad, vendedor]
      properties:
        clienteId:
          type: integer
        codigo:
          type: integer
        rsocial:
          type: string
        fantasia:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        telef:
          type: string
          nullable: true
        domicilio:
          type: string
          nullable: true
        localidad:
          type: string
          nullable: true
        vendedor:
          type: object
          nullable: true
          properties:
            id:
              type: integer
            username:
              type: string

    PortalVerifyEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [me]
          properties:
            me:
              $ref: '#/components/schemas/PortalMe'

    PortalMeEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [me, branding]
          properties:
            me:
              $ref: '#/components/schemas/PortalMe'
            branding:
              $ref: '#/components/schemas/PortalBranding'

    PortalLogoutEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [loggedOut]
          properties:
            loggedOut:
              type: boolean
              const: true

    PortalFacturaRow:
      type: object
      required: [id, ref, fecha, total, pagado, pendiente, estado]
      properties:
        id:
          type: integer
        ref:
          type: string
        fecha:
          type: string
          format: date-time
        total:
          type: string
        pagado:
          type: string
        pendiente:
          type: string
        estado:
          type: string
          enum: [pagada, pendiente, vencida]
        mpPaymentLink:
          type: string
          format: uri
        mpEstado:
          type: string

    PortalFacturaListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [facturas, total]
          properties:
            facturas:
              type: array
              items:
                $ref: '#/components/schemas/PortalFacturaRow'
            total:
              type: integer

    PortalPedidoRow:
      type: object
      required: [id, estado, total, createdAt, validUntil, facturaRef, remitoEstado]
      properties:
        id:
          type: integer
        estado:
          type: string
        total:
          type: string
        createdAt:
          type: string
          format: date-time
        validUntil:
          type: string
          format: date-time
          nullable: true
        facturaRef:
          type: string
          nullable: true
        remitoEstado:
          type: string
          nullable: true

    PortalPedidoListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [pedidos, total]
          properties:
            pedidos:
              type: array
              items:
                $ref: '#/components/schemas/PortalPedidoRow'
            total:
              type: integer

    FacturaPendienteEstado:
      type: string
      enum: [pendiente, proxima_vencer, vencida_hoy, vencida_critica]

    FacturaPendienteRow:
      type: object
      required:
        - comprobanteCompraId
        - proveedorId
        - proveedorCodigo
        - proveedorRsocial
        - facturaRef
        - fecha
        - vencimiento
        - total
        - pagado
        - pendiente
        - estado
        - diasHastaVencimiento
        - diasVencido
      properties:
        comprobanteCompraId:
          type: integer
        proveedorId:
          type: integer
        proveedorCodigo:
          type: integer
        proveedorRsocial:
          type: string
        facturaRef:
          type: string
        fecha:
          type: string
          format: date-time
        vencimiento:
          type: string
          format: date-time
        total:
          type: string
        pagado:
          type: string
        pendiente:
          type: string
        estado:
          $ref: '#/components/schemas/FacturaPendienteEstado'
        diasHastaVencimiento:
          type: integer
        diasVencido:
          type: integer

    FacturasPendientesEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/FacturaPendienteRow'

    AlertaProveedorConfig:
      type: object
      required: [diasPrevioAviso, diasCritico, notifEmail, notifInApp]
      properties:
        diasPrevioAviso:
          type: integer
          minimum: 0
          maximum: 90
        diasCritico:
          type: integer
          minimum: 1
          maximum: 365
        notifEmail:
          type: boolean
        notifInApp:
          type: boolean

    AlertaProveedorConfigInput:
      type: object
      properties:
        diasPrevioAviso:
          type: integer
          minimum: 0
          maximum: 90
        diasCritico:
          type: integer
          minimum: 1
          maximum: 365
        notifEmail:
          type: boolean
        notifInApp:
          type: boolean

    AlertaProveedorConfigEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/AlertaProveedorConfig'

    DashboardFacturasPagarWidget:
      type: object
      required: [vencido, proximoVencer]
      properties:
        vencido:
          type: object
          required: [count, total]
          properties:
            count:
              type: integer
            total:
              type: string
        proximoVencer:
          type: object
          required: [count, total]
          properties:
            count:
              type: integer
            total:
              type: string

    DashboardSummary:
      type: object
      required: [ventasHoy, facturasVencidas, cobrosHoy, alertasActivas, facturasPagar]
      properties:
        ventasHoy:
          type: object
          required: [count, total]
          properties:
            count:
              type: integer
            total:
              type: string
        facturasVencidas:
          type: object
          required: [count, total]
          properties:
            count:
              type: integer
            total:
              type: string
        cobrosHoy:
          type: object
          required: [count, total]
          properties:
            count:
              type: integer
            total:
              type: string
        alertasActivas:
          type: integer
        facturasPagar:
          $ref: '#/components/schemas/DashboardFacturasPagarWidget'

    DashboardSummaryEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DashboardSummary'

    DashboardVentasSeriesRow:
      type: object
      required: [period, count, total]
      properties:
        period:
          type: string
        count:
          type: integer
        total:
          type: string

    DashboardTopArticuloRow:
      type: object
      required: [articuloId, codigo, descripcion, quantity, total]
      properties:
        articuloId:
          type: integer
        codigo:
          type: integer
        descripcion:
          type: string
        quantity:
          type: integer
        total:
          type: string

    DashboardVentasBySellerRow:
      type: object
      required: [vendedorId, username, count, total]
      properties:
        vendedorId:
          type: integer
          nullable: true
        username:
          type: string
          nullable: true
        count:
          type: integer
        total:
          type: string

    DashboardVentasHistorico:
      type: object
      required: [series, topArticles, bySeller]
      properties:
        series:
          type: array
          items:
            $ref: '#/components/schemas/DashboardVentasSeriesRow'
        topArticles:
          type: array
          items:
            $ref: '#/components/schemas/DashboardTopArticuloRow'
        bySeller:
          type: array
          items:
            $ref: '#/components/schemas/DashboardVentasBySellerRow'

    DashboardVentasHistoricoEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/DashboardVentasHistorico'

    AuditEvent:
      type: object
      required: [id, tenantId, action, resource, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        userId:
          type: integer
          nullable: true
        username:
          type: string
          nullable: true
          description: Username of the actor when resolved; otherwise null
        action:
          type: string
        resource:
          type: string
        resourceId:
          type: string
          nullable: true
        ipAddress:
          type: string
          nullable: true
        metadata:
          description: Optional structured details for the event
          anyOf:
            - type: object
              additionalProperties: true
            - type: 'null'
        createdAt:
          type: string
          format: date-time

    AuditEventListEnvelope:
      allOf:
        - type: object
          required: [success, data]
          properties:
            success:
              type: boolean
              const: true
            data:
              type: array
              items:
                $ref: '#/components/schemas/AuditEvent'
        - $ref: '#/components/schemas/ListPaginationMeta'

    Notification:
      type: object
      required: [id, tenantId, userId, type, payload, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        userId:
          type: integer
        type:
          type: string
        payload:
          type: object
          additionalProperties: true
        readAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time

    NotificationListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/Notification'

    NotificationEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/Notification'

    NotificationReadAllEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          required: [updated]
          properties:
            updated:
              type: integer

    ModuleCatalogEntry:
      type: object
      required:
        - key
        - label
        - required
        - requiredInProd
        - dependencies
        - plan
        - price
        - canDeactivate
      properties:
        key:
          type: string
        label:
          type: string
        required:
          type: boolean
        requiredInProd:
          type: boolean
        dependencies:
          type: array
          items:
            type: string
        plan:
          type: string
          enum: [starter, pro, enterprise]
        price:
          type: number
        canDeactivate:
          type: boolean

    ModuleCatalogPreset:
      type: object
      required: [modules]
      properties:
        modules:
          type: array
          items:
            type: string

    ModuleCatalogData:
      type: object
      required: [deploymentEnv, modules, presets]
      properties:
        deploymentEnv:
          type: string
          enum: [dev, prod]
        modules:
          type: array
          items:
            $ref: '#/components/schemas/ModuleCatalogEntry'
        presets:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/ModuleCatalogPreset'

    ModuleCatalogEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/ModuleCatalogData'

    TenantFeaturesData:
      type: object
      required: [modules, integrations]
      properties:
        modules:
          type: array
          items:
            type: string
        integrations:
          type: array
          items:
            type: string

    TenantFeaturesEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TenantFeaturesData'

    PublicPlan:
      type: object
      required: [key, name, monthlyPrice, currency, maxUsers, maxInvoicesPerMonth, features]
      properties:
        key:
          type: string
        name:
          type: string
        monthlyPrice:
          type: integer
        currency:
          type: string
        maxUsers:
          type: integer
          nullable: true
        maxInvoicesPerMonth:
          type: integer
          nullable: true
        features:
          type: array
          items:
            type: string

    PlanCatalogEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/PublicPlan'

    TenantPlanUsage:
      type: object
      required: [usersUsed, invoicesUsed]
      properties:
        usersUsed:
          type: integer
        invoicesUsed:
          type: integer

    TenantPlanSnapshot:
      type: object
      required:
        - planKey
        - planName
        - monthlyPrice
        - currency
        - maxUsers
        - maxInvoicesPerMonth
        - features
        - status
        - usage
      properties:
        planKey:
          type: string
        planName:
          type: string
        monthlyPrice:
          type: integer
        currency:
          type: string
        maxUsers:
          type: integer
          nullable: true
        maxInvoicesPerMonth:
          type: integer
          nullable: true
        features:
          type: array
          items:
            type: string
        status:
          type: string
        usage:
          $ref: '#/components/schemas/TenantPlanUsage'

    TenantPlanSnapshotEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TenantPlanSnapshot'

    TenantPlanChangeBody:
      type: object
      required: [planKey, reason]
      properties:
        planKey:
          type: string
          enum: [starter, pro, enterprise, trial]
        reason:
          type: string
          maxLength: 500

    PlanLimitErrorEnvelope:
      type: object
      required: [success, error]
      properties:
        success:
          type: boolean
          const: false
        error:
          type: string
          enum:
            - plan_limit_users
            - plan_limit_invoices
            - plan_feature_required
        feature:
          type: string
        currentPlan:
          type: string

    TenantConfig:
      type: object
      required: [tenantId, businessType, rubros, plan, modules, integrations, updatedAt]
      properties:
        tenantId:
          type: integer
        businessType:
          type: string
          enum: [mayorista, minorista, ambos]
        rubros:
          type: array
          items:
            type: string
        plan:
          type: string
          enum: [starter, pro, enterprise]
        modules:
          type: array
          items:
            type: string
        integrations:
          type: array
          items:
            type: string
        updatedAt:
          type: string
          format: date-time

    TenantConfigEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TenantConfig'

    TenantConfigUpsertBody:
      type: object
      required: [modules, reason]
      properties:
        businessType:
          type: string
          enum: [mayorista, minorista, ambos]
        rubros:
          type: array
          items:
            type: string
        plan:
          type: string
          enum: [starter, pro, enterprise]
        modules:
          type: array
          items:
            type: string
        integrations:
          type: array
          items:
            type: string
        reason:
          type: string
          minLength: 1
          maxLength: 500

    TenantConfigApplyTemplateBody:
      type: object
      required: [preset]
      properties:
        preset:
          type: string
        reason:
          type: string
          maxLength: 500

    TenantConfigHistoryEntry:
      type: object
      required: [id, changedById, before, after, createdAt]
      properties:
        id:
          type: integer
        changedById:
          type: integer
        before:
          type: object
          additionalProperties: true
        after:
          type: object
          additionalProperties: true
        reason:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time

    TenantConfigHistoryData:
      type: object
      required: [total, items]
      properties:
        total:
          type: integer
        items:
          type: array
          items:
            $ref: '#/components/schemas/TenantConfigHistoryEntry'

    TenantConfigHistoryEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TenantConfigHistoryData'

    TenantPricingAddon:
      type: object
      required: [moduleKey, price]
      properties:
        moduleKey:
          type: string
        price:
          type: number
          minimum: 0

    TenantPricingData:
      type: object
      required: [plan, basePrice, addons, totalMonthly]
      properties:
        plan:
          type: string
        basePrice:
          type: number
          minimum: 0
        addons:
          type: array
          items:
            $ref: '#/components/schemas/TenantPricingAddon'
        totalMonthly:
          type: number
          minimum: 0

    TenantPricingEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TenantPricingData'

    TenantModuleTrial:
      type: object
      required: [id, tenantId, moduleKey, expiresAt, active, daysRemaining, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        moduleKey:
          type: string
        expiresAt:
          type: string
          format: date-time
        active:
          type: boolean
        daysRemaining:
          type: integer
          minimum: 0
        createdAt:
          type: string
          format: date-time

    TenantModuleTrialListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/TenantModuleTrial'

    TenantModuleTrialEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/TenantModuleTrial'

    TenantModuleTrialActivateBody:
      type: object
      required: [moduleKey]
      properties:
        moduleKey:
          type: string
        days:
          type: integer
          minimum: 1
          maximum: 365
          default: 30
        reason:
          type: string
          maxLength: 500

    SuperadminGlobalStats:
      type: object
      required: [activeTenants, totalTenants, inactiveTenants, facturasToday, totalUsers]
      properties:
        activeTenants:
          type: integer
          minimum: 0
        totalTenants:
          type: integer
          minimum: 0
        inactiveTenants:
          type: integer
          minimum: 0
        facturasToday:
          type: integer
          minimum: 0
        totalUsers:
          type: integer
          minimum: 0

    SuperadminGlobalStatsEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/SuperadminGlobalStats'

    SuperadminTenantListRow:
      type: object
      required: [id, name, slug, active, plan, userCount, facturaCount, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        active:
          type: boolean
        plan:
          type: string
          nullable: true
        userCount:
          type: integer
          minimum: 0
        facturaCount:
          type: integer
          minimum: 0
        createdAt:
          type: string
          format: date-time

    SuperadminTenantListEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/SuperadminTenantListRow'

    SuperadminTenantStats:
      type: object
      required: [userCount, facturaCount, pedidoCount, clienteCount]
      properties:
        userCount:
          type: integer
          minimum: 0
        facturaCount:
          type: integer
          minimum: 0
        pedidoCount:
          type: integer
          minimum: 0
        clienteCount:
          type: integer
          minimum: 0

    SuperadminTenantDetail:
      type: object
      required:
        - id
        - name
        - slug
        - active
        - createdAt
        - updatedAt
        - plan
        - modulesCount
        - configUpdatedAt
        - stats
        - lastActivityAt
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        plan:
          type: string
          nullable: true
        modulesCount:
          type: integer
          minimum: 0
        configUpdatedAt:
          type: string
          format: date-time
          nullable: true
        stats:
          $ref: '#/components/schemas/SuperadminTenantStats'
        lastActivityAt:
          type: string
          format: date-time
          nullable: true

    SuperadminTenantDetailEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/SuperadminTenantDetail'

    SuperadminTenantCreateInput:
      type: object
      required: [name, slug]
      properties:
        name:
          type: string
          minLength: 1
        slug:
          type: string
          minLength: 1
        plan:
          type: string
          enum: [starter, pro, enterprise]
        ownerUsername:
          type: string
        ownerPassword:
          type: string
          format: password

    SuperadminTenantCreateResult:
      type: object
      required: [tenantId, ownerUserId]
      properties:
        tenantId:
          type: integer
        ownerUserId:
          type: integer
          nullable: true

    SuperadminTenantCreateEnvelope:
      type: object
      required: [success, data]
      properties:
        success:
          type: boolean
          const: true
        data:
          $ref: '#/components/schemas/SuperadminTenantCreateResult'

    SuperadminTenantPatchInput:
      type: object
      required: [active]
      properties:
        active:
          type: boolean

    ModuleNotEnabledEnvelope:
      type: object
      required: [success, error, module]
      properties:
        success:
          type: boolean
          const: false
        error:
          type: string
          const: module_not_enabled
        module:
          type: string

  responses:
    BadRequest:
      description: Request payload is invalid
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    UnauthorizedError:
      description: Authentication required or invalid credentials
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    ForbiddenError:
      description: Authenticated but missing permission
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    ConflictError:
      description: Resource conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    NotFoundError:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    TooManyRequests:
      description: Rate limit exceeded
      headers:
        X-RateLimit-Limit:
          schema:
            type: integer
        X-RateLimit-Remaining:
          schema:
            type: integer
        X-RateLimit-Reset:
          schema:
            type: string
            format: date-time
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
    ServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorEnvelope'
