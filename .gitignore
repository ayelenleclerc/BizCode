node_modules/
.pnpm-store/
.turbo/
package-lock.json
uploads/
.bizcode-backups/
data/documentos-compra/
data/documentos-compra-templates/
dist/
packages/*/dist/
build/

# Local legacy / reference copy (not part of the repo tree; match casing on disk / Git)
Programa_Viejo/
programa_viejo/

.env
e2e/.auth/
*.tsbuildinfo
*.log
.DS_Store
.claude/
.vite/
src-tauri/target/
# Legacy / local SQL at repo root only — Prisma history lives in prisma/migrations/
/migrations/
coverage/
docs/generated/.schema-md-stamp/

playwright-report/
test-results/
blob-report/
zap-out/

# Local Cursor plan copies (canonical plan lives under docs/*/quality/ per DOCUMENT_LOCALE_MAP.md)
.cursor/plans/

# Optional full SBOM (large); default committed file is sbom-cyclonedx.json
docs/evidence/sbom-cyclonedx-full.json

# Scratch outputs from ISO map / package-index helpers (regenerate via scripts)
scripts/_*.md
scripts/_*.txt
node_modules
BizCode.code-workspace

# plan:sync local reports (state under .github/plan-sync/state/ is versioned)
.github/plan-sync/reports/
