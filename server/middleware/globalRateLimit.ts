/**
 * @en Back-compat re-exports for route-group HTTP rate limiting (#87).
 * @es Reexportaciones por compatibilidad del rate limit HTTP por grupo de ruta (#87).
 * @pt-BR Reexportações de compatibilidade do rate limit HTTP por grupo de rota (#87).
 */
export { apiHttpRateLimiter, routeHttpRateLimiter, routeHttpRateLimiter as globalHttpRateLimiter } from './routeRateLimit'
